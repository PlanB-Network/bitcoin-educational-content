---
term: OP_BOOLAND (0X9A)
definition: Opcode-ی که یک عمل 'و' منطقی (AND) روی دو مقدار بالای استک انجام می‌دهد.
---

رفتار یک گیت منطقی `AND` را تقلید می‌کند. دو مقدار را از بالای پشته می‌گیرد و فقط در صورتی که هر دو مقدار غیرصفر باشند، `1` را برمی‌گرداند. در غیر این صورت، `0` را برمی‌گرداند.
---
term: Client-side Validation
---

各方（客户）根据协议规则对一组私下交换的数据进行验证的过程。在 RGB 协议中，这些交换数据被组合在一起，称为 "委托"。Bitcoin 协议要求在 On-Chain 中公布所有交易，与之不同的是，RGB 只允许公开存储承诺（锚定在 Bitcoin），而基本的 Contract 信息（过渡、证明、证明）则保留在 off-chain 中，只在相关用户之间共享。
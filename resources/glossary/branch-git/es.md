---
term: SUCURSAL (GIT)
---

En el contexto de Git, representa una separación del flujo de trabajo principal, permitiendo el desarrollo paralelo sin afectar a la rama principal (normalmente llamada `master` o `main`). Las ramas permiten realizar modificaciones, pruebas y experimentos en un entorno aislado, antes de integrarlas en el proyecto principal mediante una fusión.
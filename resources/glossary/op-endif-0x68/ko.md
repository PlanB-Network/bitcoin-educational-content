---
term: OP_ENDIF (0x68)
---

일반적으로 하나 이상의 `OP_IF` 또는 `OP_NOTIF`로 시작된 조건부 제어 구조의 끝을 표시하며, 그 뒤에 하나 이상의 `OP_ELSE`가 이어집니다. 이는 실행된 분기에 관계없이 스크립트 실행이 조건부 구조를 넘어 계속되어야 함을 나타냅니다. 즉, `OP_ENDIF`는 스크립트에서 조건부 블록의 끝을 나타내는 역할을 합니다.
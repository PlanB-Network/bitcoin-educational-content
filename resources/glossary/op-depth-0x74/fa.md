---
term: OP_DEPTH (0X74)
definition: Opcode-ی که تعداد عناصر موجود در استک را به خود استک می‌فرستد.
---

تعداد Elements در پشته را به خود پشته اضافه می‌کند. اگر پشته شامل `N` Elements باشد، `OP_DEPTH` عدد `N` را به عنوان یک عنصر جدید در بالای پشته اضافه خواهد کرد.
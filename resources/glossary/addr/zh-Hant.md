---
term: Addr
definition: 一條舊的比特幣網絡消息，允許通信接受連接的節點的IP地址。由addrv2 (BIP155)取代以支持更長的地址格式。
---

以前在 Bitcoin 上使用的網路訊息，用來傳達接受進入連線的節點地址。這種舊格式每個 Address 的位址限制為 128 位元，只適用於 IPv6、IPv4 和版本 2 的 Tor 位址。隨著 Tor V3 等新通訊協定的出現，以及未來網路通訊協定需要有更好的擴充性，`addr` 格式被 BIP155 中引入的 `addrv2` 所取代。
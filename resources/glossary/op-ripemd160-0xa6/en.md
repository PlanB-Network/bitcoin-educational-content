---
term: OP_RIPEMD160 (0XA6)
definition: Opcode hashing the element at the top of the stack with RIPEMD160.
---

Takes the top item from the stack and replaces it with its hash using the `RIPEMD160` function.


question: 在平行組合中，輸入會發生什麼？
answer: 兩個運算都接收相同的輸入，且它們的輸出構成一個積（配對）。
wrong_answers:
  - 輸入被切成兩半，每個運算各接收一個分開的部分。
  - 一個運算先處理輸入，另一個接收被修改後的結果。
  - 輸入被加上標籤並僅路由給兩個運算中的一個來處理。
explanation: >-
  平行組合同時將相同且未修改的輸入交給兩個運算。結果被打包成一個積型別（配對）。在 Simplicity 中，這使用 pair 組合子，寫作 f ▵ g，對於輸入 a 會產生 ⟨f(a), g(a)⟩。
reviewed: false

---
term: BIP0031

definition: 引入针对 ping 的 pong 响应消息，以验证比特币网络节点之间的连接性。
---
这个 BIP 旨在改进比特币节点网络管理机制的提案。在 BIP31 启动之前，比特币节点无法直接知道其对等节点是否仍在连接、是否运行或是否未超载。BIP31 引入了 `pong` 消息，作为对 `ping` 消息的回应，可以主动检查节点之间的连接情况。

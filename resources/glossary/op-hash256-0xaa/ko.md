---
term: OP_HASH256 (0XAA)
definition: 최상단 요소를 두 번 연속 SHA256으로 해싱하는 Opcode.
---

스택에서 최상위 항목을 가져와서 `SHA256` 함수를 두 번 사용하여 해당 Hash으로 바꿉니다. 입력은 `SHA256`으로 한 번 해시되고, 다이제스트는 `SHA256`으로 두 번 해시됩니다. 이 2단계 과정을 통해 256비트 지문이 생성됩니다.
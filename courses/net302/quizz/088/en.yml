question: Which Linux command displays the active network routing table?
answer: ip route show
wrong_answers:
  - ifconfig -a
  - ping route
  - netstat -s
explanation: |
  ip route show lists the routes known to the kernel and the paths taken by IP packets.
reviewed: false
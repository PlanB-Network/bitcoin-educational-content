# Lessons — sw

## 2026-07-03
- Kwa maudhui ya Simplicity, hifadhi majina ya combinators na istilahi za utekelezaji kama `iden`, `comp`, `case`, `take`, `drop`, `pair`, `jet`, `CMR`, `Writer`, `Reader`, `Failure`, `key-spend`, na `script path` kwa Kiingereza; tafsiri tu prose inayozunguka.
- Kwa mujibu wa glossary ya Swahili, tumia `Shina la Merkle` kwa `Merkle root` na `Mti wa Merkle` kwa `Merkle tree`, huku `Taproot`, `UTXO`, na `Schnorr` zikibaki kama majina ya itifaki/istilahi.
- For Simplicity formal-course material in Swahili, keep formal effect/property labels such as `Failure`, `Reader`, `Writer`, `commutative`, `idempotent`, and `unitary` in English when they function as technical names, and translate the surrounding prose (for example, `athari ya Failure`).
- Keep protocol/programming labels that are also code-adjacent concepts in English in prose when translating them would obscure references: `witness`, `jet`, `expression`, `sum`, `product`, `unit`, `Option`, `TapLeaf`, `CMR`, and similar terms.
- In Simplicity formal material, keep code/formal labels such as `case`, `take`, `drop`, `iden`, `combinator`, `branch`, `jet`, `script`, `CMR`, and `TapLeaf` in English, adapting only Swahili grammar around them.
- Use the glossary's `Shina la Merkle` for a generic Merkle root, but keep the named Simplicity term `Commitment Merkle Root (CMR)` in English.
- For Simplicity/type-theory quizzes, keep protocol/combinator/type-theory tokens such as `Simplicity`, `Bitcoin Script`, `iden`, `unit`, `comp`, `pair`, `case`, `take`, `drop`, `injl`, `injr`, `product`, `sum`, `boolean`, `expression`, and `DAG` in English; translate surrounding prose.
- Render `static analysis` as `uchambuzi tuli`, and `dynamic gas model` as `mfano wa gas unaobadilika`.
- The Swahili glossary currently gives `smart-contract` as `Contrat intelligent`; use that canonical form when the English source says smart contract.

## 2026-07-06
- Kwa maudhui ya falsafa ya kisiasa, tumia `dola` kwa `the state` kama taasisi ya mamlaka/udhibiti, na `serikali` kwa `government` kama chombo cha kiutendaji.
- Tafsiri `libertarianism` kama `ulibertaria` na `libertarians` kama `walibertaria`; hifadhi lebo maalum `anarcho-capitalists` na `minarchists` kwa Kiingereza pale zinapotokea kama majina ya makundi.
- Kwa `Nolan diagram` tumia `mchoro wa Nolan`; kwa `Nolan diamond` tumia `almasi ya Nolan`.
- For SOC/political-theory quizzes in Swahili, keep movement labels such as `libertarian`, `paleo-libertarianism`, `neo-libertarianism`, `liberal`, `liberalism`, `conservatives`, `neoconservatives`, `laissez-faire`, and `spontaneous order` in English when they function as named ideological terms; translate the surrounding prose.
- Render `statism` as `ustatisti`, and use descriptive forms like `wanaounga mkono ustatisti` for `statists` when a direct noun would sound unnatural.
- For the Genesis block source headline, keep the exact embedded English headline (`The Times 03/Jan/2009 Chancellor on the brink of a second bank bailout`) and translate only the surrounding explanatory prose.
- In SOC104 political-philosophy quizzes, keep technical labels such as `constructivism`, `anti-constructivism`, `code`, `server`, `open-source`, `mining`, and `white paper` in English when they function as named concepts or protocol-adjacent terms.
- Kwa maswali ya falsafa ya kisiasa, tumia `uhafidhina`, `ujamaa`, `msimamo wa kati`, na `ulibertaria`; kwa matawi mahususi kama `minarchism`, `anarcho-capitalists`, `pro-business`, na `pro-market`, hifadhi istilahi za Kiingereza na badilisha sarufi ya Kiswahili inayozizunguka.
- Kwa maudhui ya fedha ya libertarian, tumia glossary `Fiat` kama ilivyo, `mfumuko wa bei` kwa `inflation`, `fedha thabiti` kwa `sound money`, na `kiwango cha dhahabu` kwa `gold standard`.
- Tumia `klasiki` badala ya `kale` kwa `classical` katika istilahi za falsafa/uchumi kama `classical liberalism` na `classical economics`.
- Kwa maswali ya SOC104, tumia `utotalitari`/`kitotalitari` kwa `totalitarianism`/`totalitarian`, huku `dola` ikitumika kwa `state` kama taasisi ya kisiasa.
- Kwa familia za kisiasa, tumia `wahafidhina` kwa `conservatives`, `walibertaria`/`kilibertaria` kwa `libertarians`/`libertarian`, `wanajamaa`/`kijamaa` kwa `socialists`/`socialist`, na `wenye msimamo wa kati` kwa `centrists`.
- Hifadhi `moral hazard` kwa Kiingereza kama istilahi ya kiuchumi, kisha tafsiri maelezo yanayoizunguka.
- Kwa maudhui ya SOC104 yanayotofautisha `libertaires` wa Kifaransa na `libertarians`, hifadhi istilahi hizo kwa Kiingereza/Kifaransa ili kuepuka kuchanganya dhana; tafsiri prose inayozunguka.
- Tumia `mali binafsi` kwa `private property`, `dola` kwa `state`, `usoshalisti` kwa `socialism`, na `libertarianism` ibaki kama istilahi ya kiitikadi inapolinganishwa moja kwa moja na `libertaires`.
- For SOC/political-philosophy material, use `walibertari` / `kilibertari` for libertarians/libertarian, `wahafidhina` for conservatives, `wanasoshalisti` for socialists, and `watu wa msimamo wa kati` for centrists.
- Render Hayek's `spontaneous order` as `utaratibu unaojitokeza wenyewe` to avoid confusing it with merely voluntary action (`hiari`).

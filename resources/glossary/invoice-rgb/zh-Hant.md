---
term: Invoice RGB
definition: 允許構建狀態轉換 (State Transition) 以轉移 RGB 資產的編碼 URL。
---

在 RGB 協定中，Invoice 採用以 base58 編碼的 URL 形式，其中包含付款方構建 State Transition 所需的資訊。實際上，這是一個 Invoice，可讓 generate 的對手方進行適當的轉換，以轉移資產或更新 Contract 的狀態。
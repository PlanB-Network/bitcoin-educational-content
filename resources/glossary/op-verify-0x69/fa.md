---
term: OP_VERIFY (0X69)
definition: Opcode که نیاز دارد بالای پشته غیر صفر باشد، در غیر این صورت تراکنش نامعتبر است.
---

نیاز دارد که مقدار بالای پشته غیر صفر (صحیح) باشد. اگر اینطور نباشد، تراکنش نامعتبر است. `OP_VERIFY` برای تأیید شرایط اسکریپت استفاده می‌شود.
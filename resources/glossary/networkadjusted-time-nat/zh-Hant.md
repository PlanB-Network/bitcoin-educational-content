---
term: 網路調整時間 (nat)
---

根據網路節點的時鐘建立的通用時間估算。每個節點在計算其 NAT 時，先將其本機時鐘 (UTC) 與所連接節點的時差中位數加起來，再將其本機時鐘與這些時差中位數加起來，最多不超過 70 分鐘。因此，網路調整時間是每個節點在本地計算的節點時間的中值。這個參考框架之後會用來驗證區塊時間戳記的有效性。事實上，一個區塊若要被節點接受，其 Timestamp 必須介於 MTP（最近 11 個挖出區塊的中位時間）和 NAT 加 2 小時之間：


```text
MTP < Valid Timestamp < (NAT + 2h)
```
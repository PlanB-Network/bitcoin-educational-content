question: 如果 Rust 方法需要修改結構實體，同時允許呼叫者保留所有權，它應該使用哪種參數類型？
answer: "&mut self"
wrong_answers:
  - "&self"
  - 自我
  - mut 自
explanation: |
  需要修改結構實體，同時允許呼叫者保留所有權的方法，應該使用 `&mut self` 作為第一個參數。這會建立一個對實體的可變參照，讓方法可以在不奪走呼叫者所有權的情況下改變結構的欄位。使用 `&self` 只允許讀取實體，而 `self` 則會完全消耗實體，而且 `mut self` 並不是方法參數的有效 Rust 語法。
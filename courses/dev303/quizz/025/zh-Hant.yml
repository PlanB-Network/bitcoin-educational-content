question: 什麼關鍵字用來定義 Rust 中包含方法和結構相關函數的實作區塊？
answer: 植入物
wrong_answers:
  - 方法
  - 結構
  - fn
explanation: |
  `impl` 關鍵字是專門用來建立實作區塊的，在這裡您可以定義方法和結構的相關函式。當 `struct` 定義結構本身，`fn` 定義個別函數，而 `method` 完全不是 Rust 關鍵字時，只有 `impl` 建立了包含與類型相關行為的區塊。使用 `impl` 的實作區塊允許您在結構中加入功能，讓它們不只是資料容器。
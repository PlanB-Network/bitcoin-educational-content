---
term: Contract RGB
---

指通过 RGB 协议在多个行为者之间以数字方式执行的一组权限。它有一个激活状态和一个由 Schema 定义的 Business Logic，Business Logic 规定了哪些操作是授权的（转接、扩展等）。Contract 的状态和有效性规则由 Schema 表示。在任何时候，Contract 都只能根据 Schema 和验证脚本（例如在 AluVM 中运行）所允许的内容进行演变。
---
name: پروتکل RGB، از نظریه تا عمل
goal: مهارت‌های لازم برای درک و استفاده از RGB را کسب کنید
objectives: 

  - درک مفاهیم بنیادی پروتکل RGB
  - اصول تعهدات Client-side Validation و Bitcoin را بیاموزید
  - یاد بگیرید چگونه قراردادهای RGB را ایجاد، مدیریت و انتقال دهید.
  - نحوه کار با یک نود لایتنینگ سازگار با RGB


---
# کشف پروتکل RGB


به دنیای RGB وارد شوید، پروتکلی که برای پیاده‌سازی و اجرای حقوق دیجیتال، به شکل قراردادها و دارایی‌ها، بر اساس قوانین و عملیات توافقی Bitcoin Blockchain طراحی شده است. این دوره آموزشی جامع شما را از مفاهیم "Client-side Validation" و "مهرهای یک‌بار مصرف"، تا پیاده‌سازی قراردادهای هوشمند پیشرفته، از طریق مبانی فنی و عملی RGB راهنمایی می‌کند.


از طریق یک برنامه ساختاریافته و گام به گام، مکانیسم‌های Client-side Validation، تعهدات قطعی در Bitcoin و الگوهای تعامل بین کاربران را کشف خواهید کرد. یاد بگیرید چگونه توکن‌های RGB را بر روی Bitcoin یا Lightning Network ایجاد، مدیریت و انتقال دهید.


چه شما یک توسعه‌دهنده، علاقه‌مند به Bitcoin، یا صرفاً کنجکاو برای یادگیری بیشتر درباره این فناوری باشید، این دوره آموزشی ابزارها و دانش مورد نیاز برای تسلط بر RGB و ساخت راه‌حل‌های نوآورانه بر روی Bitcoin را به شما ارائه می‌دهد.


این دوره بر اساس یک سمینار زنده که توسط Fulgur'Ventures سازماندهی شده و توسط سه معلم مشهور و کارشناسان RGB تدریس می‌شود، طراحی شده است.


+++
# معرفی


<partId>c6f7a70f-d894-595f-8c0a-b54759778839</partId>


## ارائه دوره


<chapterId>cf2f087b-6c6b-5037-8f98-94fc9f1d7f46</chapterId>


سلام به همه، و خوش آمدید به این دوره آموزشی که به RGB اختصاص دارد، سیستمی که در سمت مشتری اعتبارسنجی شده و بر روی Bitcoin و Lightning Network اجرا می‌شود. ساختار این دوره به گونه‌ای طراحی شده است که امکان بررسی عمیق این موضوع پیچیده را فراهم کند. در اینجا نحوه سازماندهی دوره آمده است:


**بخش ۱: نظریه**


بخش اول به مفاهیم نظری اختصاص دارد که برای درک اصول Client-side Validation و RGB لازم است. همانطور که در این دوره خواهید دید، RGB مجموعه‌ای از مفاهیم فنی را معرفی می‌کند که معمولاً در Bitcoin دیده نمی‌شوند. در این بخش، همچنین یک واژه‌نامه خواهید یافت که تعاریف تمامی اصطلاحات خاص پروتکل RGB را ارائه می‌دهد.


**بخش ۲: تمرین**


بخش دوم بر کاربرد مفاهیم نظری که در بخش 1 مشاهده شد تمرکز خواهد کرد. ما یاد خواهیم گرفت که چگونه قراردادهای RGB را ایجاد و دستکاری کنیم. همچنین خواهیم دید که چگونه با این ابزارها برنامه‌نویسی کنیم. این دو بخش اول توسط ماکسیم اورلوفسکی ارائه می‌شود.


**بخش ۳: کاربردها**


بخش نهایی توسط سخنرانان دیگر هدایت می‌شود که برنامه‌های کاربردی مبتنی بر RGB را ارائه می‌دهند تا موارد استفاده واقعی را برجسته کنند.


---
این دوره آموزشی در اصل از یک بوت‌کمپ توسعه پیشرفته دو هفته‌ای در ویارجو، توسکانی، که توسط [Fulgur'Ventures](https://fulgur.ventures/) سازماندهی شده بود، رشد کرد. هفته اول، که بر Rust و SDKها متمرکز بود، در این دوره دیگر قابل یافتن است:


https://planb.network/courses/9fbd8b57-f278-4304-8d88-a2d384eaff58

در این دوره، ما بر روی هفته دوم بوت‌کمپ تمرکز می‌کنیم که بر روی RGB تمرکز دارد.


**هفته 1 - LNP402:**


![RGB-Bitcoin](assets/fr/001.webp)


**هفته ۲ - آموزش جاری CSV402:**


![RGB-Bitcoin](assets/fr/002.webp)


با تشکر فراوان از برگزارکنندگان این دوره‌های زنده و ۳ معلمی که شرکت کردند:




- ماکسیم اورلوفسکی: *Ex Tenebrae sententia sapiens dominabitur astris. سایفر، هوش مصنوعی، رباتیک، ترانس‌هیومنیسم. خالق RGB، Prime، Radiant و lnp_bp، mycitadel_io و cyphernet_io*;
- هانتر تروجیلو: *توسعه‌دهنده، Rust، Bitcoin، Lightning، RGB*;
- فدریکو تنگا: *من در حال انجام سهم خود برای تبدیل جهان به یک دیستوپیای Cypherpunk هستم. در حال حاضر در حال کار بر روی RGB در Bitfinex هستم*.


نسخه مکتوب این دوره آموزشی با استفاده از ۲ منبع اصلی تهیه شده است:




- ویدیوهای سمینار ماکسیم اورلوفسکی، هانتر تروجیلو و فردریکو تنگا در بوت‌کمپ لایتنینگ؛
- مستندات RGB که تولید آن توسط [Bitfinex](https://www.bitfinex.com/) حمایت مالی شده است.


آماده‌اید تا به دنیای پیچیده و جذاب RGB شیرجه بزنید؟ بزن بریم!


# RGB در تئوری


<partId>80e797ee-3f33-599f-ab82-e82eeee08219</partId>


## مقدمه‌ای بر مفاهیم محاسبات توزیع‌شده


<chapterId>f52f8af5-5d7c-588b-b56d-99b97176204b</chapterId>


:::video id=f27338bc-4210-4a2e-9b27-30278ed3282c:::


RGB پروتکلی است که برای اعمال و اجرای حقوق دیجیتال (به صورت قراردادها و دارایی‌ها) به روشی مقیاس‌پذیر و محرمانه طراحی شده است، بر اساس قوانین اجماع و عملیات‌های Bitcoin Blockchain. هدف از این فصل اول ارائه مفاهیم و اصطلاحات پایه‌ای پیرامون پروتکل RGB است، به ویژه با تأکید بر ارتباط نزدیک آن با مفاهیم پایه‌ای محاسبات توزیع‌شده مانند Client-side Validation و مهر و موم‌های یک‌بار مصرف.


در این فصل، به بررسی اصول اولیه **سیستم‌های اجماع توزیع‌شده** می‌پردازیم و می‌بینیم که چگونه RGB در این خانواده از فناوری‌ها جای می‌گیرد. همچنین اصول اصلی را معرفی خواهیم کرد که به ما کمک می‌کنند بفهمیم چرا RGB قصد دارد قابل توسعه و مستقل از مکانیزم اجماع Bitcoin باشد، در حالی که در مواقع ضروری به آن متکی است.


### مقدمه


رایانش توزیع‌شده، شاخه‌ای خاص از علوم کامپیوتر است که پروتکل‌های مورد استفاده برای توزیع و پردازش اطلاعات در یک شبکه از گره‌ها را مطالعه می‌کند. این گره‌ها و قوانین پروتکل با هم چیزی را تشکیل می‌دهند که به عنوان یک سیستم توزیع‌شده شناخته می‌شود. از جمله ویژگی‌های اساسی که چنین سیستمی را مشخص می‌کنند، برخی عبارتند از:




- **قابلیت تأیید و اعتبارسنجی مستقل** داده‌های خاص توسط هر گره؛
- امکان ساخت گره‌ها (بسته به پروتکل) یک نمای کامل یا جزئی از اطلاعات. این نماها **حالت‌ها**ی سیستم توزیع‌شده هستند؛
- ترتیب **زمانی** عملیات، به‌طوری که داده‌ها به‌طور قابل‌اعتماد زمان‌سنجی شوند و توافقی در مورد توالی رویدادها (توالی حالت‌ها) وجود داشته باشد.


به طور خاص، مفهوم **اجماع** در یک سیستم توزیع‌شده دو جنبه را پوشش می‌دهد:




- شناسایی اعتبار** تغییرات ایالتی (طبق قوانین پروتکل)؛
- **توافق بر سر ترتیب** این تغییرات حالت، که بازنویسی یا معکوس کردن عملیات تأیید شده را به صورت پسینی غیرممکن می‌سازد (این موضوع در Bitcoin به عنوان "حفاظت در برابر دوبار خرج کردن" نیز شناخته می‌شود).


اولین پیاده‌سازی کاربردی و بدون نیاز به مجوز از یک مکانیزم اجماع توزیع‌شده توسط Satoshi ناکاموتو با Bitcoin معرفی شد، به لطف استفاده ترکیبی از یک ساختار داده Blockchain و یک الگوریتم Proof-of-Work (PoW). در این سیستم، اعتبار تاریخچه بلاک به قدرت محاسباتی اختصاص داده شده به آن توسط نودها (ماینرها) بستگی دارد. بنابراین، Bitcoin یک مثال بزرگ و تاریخی از یک سیستم اجماع توزیع‌شده باز برای همه (*بدون نیاز به مجوز*) است.


در دنیای Blockchain و محاسبات توزیع‌شده، می‌توانیم دو پارادایم اساسی را تشخیص دهیم: ***Blockchain*** به معنای سنتی و ***کانال‌های حالت*** که بهترین مثال آن در تولید، Lightning Network است. Blockchain به عنوان یک ثبت وقایع به ترتیب زمانی تعریف می‌شود که با اجماع در یک شبکه باز و بدون نیاز به مجوز تکرار می‌شود. از سوی دیگر، کانال‌های حالت، کانال‌های همتا به همتا هستند که به دو (یا چند) شرکت‌کننده اجازه می‌دهند تا یک حالت به‌روز شده off-chain را حفظ کنند و از Blockchain فقط هنگام باز کردن و بستن این کانال‌ها استفاده می‌کنند.


در زمینه Bitcoin، بدون شک با اصول Mining، تمرکززدایی و نهایی‌سازی تراکنش‌ها در Blockchain، و همچنین نحوه کار کانال‌های پرداخت آشنا هستید. با RGB، ما یک پارادایم جدید به نام **Client-side Validation** معرفی می‌کنیم که برخلاف Blockchain یا Lightning، شامل ذخیره‌سازی و اعتبارسنجی انتقالات حالت یک Smart contract به صورت محلی (سمت کلاینت) است. این همچنین با سایر تکنیک‌های "DeFi" (مانند _rollups_، _plasma_، _ARK_ و غیره) متفاوت است، جایی که Client-side Validation به Blockchain متکی است تا از Double-spending جلوگیری کند و یک سیستم زمان‌سنجی داشته باشد، در حالی که ثبت حالت‌ها و انتقالات off-chain را فقط با شرکت‌کنندگان مربوطه حفظ می‌کند.


![RGB-Bitcoin](assets/fr/003.webp)


بعداً، ما همچنین یک اصطلاح مهم را معرفی خواهیم کرد: مفهوم "**Stash**"، که به مجموعه‌ای از داده‌های سمت مشتری اشاره دارد که برای حفظ وضعیت یک Contract لازم است، زیرا این داده‌ها به صورت جهانی در سراسر شبکه تکرار نمی‌شوند. در نهایت، به دلایل پشت RGB خواهیم پرداخت، پروتکلی که از Client-side Validation بهره می‌برد و چرا این پروتکل مکمل رویکردهای موجود (Blockchain و کانال‌های حالت) است.


### سه‌گانه‌ها در محاسبات توزیع‌شده


برای درک چگونگی مشکلات Client-side Validation و RGB که توسط Blockchain و Lightning حل نشده‌اند، بیایید ۳ "سه‌گانه" اصلی در محاسبات توزیع‌شده را کشف کنیم:




- مقیاس‌پذیری، غیرمتمرکزسازی، حریم خصوصی**؛
- CAP** قضیه (سازگاری، دسترسی‌پذیری، تحمل پارتیشن)؛
- سه‌گانه‌ی CIA** (محرمانگی، یکپارچگی، دسترسی‌پذیری).


#### ۱. مقیاس‌پذیری، غیرمتمرکز بودن و محرمانگی




- Blockchain (Bitcoin)**


Blockchain بسیار غیرمتمرکز است، اما مقیاس‌پذیری زیادی ندارد. علاوه بر این، از آنجا که همه چیز در یک ثبت عمومی جهانی قرار دارد، محرمانگی محدود است. ما می‌توانیم سعی کنیم با فناوری‌های دانش صفر (Confidential Transactions، طرح‌های mimblewimble و غیره) محرمانگی را بهبود بخشیم، اما زنجیره عمومی نمی‌تواند نمودار تراکنش را پنهان کند.




- کانال‌های لایتنینگ/وضعیت**


کانال‌های دولتی (همانند Lightning Network) مقیاس‌پذیرتر و خصوصی‌تر از Blockchain هستند، زیرا تراکنش‌ها در off-chain انجام می‌شوند. با این حال، الزام به اعلام عمومی برخی Elements (تراکنش‌های تأمین مالی، توپولوژی شبکه) و نظارت بر ترافیک شبکه می‌تواند تا حدی محرمانگی را به خطر بیندازد. تمرکززدایی نیز آسیب می‌بیند: مسیریابی نیازمند نقدینگی بالاست و گره‌های اصلی می‌توانند به نقاط تمرکز تبدیل شوند. این دقیقاً پدیده‌ای است که ما در حال شروع به مشاهده آن در Lightning هستیم.




- Client-side Validation (RGB)**


این پارادایم جدید حتی مقیاس‌پذیرتر و محرمانه‌تر است، زیرا نه تنها می‌توانیم تکنیک‌های اثبات دانش بدون افشا را ادغام کنیم، بلکه هیچ نمودار جهانی از تراکنش‌ها وجود ندارد، زیرا هیچ‌کس کل ثبت را در اختیار ندارد. از سوی دیگر، این موضوع به نوعی مصالحه در تمرکززدایی نیز اشاره دارد: صادرکننده یک Smart contract می‌تواند نقش مرکزی داشته باشد (مانند "توزیع‌کننده Contract" در اتریوم). با این حال، برخلاف Blockchain، با Client-side Validation، شما فقط قراردادهایی را که به آن‌ها علاقه‌مندید ذخیره و اعتبارسنجی می‌کنید، که با اجتناب از نیاز به دانلود و تأیید تمام وضعیت‌های موجود، مقیاس‌پذیری را بهبود می‌بخشد.


![RGB-Bitcoin](assets/fr/004.webp)


#### ۲. قضیه CAP (سازگاری، دسترسی‌پذیری، تحمل پارتیشن)


نظریه CAP تأکید می‌کند که برای یک سیستم توزیع‌شده، برآورده کردن همزمان *یکپارچگی*، *دسترس‌پذیری* و *تحمل پارتیشن* غیرممکن است.




- Blockchain**


Blockchain ثبات و دسترسی را ترجیح می‌دهد، اما در تقسیم‌بندی شبکه عملکرد خوبی ندارد: اگر نمی‌توانید یک بلوک را ببینید، نمی‌توانید عمل کنید و همان دیدگاه کل شبکه را داشته باشید.




- رعد و برق**


سیستمی از کانال‌های دولتی دارای قابلیت دسترسی و تحمل تقسیم‌بندی است (زیرا دو گره می‌توانند حتی اگر شبکه تکه‌تکه شود به یکدیگر متصل بمانند)، اما سازگاری کلی به باز و بسته شدن کانال‌ها بر روی Blockchain بستگی دارد.




- Client-side Validation (RGB)**


سیستمی مانند RGB ثبات (هر شرکت‌کننده داده‌های خود را به صورت محلی و بدون ابهام اعتبارسنجی می‌کند) و تحمل تقسیم‌بندی (شما داده‌های خود را به صورت مستقل نگه می‌دارید) ارائه می‌دهد، اما تضمین نمی‌کند که به صورت جهانی در دسترس باشد (همه باید مطمئن شوند که قطعات مربوط به تاریخچه را دارند و برخی از شرکت‌کنندگان ممکن است هیچ چیزی منتشر نکنند یا از به اشتراک‌گذاری اطلاعات خاصی دست بکشند).


![RGB-Bitcoin](assets/fr/005.webp)


#### ۳. سه‌گانه CIA (محرمانگی، یکپارچگی، دسترس‌پذیری)


این سه‌گانه به ما یادآوری می‌کند که محرمانگی، یکپارچگی و دسترسی‌پذیری نمی‌توانند همزمان بهینه‌سازی شوند. Blockchain، Lightning و Client-side Validation به‌طور متفاوتی در این تعادل قرار می‌گیرند. ایده این است که هیچ سیستم واحدی نمی‌تواند همه چیز را فراهم کند؛ لازم است چندین رویکرد (زمان‌سنجی Blockchain، رویکرد همزمان Lightning و اعتبارسنجی محلی با RGB) را ترکیب کرد تا بسته‌ای منسجم با تضمین‌های خوب در هر بُعد به دست آید.


![RGB-Bitcoin](assets/fr/006.webp)


### نقش Blockchain و مفهوم شاردینگ


Blockchain (در این مورد، Bitcoin) در درجه اول به عنوان یک مکانیزم _زمان‌سنجی_ و محافظت در برابر دوبار خرج کردن عمل می‌کند. به جای وارد کردن داده‌های کامل یک سیستم Smart contract یا غیرمتمرکز، ما به سادگی **تعهدات رمزنگاری** به تراکنش‌ها (به معنای Client-side Validation، که آن را "انتقالات حالت" خواهیم نامید) را شامل می‌شویم. بنابراین:




- ما Blockchain را از مقدار زیادی داده و منطق آزاد می‌کنیم؛
- هر کاربر فقط تاریخچه مورد نیاز برای بخش خود از Contract (یعنی "*Shard*" خود) را ذخیره می‌کند، به جای اینکه Global State را تکرار کند.


شارْدینگ مفهومی است که در پایگاه‌های داده توزیع‌شده (مانند MySQL برای شبکه‌های اجتماعی مانند فیس‌بوک یا توییتر) به وجود آمده است. برای حل مشکل حجم داده و تأخیرهای همگام‌سازی، پایگاه داده به _شارْدها_ (آمریکا، اروپا، آسیا، و غیره) تقسیم می‌شود. هر بخش به صورت محلی سازگار است و تنها به صورت جزئی با دیگر بخش‌ها همگام‌سازی می‌شود.


برای قراردادهای هوشمند نوع RGB، ما Shard را بر اساس خود قراردادها انجام می‌دهیم. هر Contract یک _شارد_ مستقل است. به عنوان مثال، اگر شما فقط توکن‌های USDT را نگه دارید، نیازی به ذخیره یا اعتبارسنجی کل تاریخچه یک token دیگر مانند USDC ندارید. در Bitcoin، Blockchain _شاردینگ_ انجام نمی‌دهد: شما یک مجموعه جهانی از UTXOها دارید. با Client-side Validation، هر شرکت‌کننده فقط داده‌های Contract را که نگه می‌دارد یا استفاده می‌کند، حفظ می‌کند.


بنابراین می‌توانیم اکوسیستم را به صورت زیر تصور کنیم:




- Blockchain (Bitcoin)** به عنوان بنیادی که تکرار کامل یک رجیستر حداقلی را تضمین می‌کند و به عنوان زمان‌سنج Layer عمل می‌کند؛
- Lightning Network** برای سریع، Confidential Transactions، هنوز بر اساس امنیت و تسویه نهایی Bitcoin Blockchain؛
- RGB و Client-side Validation** برای افزودن منطق پیچیده‌تر Smart contract، بدون ایجاد شلوغی در Blockchain یا از دست دادن محرمانگی.


![RGB-Bitcoin](assets/fr/007.webp)


این سه Elements یک کل مثلثی را تشکیل می‌دهند، نه یک پشته خطی از "Layer 2"، "Layer 3" و غیره. رعد و برق می‌تواند مستقیماً به Bitcoin متصل شود، یا با تراکنش‌های Bitcoin که داده‌های RGB را در بر می‌گیرند، مرتبط باشد. به همین ترتیب، یک "BiFi" (مالی بر روی Bitcoin) می‌تواند با Blockchain، با رعد و برق و با RGB بر اساس نیازهای محرمانگی، مقیاس‌پذیری یا منطق Contract ترکیب شود.


![RGB-Bitcoin](assets/fr/008.webp)


### مفهوم انتقالات حالت


در هر سیستم توزیع‌شده، هدف مکانیزم اعتبارسنجی این است که بتواند **اعتبار و ترتیب زمانی تغییرات وضعیت را تعیین کند**. هدف این است که تأیید شود قوانین پروتکل رعایت شده‌اند و ثابت شود که این تغییرات وضعیت به ترتیب قطعی و غیرقابل انکار دنبال می‌شوند.


برای درک چگونگی عملکرد این اعتبارسنجی در زمینه **Bitcoin** و به طور کلی، برای فهم فلسفه پشت Client-side Validation، بیایید ابتدا نگاهی به مکانیزم‌های Bitcoin و Blockchain بیندازیم، قبل از اینکه ببینیم Client-side Validation چگونه با آنها تفاوت دارد و چه بهینه‌سازی‌هایی را ممکن می‌سازد.


![RGB-Bitcoin](assets/fr/009.webp)


در مورد Bitcoin Blockchain، اعتبارسنجی تراکنش بر اساس یک قانون ساده است:




- همه گره‌های شبکه هر بلوک و تراکنش را دانلود می‌کنند؛
- آن‌ها این تراکنش‌ها را اعتبارسنجی می‌کنند تا تکامل صحیح مجموعه UTXO (همه خروجی‌های خرج‌نشده) را تأیید کنند؛
- آن‌ها این داده‌ها را (به صورت بلوک‌ها) ذخیره می‌کنند تا در صورت لزوم تاریخچه بتواند بازپخش شود.


![RGB-Bitcoin](assets/fr/010.webp)


با این حال، این مدل دو اشکال عمده دارد:




- مقیاس‌پذیری**: از آنجا که هر گره باید تراکنش‌های همه را پردازش، تأیید و بایگانی کند، محدودیت آشکاری برای ظرفیت تراکنش وجود دارد که به‌ویژه به حداکثر اندازه بلوک (به‌طور متوسط 1 مگابایت در هر 10 دقیقه برای Bitcoin، بدون احتساب کوکی‌ها) مرتبط است؛
- حریم خصوصی**: همه چیز به صورت عمومی پخش و ذخیره می‌شود (مبالغ، آدرس‌های مقصد، و غیره)، که محرمانگی تبادلات را محدود می‌کند.


![RGB-Bitcoin](assets/fr/012.webp)


در عمل، این مدل برای Bitcoin به عنوان پایه Layer (Layer 1) کار می‌کند، اما ممکن است برای استفاده‌های پیچیده‌تر که به طور همزمان به توان عملیاتی بالای تراکنش و درجه‌ای از محرمانگی نیاز دارند، ناکافی شود.


Client-side Validation بر اساس ایده‌ی مخالف بنا شده است: به جای اینکه کل شبکه نیاز به تأیید و ذخیره‌سازی تمام تراکنش‌ها داشته باشد، هر شرکت‌کننده (کلاینت) فقط بخشی از تاریخچه که به او مربوط می‌شود را تأیید خواهد کرد:




- وقتی یک فرد دارایی (یا هر نوع دارایی دیجیتال دیگر) دریافت می‌کند، تنها نیاز دارد که زنجیره عملیات (انتقالات وضعیت) که به آن دارایی منتهی می‌شود را بشناسد و تأیید کند و مشروعیت آن را اثبات کند؛
- این توالی عملیات، از ***Genesis*** (انتشار اولیه) تا آخرین تراکنش، یک گراف جهت‌دار غیرمدور (DAG) یا Shard را تشکیل می‌دهد، یعنی بخشی از تاریخچه کلی.


![RGB-Bitcoin](assets/fr/013.webp)


به طور همزمان، به طوری که بقیه شبکه (یا به طور دقیق‌تر، Layer زیرین، مانند Bitcoin) بتوانند بدون دیدن جزئیات این داده‌ها، وضعیت نهایی را قفل کنند، Client-side Validation به مفهوم ***Commitment*** متکی است.


یک *Commitment* یک Commitment رمزنگاری‌شده است، که معمولاً یک _هش_ (برای مثال SHA-256) است که در یک تراکنش Bitcoin وارد شده و ثابت می‌کند که داده‌های خصوصی گنجانده شده‌اند، بدون اینکه این داده‌ها را فاش کند.


با تشکر از این _تعهدات_، می‌توانیم ثابت کنیم:




- وجود اطلاعات (از آنجا که به Hash متعهد است)؛
- قدمت بودن این اطلاعات (زیرا در Blockchain با تاریخ و ترتیب بلوک زمان‌مهر شده و لنگر انداخته است).


با این حال، محتوای دقیق فاش نمی‌شود و بدین ترتیب محرمانه بودن آن حفظ می‌شود.


به طور مشخص، در اینجا نحوه عملکرد یک RGB State Transition آمده است:




- شما یک State Transition جدید آماده می‌کنید (مثلاً انتقال یک RGB token)؛
- شما generate یک Commitment رمزنگاری به این انتقال و آن را به یک تراکنش Bitcoin وارد کنید (این تعهدات در پروتکل RGB به عنوان "*لنگر*" نامیده می‌شوند)؛
- طرف مقابل (گیرنده) تاریخچه سمت مشتری مرتبط با این دارایی را بازیابی کرده و یکپارچگی انتها به انتها را از Genesis تا Smart contract تا انتقالی که به آن ارسال می‌کنید، اعتبارسنجی می‌کند.


![RGB-Bitcoin](assets/fr/014.webp)


Client-side Validation دو مزیت عمده ارائه می‌دهد:




- مقیاس‌پذیری:**


تعهدات موجود در Blockchain کوچک هستند (در حد چند ده بایت). این امر تضمین می‌کند که فضای بلاک اشباع نشود، زیرا تنها نیاز است که Hash شامل شود. همچنین این امکان را فراهم می‌کند که پروتکل off-chain تکامل یابد، زیرا هر کاربر تنها باید قطعه تاریخچه خود را ذخیره کند (ذخیره‌گاه خود).




- حریم خصوصی:**


خود تراکنش‌ها (یعنی محتوای دقیق آن‌ها) منتشر نمی‌شوند On-Chain. تنها اثر انگشت آن‌ها (*Hash*) منتشر می‌شود. بنابراین، مقادیر، آدرس‌ها و منطق Contract خصوصی باقی می‌مانند و گیرنده می‌تواند با بررسی تمام انتقالات قبلی، به صورت محلی، اعتبار Shard خود را تأیید کند. هیچ دلیلی برای گیرنده وجود ندارد که این داده‌ها را عمومی کند، مگر در صورت بروز اختلاف یا زمانی که اثبات لازم باشد.


در سیستمی مانند RGB، انتقالات حالت متعدد از قراردادهای مختلف (یا دارایی‌های مختلف) می‌توانند از طریق یک _تعهد_ واحد به یک تراکنش Bitcoin واحد تجمیع شوند. این مکانیزم یک پیوند قطعی و دارای برچسب زمانی بین تراکنش On-Chain و داده‌های off-chain (انتقالات اعتبارسنجی شده سمت مشتری) برقرار می‌کند و امکان ثبت همزمان چندین شارد را در یک نقطه Anchor واحد فراهم می‌کند، که به نوبه خود هزینه و ردپای On-Chain را کاهش می‌دهد.


در عمل، هنگامی که این تراکنش Bitcoin تأیید می‌شود، به طور دائمی وضعیت قراردادهای زیربنایی را "قفل" می‌کند، زیرا تغییر Hash که قبلاً در Blockchain ثبت شده است، غیرممکن می‌شود.


![RGB-Bitcoin](assets/fr/015.webp)


### مفهوم Stash


**Stash** مجموعه‌ای از داده‌های سمت مشتری است که یک شرکت‌کننده باید حتماً آن را حفظ کند تا یکپارچگی و تاریخچه یک RGB Smart contract را حفظ کند. برخلاف یک کانال Lightning، که در آن برخی از حالت‌ها می‌توانند به صورت محلی از اطلاعات مشترک بازسازی شوند، Stash یک RGB Contract در جای دیگری تکرار نمی‌شود: اگر آن را از دست بدهید، هیچ‌کس قادر نخواهد بود آن را برای شما بازیابی کند، زیرا شما مسئول سهم خود از تاریخچه هستید. به همین دلیل است که باید سیستمی با رویه‌های پشتیبان‌گیری قابل اعتماد در RGB اتخاذ کنید.


![RGB-Bitcoin](assets/fr/016.webp)


### Single-Use Seal: منشأ و عملکرد


هنگام پذیرش یک دارایی مانند ارز، دو تضمین ضروری است:




- اصالت کالای دریافت شده؛
- منحصر به فرد بودن کالای دریافت شده، برای جلوگیری از هزینه‌های مضاعف.


برای دارایی‌های فیزیکی، مانند یک اسکناس، حضور فیزیکی کافی است تا ثابت کند که تکرار نشده است. با این حال، در دنیای دیجیتال، جایی که دارایی‌ها صرفاً اطلاعاتی هستند، این تأیید پیچیده‌تر است، زیرا اطلاعات به راحتی می‌توانند تکثیر و تکرار شوند.


همان‌طور که قبلاً دیدیم، افشای تاریخچه انتقالات حالت توسط فرستنده به ما امکان می‌دهد تا اصالت یک RGB token را تضمین کنیم. با دسترسی به تمام تراکنش‌ها از زمان تراکنش Genesis، می‌توانیم اصالت token را تأیید کنیم. این اصل مشابه با Bitcoin است، جایی که تاریخچه سکه‌ها می‌تواند به Coinbase Transaction اصلی برگردد تا اعتبار آن‌ها تأیید شود. با این حال، برخلاف Bitcoin، این تاریخچه انتقالات حالت در RGB خصوصی است و در سمت مشتری نگهداری می‌شود.


برای جلوگیری از توکن‌های Double-spending از RGB، ما از مکانیزمی به نام "**Single-Use Seal**" استفاده می‌کنیم. این سیستم تضمین می‌کند که هر token، پس از استفاده، نمی‌تواند به‌طور تقلبی برای بار دوم استفاده شود.


مُهرهای یک‌بار مصرف اصول رمزنگاری هستند که در سال ۲۰۱۶ توسط پیتر تاد پیشنهاد شدند و شبیه به مفهوم مُهرهای فیزیکی هستند: زمانی که یک Seal بر روی یک کانتینر قرار می‌گیرد، باز کردن یا تغییر آن بدون شکستن غیرقابل برگشت Seal غیرممکن می‌شود.


![RGB-Bitcoin](assets/fr/018.webp)


این رویکرد، که به دنیای دیجیتال منتقل شده است، امکان اثبات این را فراهم می‌کند که یک توالی از رویدادها واقعاً رخ داده است و دیگر نمی‌توان آن را پس از وقوع تغییر داد. مهرهای یک‌بار مصرف به این ترتیب فراتر از منطق ساده `Hash + Timestamp` می‌روند و مفهوم Seal را اضافه می‌کنند که **فقط یک بار** می‌تواند بسته شود.


![RGB-Bitcoin](assets/fr/017.webp)


برای کارکرد مهرهای یک‌بار مصرف، شما به یک رسانه اثبات انتشار نیاز دارید که قادر به اثبات وجود یا عدم وجود یک انتشار باشد و پس از انتشار اطلاعات، جعل آن دشوار (اگر نگوییم غیرممکن) باشد. یک **Blockchain** (مانند Bitcoin) می‌تواند این نقش را ایفا کند، همان‌طور که یک روزنامه کاغذی با گردش عمومی می‌تواند این کار را انجام دهد، به عنوان مثال. ایده به شرح زیر است:




- ما می‌خواهیم ثابت کنیم که یک Commitment خاص بر روی یک پیام `h(m)` به مخاطبی منتشر شده است بدون اینکه محتوای پیام `m` را فاش کنیم؛
- می‌خواهیم ثابت کنیم که هیچ پیام رقابتی `h(m')` به جای `h(m)` به نام Commitment منتشر نشده است؛
- ما همچنین می‌خواهیم بتوانیم بررسی کنیم که پیام `m` قبل از تاریخ معینی وجود دارد.


یک Blockchain به‌طور ایده‌آل برای این نقش مناسب است: به محض اینکه یک تراکنش در یک بلوک گنجانده شود، کل شبکه دارای همان اثبات غیرقابل جعل از وجود و محتوای آن است (حداقل تا حدی، زیرا _تعهد_ می‌تواند جزئیات را پنهان کند در حالی که اصالت پیام را اثبات می‌کند).


بنابراین، Single-Use Seal می‌تواند به‌عنوان یک وعده رسمی برای انتشار یک پیام (که در این مرحله هنوز ناشناخته است) تنها یک‌بار و به‌صورتی که توسط تمامی طرف‌های علاقه‌مند قابل تأیید باشد، دیده شود.


برخلاف _تعهدات_ ساده (Hash) یا برچسب‌های زمانی که وجود یک تاریخ را تأیید می‌کنند، یک Single-Use Seal تضمین اضافی ارائه می‌دهد که **هیچ Commitment جایگزینی** نمی‌تواند همزمان وجود داشته باشد: شما نمی‌توانید یک Seal را دو بار ببندید یا سعی کنید پیام مهر و موم شده را جایگزین کنید.


مقایسه زیر به درک این اصل کمک می‌کند:




- رمزنگاری Commitment (Hash)**: با استفاده از تابع Hash، می‌توانید با انتشار Hash یک داده (یک عدد) به آن متعهد شوید. داده تا زمانی که پیش‌تصویر را فاش نکنید مخفی می‌ماند، اما می‌توانید ثابت کنید که از قبل آن را می‌دانستید؛
- Timestamp (Blockchain)**: با وارد کردن این Hash در Blockchain، ما همچنین اثبات می‌کنیم که آن را در یک لحظه دقیق (لحظه گنجاندن در یک بلوک) می‌دانستیم؛
- Single-Use Seal**: با استفاده از مهرهای یک‌بار مصرف، ما یک قدم جلوتر می‌رویم و Commitment را منحصر به فرد می‌سازیم. با یک Hash، می‌توانید چندین تعهد متناقض را به صورت موازی ایجاد کنید (مشکل دکتری که به خانواده اعلام می‌کند "*پسر است*" و در دفترچه شخصی‌اش می‌نویسد "*دختر است*"). Single-Use Seal این امکان را با اتصال Commitment به یک رسانه اثبات انتشار، مانند Bitcoin Blockchain، از بین می‌برد، به طوری که با صرف UTXO، Commitment به طور قطعی مهر و موم می‌شود. پس از صرف، همان UTXO نمی‌تواند دوباره صرف شود تا Commitment را جایگزین کند.
- رمزنگاری Commitment (Hash)**: با استفاده از تابع Hash، می‌توانید با انتشار Hash یک داده (یک عدد) به آن متعهد شوید. داده تا زمانی که پیش‌تصویر را فاش نکنید مخفی می‌ماند، اما می‌توانید ثابت کنید که از قبل آن را می‌دانستید؛
- Timestamp (Blockchain)**: با وارد کردن این Hash در Blockchain، ما همچنین اثبات می‌کنیم که آن را در یک لحظه دقیق (لحظه گنجاندن در یک بلوک) می‌دانستیم؛
- Single-Use Seal**: با استفاده از مهر و موم‌های یک‌بار مصرف، ما یک قدم جلوتر می‌رویم و Commitment را منحصر به فرد می‌سازیم. با یک Hash، می‌توانید چندین تعهد متناقض را به‌صورت موازی ایجاد کنید (مشکل دکتری که به خانواده اعلام می‌کند "*پسر است*" و در دفترچه شخصی‌اش می‌نویسد "*دختر است*"). Single-Use Seal این امکان را با اتصال Commitment به یک رسانه اثبات انتشار، مانند Bitcoin Blockchain، از بین می‌برد، به‌طوری‌که با صرف UTXO، Commitment به‌طور قطعی مهر و موم می‌شود. پس از صرف، همان UTXO نمی‌تواند دوباره صرف شود تا Commitment را جایگزین کند.


|                                                                                  | Simple commitment (digest/hash) | Timestamps | Single-use seals |
| -------------------------------------------------------------------------------- | ------------------------------- | ---------- | ---------------- |
| Publishing the commitment does not reveal the message                           | Yes                             | Yes        | Yes              |
| Proof of the commitment date / existence of the message before a certain date  | Impossible                      | Possible   | Possible         |
| Proof that no alternative commitment can exist                                 | Impossible                      | Impossible | Possible         |

پلمپ‌های یک‌بار مصرف در سه مرحله اصلی کار می‌کنند:


**Seal Definition:**




- Alice قوانین مربوط به انتشار Seal را از پیش تعریف می‌کند (چه زمانی، کجا و چگونه پیام منتشر خواهد شد)؛
- Bob این شرایط را می‌پذیرد یا تأیید می‌کند.


![RGB-Bitcoin](assets/fr/021.webp)


**بستن Seal:**




- در زمان اجرا، Alice با انتشار پیام واقعی (معمولاً به صورت یک _تعهد_، مثلاً یک Hash) Seal را می‌بندد؛
- همچنین یک **شاهد** (اثبات رمزنگاری) ارائه می‌دهد که ثابت می‌کند Seal بسته و غیرقابل برگشت است.


![RGB-Bitcoin](assets/fr/019.webp)


**Seal تأیید:**




- هنگامی که Seal بسته شود، Bob دیگر نمی‌تواند آن را باز کند: او فقط می‌تواند بررسی کند که بسته شده است؛
- Bob، Seal، **شاهد** و پیام (یا Commitment او) را جمع‌آوری می‌کند تا مطمئن شود که همه چیز مطابقت دارد و هیچ مهر رقابتی یا نسخه‌های متفاوتی وجود ندارد.


فرآیند را می‌توان به صورت زیر خلاصه کرد:


```txt
# Defined by Alice, validated or accepted by Bob
seal <- Define()
# Seal is closed by Alice with the message
witness <- Close(seal, message)
# Verification by Bob
bool <- Verify(seal, witness, message)
```


با این حال، Client-side Validation یک قدم فراتر می‌رود: اگر تعریف یک Seal خود خارج از Blockchain باقی بماند، این امکان (به‌طور نظری) وجود دارد که کسی وجود یا مشروعیت Seal مورد نظر را به چالش بکشد. برای غلبه بر این مشکل، از یک زنجیره مهرهای یک‌بار مصرف به هم پیوسته استفاده می‌شود:




- هر Seal بسته شامل تعریف Seal بعدی است؛
- ما این بسته شدن‌ها (با _تعهدات_ آن‌ها) را در Blockchain (در یک تراکنش Bitcoin) ثبت می‌کنیم؛
- بنابراین، هر تلاشی برای تغییر Seal قبلی با تاریخچه‌ای که در Bitcoin نهفته است، در تضاد خواهد بود.


این دقیقاً همان کاری است که سیستم RGB انجام می‌دهد:




- پیام‌های منتشر شده _تعهداتی_ به داده‌های اعتبارسنجی شده سمت مشتری هستند؛
- Seal Definition با Bitcoin و UTXO مرتبط است؛
- Seal زمانی بسته می‌شود که این UTXO خرج شود یا زمانی که یک خروجی جدید به همان Commitment اعتبار داده شود؛
- زنجیره تراکنش که این UTXOها را خرج می‌کند، با اثبات انتشار مطابقت دارد: هر انتقال یا تغییر حالت در RGB به این ترتیب در Bitcoin لنگر انداخته است.


خلاصه:




- تعریف _مهر_ همان UTXO است که قصد دارید در آینده Seal را به Commitment تبدیل کنید؛
- _مهر بستن_ زمانی اتفاق می‌افتد که شما این UTXO را خرج می‌کنید و تراکنشی ایجاد می‌کنید که حاوی Commitment است؛
- _شاهد_ خود معامله است که ثابت می‌کند شما Seal را با این محتوا بسته‌اید؛
- شما نمی‌توانید ثابت کنید که یک Seal بسته نشده است (نمی‌توانید به‌طور قطعی مطمئن باشید که یک UTXO قبلاً خرج نشده یا در بلوکی که هنوز ندیده‌اید خرج نخواهد شد)، اما می‌توانید ثابت کنید که واقعاً بسته شده است.


این یکتایی برای Client-side Validation مهم است: زمانی که یک State Transition را اعتبارسنجی می‌کنید، بررسی می‌کنید که با یک UTXO یکتا مطابقت دارد که قبلاً در یک Commitment رقیب خرج نشده باشد. این همان چیزی است که عدم وجود دوبار خرج کردن در قراردادهای هوشمند off-chain را تضمین می‌کند.


### تعهدات متعدد و ریشه‌ها


یک RGB Smart contract ممکن است نیاز داشته باشد که به طور همزمان چندین مهر و موم یکبار مصرف (چندین UTXO) را خرج کند. علاوه بر این، یک تراکنش Bitcoin ممکن است به چندین قرارداد متمایز اشاره کند که هر کدام State Transition خود را مهر و موم می‌کند. این نیازمند مکانیزم **چند-Commitment** است تا به صورت قطعی و منحصر به فرد ثابت کند که هیچ‌یک از تعهدات به صورت تکراری وجود ندارد. اینجاست که مفهوم **Anchor** در RGB مطرح می‌شود: یک ساختار ویژه که یک تراکنش Bitcoin را به یک یا چند تعهد سمت مشتری (انتقال حالت) متصل می‌کند که هر کدام ممکن است به یک Contract متفاوت تعلق داشته باشد. در فصل بعدی به این مفهوم نگاهی دقیق‌تر خواهیم داشت.


![RGB-Bitcoin](assets/fr/023.webp)


دو تا از مخازن اصلی پروژه در GitHub (تحت سازمان LNPBP) پیاده‌سازی‌های پایه‌ای این مفاهیم که در فصل اول مطالعه شده‌اند را گرد هم می‌آورند:




- اعتبارسنجی_سمت_کاربر**: شامل ابتداییات Rust برای اعتبارسنجی محلی؛
- single_use_seals**: منطق تعریف و بستن ایمن این مهر و موم‌ها را پیاده‌سازی می‌کند.


![RGB-Bitcoin](assets/fr/020.webp)


توجه داشته باشید که این بلوک‌های نرم‌افزاری نسبت به Bitcoin بی‌تفاوت هستند؛ از نظر تئوری، می‌توانند به هر رسانه اثبات انتشار دیگری (یک رجیستری دیگر، یک ژورنال، و غیره) اعمال شوند. در عمل، RGB برای استحکام و اجماع گسترده خود به Bitcoin متکی است.


![RGB-Bitcoin](assets/fr/021.webp)


### سوالات از عموم مردم


#### به سوی استفاده گسترده‌تر از مهر و موم‌های یکبار مصرف


پیتر تاد همچنین پروتکل _Open Timestamps_ را ایجاد کرد و مفهوم Single-Use Seal یک گسترش طبیعی از این ایده‌ها است. فراتر از RGB، موارد استفاده دیگری نیز می‌توان تصور کرد، مانند ساخت _sidechains_ بدون توسل به _merge mining_ یا پیشنهادات مرتبط با drivechain مانند BIP300. هر سیستمی که به یک Commitment نیاز دارد، می‌تواند اصولاً از این ابتدایی رمزنگاری بهره‌برداری کند. امروز، RGB اولین پیاده‌سازی بزرگ در مقیاس کامل است.


#### مشکلات دسترسی به داده‌ها


از آنجا که در Client-side Validation، هر کاربر بخش خود از تاریخچه را ذخیره می‌کند، دسترسی به داده‌ها به صورت جهانی تضمین نمی‌شود. اگر یک صادرکننده Contract اطلاعات خاصی را نگه دارد یا لغو کند، ممکن است از تحول واقعی پیشنهاد بی‌خبر باشید. در برخی موارد (مانند استیبل‌کوین‌ها)، انتظار می‌رود که صادرکننده داده‌های عمومی را برای اثبات حجم در گردش حفظ کند، اما هیچ الزام فنی برای انجام این کار وجود ندارد. بنابراین، امکان طراحی قراردادهای عمداً مبهم با Supply نامحدود وجود دارد که سوالاتی در مورد اعتماد را مطرح می‌کند.


#### شاردینگ و جداسازی Contract


هر Contract نمایانگر یک _شارد_ مجزا است: به عنوان مثال، USDT و USDC نیازی به اشتراک‌گذاری تاریخچه‌های خود ندارند. مبادلات اتمی همچنان ممکن است، اما این شامل ادغام دفاتر آن‌ها نمی‌شود. همه چیز توسط Commitment رمزنگاری شده انجام می‌شود، بدون اینکه نمودار تاریخچه کامل به هر شرکت‌کننده افشا شود.


### نتیجه‌گیری


ما دیده‌ایم که مفهوم Client-side Validation چگونه با Blockchain و _کانال‌های حالت_ سازگار است، چگونه به سه‌گانه‌های محاسبات توزیع‌شده پاسخ می‌دهد، و چگونه به‌طور منحصربه‌فرد از Bitcoin Blockchain برای اجتناب از Double-spending و برای *زمان‌سنجی* استفاده می‌کند. این ایده بر اساس مفهوم **Single-Use Seal** است که امکان ایجاد تعهدات منحصربه‌فردی را فراهم می‌کند که نمی‌توانید به دلخواه دوباره خرج کنید. به این ترتیب، هر شرکت‌کننده تنها تاریخی را که به‌طور ضروری لازم است بارگذاری می‌کند، مقیاس‌پذیری و محرمانگی قراردادهای هوشمند را افزایش می‌دهد در حالی که امنیت Bitcoin را به‌عنوان پس‌زمینه حفظ می‌کند.


گام بعدی توضیح با جزئیات بیشتر در مورد نحوه اعمال این مکانیزم Single-Use Seal در Bitcoin (از طریق UTXOs)، نحوه ایجاد و اعتبارسنجی انکرها، و سپس نحوه ساخت قراردادهای هوشمند کامل در RGB خواهد بود. به‌ویژه، ما به مسئله تعهدات متعدد، چالش فنی اثبات اینکه یک تراکنش Bitcoin به‌طور همزمان چندین انتقال حالت را در قراردادهای مختلف مهر و موم می‌کند، بدون ایجاد آسیب‌پذیری یا تعهدات دوگانه، خواهیم پرداخت.


قبل از پرداختن به جزئیات فنی‌تر فصل دوم، می‌توانید تعاریف کلیدی (Client-side Validation، Single-Use Seal، انکرها، و غیره) را دوباره بخوانید و به منطق کلی توجه داشته باشید: ما به دنبال آشتی دادن نقاط قوت Bitcoin Blockchain (امنیت، تمرکززدایی، زمان‌سنجی) با راه‌حل‌های off-chain (سرعت، محرمانگی، مقیاس‌پذیری) هستیم و این دقیقاً همان چیزی است که RGB و Client-side Validation سعی در دستیابی به آن دارند.


## Commitment Layer


<chapterId>cc2fe85a-9cc7-5b8c-a00a-c0a867241061</chapterId>


:::video id=73ddea2d-c243-479d-a3dc-12d7db8eef70:::


در این فصل، به بررسی پیاده‌سازی Client-side Validation و مهر و موم‌های یکبار مصرف درون Bitcoin Blockchain خواهیم پرداخت. اصول اصلی **Commitment Layer** (Layer 1) در RGB را ارائه خواهیم کرد، با تمرکز ویژه بر روی طرح **TxO2**، که RGB برای تعریف و بستن یک Seal در یک تراکنش Bitcoin استفاده می‌کند. سپس، به دو نکته مهم که هنوز به‌طور کامل پوشش داده نشده‌اند، خواهیم پرداخت:




- _تعهدات قطعی Bitcoin_;
- تعهدات چند پروتکلی.


ترکیب این مفاهیم است که به ما امکان می‌دهد چندین سیستم یا قرارداد را بر روی یک UTXO و در نتیجه یک Blockchain قرار دهیم.


باید به خاطر داشت که عملیات رمزنگاری توصیف‌شده می‌تواند به‌طور مطلق به بلاک‌چین‌ها یا رسانه‌های انتشاری دیگر اعمال شود، اما ویژگی‌های Bitcoin (از نظر تمرکززدایی، مقاومت در برابر سانسور و باز بودن برای همه) آن را به پایه‌ای ایده‌آل برای توسعه برنامه‌پذیری پیشرفته مانند آنچه توسط **RGB** نیاز است، تبدیل می‌کند.


### طرح‌های Commitment در Bitcoin و استفاده آن‌ها توسط RGB


همان‌طور که در فصل اول دوره دیدیم، مهرهای یک‌بار مصرف یک مفهوم کلی هستند: ما قول می‌دهیم که یک Commitment (_تعهد_) را در یک مکان خاص از یک تراکنش قرار دهیم و این مکان مانند یک Seal عمل می‌کند که ما روی یک پیام می‌بندیم. با این حال، در Bitcoin Blockchain، چندین گزینه برای انتخاب محل قرار دادن این _تعهد_ وجود دارد.


برای درک منطق، بیایید اصل اساسی را به خاطر بیاوریم: برای بستن یک _مهر یک‌بار مصرف_، ما ناحیه مهر و موم شده را با وارد کردن _تعهد_ بر روی یک پیام خاص مصرف می‌کنیم. در Bitcoin، این کار می‌تواند به چندین روش انجام شود:




- از یک کلید عمومی یا Address** استفاده کنید


ما می‌توانیم تصمیم بگیریم که یک کلید عمومی خاص یا Address به عنوان _مهر یک‌بار مصرف_ باشد. به محض اینکه این کلید یا Address در یک تراکنش On-Chain ظاهر شود، به این معنی است که Seal با یک پیام خاص بسته شده است.




- از خروجی تراکنش **Bitcoin** استفاده کنید


این بدان معناست که یک _مهر یک‌بار مصرف_ به عنوان یک _نقطه خروجی_ دقیق (یک جفت txid + شماره خروجی) تعریف می‌شود. به محض اینکه این _نقطه خروجی_ خرج شود، Seal بسته می‌شود.


در حین کار بر روی RGB، حداقل ۴ روش مختلف برای پیاده‌سازی این مهر و موم‌ها بر روی Bitcoin شناسایی کردیم:




- Seal را از طریق یک کلید عمومی تعریف کنید و آن را در یک _خروجی_ ببندید؛
- Seal را با یک _outpoint_ تعریف کنید و آن را با یک _output_ ببندید؛
- Seal را از طریق مقدار یک کلید عمومی تعریف کنید و آن را در یک _input_ ببندید؛
- Seal را از طریق یک _outpoint_ تعریف کنید و آن را در یک _input_ ببندید.


| Schema Name | Seal Definition           | Seal Closure              | Additional Requirements                                        | Main Application            | Possible Commitment Schemes     |
| ----------- | ------------------------- | ------------------------- | -------------------------------------------------------------- | --------------------------- | -------------------------------- |
| PkO         | Public Key Value          | Transaction Output        | P2(W)PKH                                                       | None at the moment          | Keytweak, taptweak, opret       |
| TxO2        | Transaction Output        | Transaction Output        | Requires deterministic commitments on Bitcoin                  | RGBv1 (universal)           | Keytweak, tapret, opret         |
| PkI         | Public Key Value          | Transaction Input         | Taproot only & not compatible with legacy wallets              | Bitcoin-based identities    | Sigtweak, witweak               |
| TxO1        | Transaction Output        | Transaction Input         | Taproot only & not compatible with legacy wallets              | None at the moment          | Sigtweak, witweak               |


ما به جزئیات هر یک از این پیکربندی‌ها نمی‌پردازیم، زیرا در RGB تصمیم گرفته‌ایم از **یک _outpoint_ به عنوان تعریف Seal** استفاده کنیم و _commitment_ را در خروجی تراکنش مصرف‌کننده این _outpoint_ قرار دهیم. بنابراین می‌توانیم مفاهیم زیر را برای ادامه معرفی کنیم:




- "Seal Definition "**: یک _outpoint_ داده شده (شناسایی شده توسط txid + شماره خروجی)؛
- "Seal closing "**: تراکنشی که این _outpoint_ را خرج می‌کند، که در آن یک _commitment_ به یک پیام اضافه می‌شود.


این طرح به دلیل سازگاری با معماری RGB انتخاب شده است، اما پیکربندی‌های دیگر می‌توانند برای استفاده‌های مختلف مفید باشند.


"O2" در "TxO2" به ما یادآوری می‌کند که هم تعریف و هم بسته شدن بر اساس هزینه (یا ایجاد) یک خروجی تراکنش است.


### مثال نمودار TxO2


به عنوان یک یادآوری، تعریف یک _مهر یک‌بار مصرف_ لزوماً نیاز به انتشار یک تراکنش On-Chain ندارد. به عنوان مثال، کافی است که Alice از قبل یک UTXO خرج نشده داشته باشد. او می‌تواند تصمیم بگیرد: "این _نقطه خروج_ (که از قبل وجود دارد) اکنون Seal من است". او این را به صورت محلی (_سمت کلاینت_) یادداشت می‌کند و تا زمانی که این UTXO خرج نشود، Seal باز در نظر گرفته می‌شود.


![RGB-Bitcoin](assets/fr/024.webp)


در روزی که می‌خواهد Seal را ببندد (برای اعلام یک رویداد، یا برای Anchor یک پیام خاص)، این UTXO را در یک تراکنش جدید خرج می‌کند (این تراکنش اغلب به عنوان "_تراکنش شاهد_" نامیده می‌شود (نامرتبط با _segwit_، این فقط اصطلاحی است که به آن می‌دهیم). این تراکنش جدید شامل _تعهد_ به پیام خواهد بود.


![RGB-Bitcoin](assets/fr/025.webp)


توجه داشته باشید که در این مثال:




- هیچ‌کس جز Bob (یا افرادی که Alice تصمیم می‌گیرد اثبات کامل را به آن‌ها نشان دهد) نخواهد دانست که یک پیام خاص در این تراکنش پنهان شده است؛
- همه می‌توانند ببینند که _outpoint_ خرج شده است، اما تنها Bob مدرک این را دارد که پیام واقعاً در تراکنش لنگر انداخته است.


برای توضیح این طرح TxO2، می‌توانیم از یک _مهر یک‌بار مصرف_ به عنوان مکانیزمی برای لغو یک کلید PGP استفاده کنیم. به جای انتشار یک گواهی لغو روی سرورها، Alice می‌تواند بگوید: "این خروجی Bitcoin، اگر خرج شود، به این معنی است که کلید PGP من لغو شده است".


بنابراین Alice دارای UTXO خاصی است که به یک حالت یا داده خاص (که فقط برای او شناخته شده است) به صورت محلی (در سمت مشتری) مرتبط است.


Alice به Bob اطلاع می‌دهد که اگر این UTXO خرج شود، یک رویداد خاص تلقی می‌شود که رخ داده است. از بیرون، تنها چیزی که می‌بینیم یک تراکنش Bitcoin است؛ اما Bob می‌داند که این هزینه معنای پنهانی دارد.


![RGB-Bitcoin](assets/fr/026.webp)


همانطور که Alice این UTXO را خرج می‌کند، او Seal را بر روی پیامی که نشان‌دهنده کلید جدید اوست یا به سادگی ابطال کلید قدیمی است، می‌بندد. به این ترتیب، هر کسی که On-Chain را نظارت می‌کند، خواهد دید که UTXO خرج شده است، اما تنها کسانی که اثبات کامل را دارند خواهند دانست که این دقیقاً ابطال کلید PGP است.


![RGB-Bitcoin](assets/fr/027.webp)


برای اینکه Bob یا هر فرد دیگری که درگیر است بتواند پیام مخفی را بررسی کند، Alice باید اطلاعات off-chain را به او ارائه دهد.


![RGB-Bitcoin](assets/fr/028.webp)


بنابراین Alice باید موارد زیر را به Bob ارائه دهد:




- خود پیام (برای مثال، کلید PGP جدید)؛
- اثبات رمزنگاری که پیام در تراکنش دخیل بوده است (معروف به _اثبات تراکنش اضافی_ یا _لنگر_).


![RGB-Bitcoin](assets/fr/029.webp)


طرف‌های ثالث این اطلاعات را ندارند. آن‌ها فقط می‌بینند که یک UTXO خرج شده است. بنابراین محرمانگی تضمین می‌شود.


برای روشن شدن ساختار، بیایید فرآیند را در دو تراکنش خلاصه کنیم:




- تراکنش 1**: این شامل _تعریف مهر_ است، یعنی _نقطه خروجی_ که به عنوان Seal عمل خواهد کرد.


![RGB-Bitcoin](assets/fr/031.webp)




- تراکنش 2**: این _outpoint_ را خرج می‌کند. این عمل Seal را می‌بندد و در همان تراکنش، _commitment_ را بر روی پیام وارد می‌کند.


![RGB-Bitcoin](assets/fr/033.webp)


ما به همین دلیل، تراکنش دوم را "_تراکنش شاهد_" می‌نامیم.


برای نشان دادن این موضوع از زاویه‌ای دیگر، می‌توانیم دو لایه را نمایش دهیم:




- بالا Layer (Blockchain، عمومی)**: همه تراکنش را می‌بینند و می‌دانند که یک _outpoint_ خرج شده است؛
- Layer پایین‌تر (سمت مشتری، خصوصی)**: تنها Alice (یا شخص مربوطه) می‌داند که این هزینه به چنین و چنان پیامی مربوط می‌شود، از طریق اثبات رمزنگاری و پیامی که او به صورت محلی نگه می‌دارد.


![RGB-Bitcoin](assets/fr/034.webp)


اما هنگام بستن Seal، این سوال پیش می‌آید که _تعهد_ باید کجا درج شود.


در بخش قبلی، به طور مختصر اشاره کردیم که چگونه مدل Client-side Validation می‌تواند به RGB و سایر سیستم‌ها اعمال شود. در اینجا، به بخش **تعهدات قطعی Bitcoin** و چگونگی ادغام آن‌ها در یک تراکنش می‌پردازیم. ایده این است که بفهمیم چرا سعی داریم یک Commitment واحد را در _تراکنش شاهد_ وارد کنیم، و مهم‌تر از همه چگونه اطمینان حاصل کنیم که هیچ تعهد رقابتی افشا نشده دیگری وجود ندارد.


### مکان‌های Commitment در یک تراکنش


وقتی به کسی اثبات می‌دهید که یک پیام خاص در یک تراکنش جاسازی شده است، باید بتوانید تضمین کنید که شکل دیگری از Commitment (یک پیام دوم و مخفی) در همان تراکنش وجود ندارد که به شما فاش نشده باشد. برای اینکه Client-side Validation قوی باقی بماند، به یک مکانیزم **قطعی** برای قرار دادن یک _تعهد_ واحد در تراکنش نیاز دارید که _مهر و موم تک‌بار مصرف_ را ببندد.


تراکنش _شاهد_، UTXO معروف (یا _تعریف مهر_) را خرج می‌کند و این هزینه مربوط به بستن Seal است. از نظر فنی، می‌دانیم که هر نقطه خروج تنها یک بار می‌تواند خرج شود. این دقیقاً همان چیزی است که مقاومت Bitcoin در برابر خرج مضاعف را تضمین می‌کند. اما تراکنش خرج ممکن است چندین _ورودی_، چندین _خروجی_ داشته باشد یا به صورت پیچیده‌ای ترکیب شود (مانند کوین‌جوین‌ها، کانال‌های لایتنینگ و غیره). بنابراین، نیاز داریم که به وضوح تعریف کنیم که _تعهد_ را در این ساختار کجا و به چه صورت، به طور واضح و یکنواخت، وارد کنیم.


هر روشی که باشد (PkO، TxO2، و غیره)، _تعهد_ می‌تواند درج شود:




- در یک **ورودی** از طریق:
    - Sigtweak** (مولفه `r` امضای ECDSA را تغییر می‌دهد، مشابه اصل "Sign-to-Contract")؛
    - Witweak** (داده‌های _شاهد تفکیک‌شده_ تراکنش تغییر می‌کند).
- در یک **خروجی** از طریق:
    - Keytweak** (کلید عمومی گیرنده با پیام "تغییر" می‌یابد)؛
    - Opret** (پیام در یک خروجی غیرقابل‌مصرف `OP_RETURN` قرار داده شده است)؛
    - Tapret** (یا _Taptweak_) که به Taproot متکی است تا Commitment را در بخش اسکریپت یک کلید Taproot وارد کند و به این ترتیب کلید عمومی را به صورت قطعی تغییر دهد.


![RGB-Bitcoin](assets/fr/035.webp)


در اینجا جزئیات هر روش آمده است:


![RGB-Bitcoin](assets/fr/038.webp)


***تغییر سیگ (sign-to-Contract):***


یک طرح قبلی شامل بهره‌برداری از بخش تصادفی یک امضا (ECDSA یا Schnorr) برای جاسازی _تعهد_ بود: این تکنیک به عنوان "**Sign-to-Contract**" شناخته می‌شود. شما Nonce تولید شده به صورت تصادفی را با یک Hash که حاوی داده است جایگزین می‌کنید. به این ترتیب، امضا به طور ضمنی Commitment شما را فاش می‌کند، بدون هیچ فضای اضافی در تراکنش. این رویکرد دارای تعدادی مزیت است:




- بدون اضافه‌بار On-Chain (از همان مکان به عنوان Nonce پایه استفاده کنید)؛
- در تئوری، این می‌تواند کاملاً مجزا باشد، زیرا Nonce در ابتدا یک داده تصادفی است.


با این حال، ۲ نقطه ضعف عمده پدیدار شده‌اند:




- Multisig قبل از Taproot: وقتی چند امضاکننده دارید، باید تصمیم بگیرید که کدام امضا _تعهد_ را به همراه خواهد داشت. امضاها می‌توانند به ترتیب‌های مختلفی قرار گیرند و اگر یک امضاکننده امتناع کند، کنترل نتیجه _تعهد_ را از دست می‌دهید؛
- MuSig و Nonce مشترک: با Schnorr Multisig (*MuSig*), تولید Nonce یک الگوریتم چندجانبه است و دستکاری Nonce به صورت فردی تقریباً غیرممکن می‌شود.


در عمل، **تغییر سیگ** نیز با سخت‌افزارهای موجود (کیف‌پول‌های سخت‌افزاری) و فرمت‌ها (لایتنینگ، و غیره) خیلی سازگار نیست. بنابراین این ایده عالی Hard برای اجرا است.


***تغییر کلیدی (پرداخت به Contract):***


**تغییر کلیدی** مفهوم تاریخی _پرداخت به قرارداد_ را به خود می‌گیرد. ما کلید عمومی `X` را گرفته و با افزودن مقدار `H(message)` آن را تغییر می‌دهیم. به طور خاص، اگر `X = x * G` و `h = H(message)` باشد، کلید جدید به صورت `X' = X + h * G` خواهد بود. این کلید تغییر یافته Commitment را به `message` پنهان می‌کند. دارنده کلید خصوصی اصلی می‌تواند با افزودن `h` به کلید خصوصی خود `x`، ثابت کند که کلید خرج کردن خروجی را دارد. از نظر تئوری، این زیباست، زیرا:




- _تعهد_ بدون افزودن هیچ فیلد اضافی وارد می‌شود؛
- شما هیچ داده اضافی On-Chain را ذخیره نمی‌کنید.


با این حال، در عمل با مشکلات زیر مواجه می‌شویم:




- کیف‌پول‌ها دیگر کلید عمومی استاندارد را تشخیص نمی‌دهند، زیرا "دستکاری" شده است، بنابراین نمی‌توانند به‌راحتی UTXO را با کلید معمولی شما مرتبط کنند؛
- کیف پول‌های سخت‌افزاری برای امضای کلیدی که از مشتق استاندارد آن‌ها مشتق نشده است، طراحی نشده‌اند؛
- شما باید اسکریپت‌ها، توصیف‌گرها و غیره خود را تطبیق دهید.


در زمینه RGB، این مسیر تا سال 2021 پیش‌بینی شده بود، اما ثابت شد که با استانداردها و زیرساخت‌های فعلی بیش از حد پیچیده است تا کار کند.


***شاهد تغییر:***


ایده دیگری که پروتکل‌های خاصی مانند _inscriptions Ordinals_ به کار گرفته‌اند، قرار دادن داده‌ها به‌طور مستقیم در بخش `witness` تراکنش است (از این رو به آن "witness tweak" گفته می‌شود). با این حال، این روش:




- تعامل را بلافاصله قابل مشاهده می‌کند (شما به معنای واقعی کلمه داده‌های خام را در شاهد قرار می‌دهید)؛
- ممکن است مشمول سانسور شود (ماینرها یا نودها ممکن است از انتقال آن خودداری کنند اگر بیش از حد بزرگ باشد یا هر ویژگی دلخواه دیگری داشته باشد)؛
- فضا را در بلوک‌ها اشغال می‌کند، برخلاف هدف RGB که پنهان‌کاری و سبکی است.


علاوه بر این، شاهد به گونه‌ای طراحی شده است که در برخی زمینه‌ها قابل هرس باشد، که می‌تواند داشتن اثبات‌های قوی را پیچیده‌تر کند.


***بازگشت باز (opret):***


بسیار ساده در عملکرد خود، یک `OP_RETURN` به شما اجازه می‌دهد تا یک Hash یا پیام را در یک فیلد خاص از تراکنش ذخیره کنید. اما این بلافاصله قابل تشخیص است: همه می‌بینند که یک _تعهد_ در تراکنش وجود دارد و می‌تواند سانسور یا حذف شود، همچنین می‌تواند خروجی اضافی اضافه کند. از آنجا که این شفافیت و اندازه را افزایش می‌دهد، از دیدگاه یک راه‌حل Client-side Validation کمتر رضایت‌بخش در نظر گرفته می‌شود.


```txt
34-byte_Opret_Commitment =
OP_RETURN   OP_PUSHBYTE_32   <mpc::Commitment>
|_________| |______________| |_________________|

1-byte       1-byte         32 bytes
```


### تاپرت


گزینه نهایی استفاده از **Taproot** (معرفی شده با BIP341) با طرح *Tapret* است. *Tapret* یک فرم پیچیده‌تر از Commitment قطعی است که بهبودهایی از نظر ردپای Blockchain و محرمانگی برای عملیات Contract به ارمغان می‌آورد. ایده اصلی این است که Commitment را در بخش `Script Path Spend` یک [تراکنش Taproot](https://github.com/Bitcoin/bips/blob/master/bip-0341.mediawiki) پنهان کند.


![RGB-Bitcoin](assets/fr/036.webp)


قبل از توضیح چگونگی وارد کردن Commitment به یک تراکنش Taproot، بیایید به **فرم دقیق** Commitment نگاه کنیم، که باید **به طور ضروری** با یک رشته ۶۴ بایتی [ساخته شده](https://github.com/BP-WG/bp-core/blob/master/dbc/src/tapret/mod.rs#L179-L196) به صورت زیر مطابقت داشته باشد:


```txt
64-byte_Tapret_Commitment =

OP_RESERVED ...  ... .. OP_RESERVED   OP_RETURN   OP_PUSHBYTE_33  <mpc::Commitment>  <Nonce>
|___________________________________| |_________| |______________| |_______________|  |______|
OP_RESERVED x 29 times = 29 bytes      1 byte         1 byte          32 bytes        1 byte
|________________________________________________________________| |_________________________|

TAPRET_SCRIPT_COMMITMENT_PREFIX = 31 bytes                    MPC commitment + NONCE = 33 bytes
```




- 29 بایت `OP_RESERVED`، به دنبال آن `OP_RETURN`، سپس `OP_PUSHBYTE_33`، بخش _پیشوند_ 31 بایتی را تشکیل می‌دهند؛
- سپس یک _تعهد_ ۳۲ بایتی (معمولاً Merkle Root از **MPC**) می‌آید، که به آن ۱ بایت از **Nonce** اضافه می‌کنیم (در مجموع ۳۳ بایت برای این بخش دوم).


بنابراین روش ۶۴ بایتی `Tapret` شبیه به یک `Opret` است که به آن ۲۹ بایت `OP_RESERVED` اضافه کرده‌ایم و یک بایت اضافی به عنوان Nonce افزوده‌ایم.


برای حفظ انعطاف‌پذیری در زمینه پیاده‌سازی، محرمانگی و مقیاس‌پذیری، طرح Tapret موارد استفاده مختلفی را با توجه به نیازها در نظر می‌گیرد:




- ادغام منحصر به فرد یک Tapret Commitment در یک تراکنش Taproot بدون ساختار مسیر اسکریپت از پیش موجود؛
- ادغام Tapret Commitment در یک تراکنش Taproot که قبلاً به یک مسیر اسکریپت مجهز شده است.


بیایید نگاهی دقیق‌تر به هر یک از این دو سناریو بیندازیم.


#### ادغام Tapret بدون مسیر اسکریپت موجود


در این مورد اول، ما از یک کلید خروجی Taproot (*Taproot Output Key*) `Q` شروع می‌کنیم که فقط شامل کلید عمومی داخلی `P` *(Internal Key*) است، بدون مسیر اسکریپت مرتبط (*Script Path*):


![RGB-Bitcoin](assets/fr/047.webp)




- `P`: کلید عمومی داخلی برای _Key Path Spend_.
- `G`: نقطه تولیدی منحنی بیضوی [secp256k1](https://en.Bitcoin.it/wiki/Secp256k1).

-`t = tH_TWEAK(P)` فاکتور تغییر است که از طریق یک _هش برچسب‌دار_ محاسبه می‌شود (مثلاً `SHA-256(SHA-256(TapTweak) || P)`)، مطابق با [BIP86](https://github.com/Bitcoin/bips/blob/master/bip-0086.mediawiki#Address-derivation). این ثابت می‌کند که هیچ اسکریپت مخفی وجود ندارد.


برای اضافه کردن یک **Tapret** Commitment، یک **Script Path Spend** با یک **اسکریپت منحصر به فرد** به صورت زیر اضافه کنید:


![RGB-Bitcoin](assets/fr/048.webp)




- `t = tH_TWEAK(P || Script_root)` سپس به عامل تغییر جدید تبدیل می‌شود که شامل **Script_root** است.
- `Script_root = tH_BRANCH(64-byte_Tapret_Commitment)` نشان‌دهنده ریشه این **اسکریپت** است که به سادگی یک Hash از نوع `SHA-256(SHA-256(TapBranch) || 64-byte_Tapret_Commitment)` می‌باشد.


اثبات شمولیت و یکتایی در درخت Taproot در اینجا به کلید عمومی داخلی واحد `P` خلاصه می‌شود.


#### ادغام Tapret در مسیر اسکریپت موجود


سناریوی دوم مربوط به خروجی پیچیده‌تر `Q` **Taproot** است که از قبل شامل چندین اسکریپت می‌باشد. به عنوان مثال، ما یک درخت با 3 اسکریپت داریم:


![RGB-Bitcoin](assets/fr/049.webp)




- `tH_LEAF(x)` تابع برچسب‌گذاری شده نرمالیزه شده Hash یک اسکریپت برگ را تعیین می‌کند.
- `A, B, C` نشان‌دهنده اسکریپت‌هایی هستند که قبلاً در ساختار Taproot گنجانده شده‌اند.


برای اضافه کردن Tapret Commitment، باید یک *اسکریپت غیرقابل‌مصرف* را در سطح اول درخت وارد کنیم و اسکریپت‌های موجود را یک سطح پایین‌تر ببریم. به صورت بصری، درخت به این شکل می‌شود:


![RGB-Bitcoin](assets/fr/050.webp)




- `tHABC` نشان‌دهنده‌ی Hash برچسب‌گذاری‌شده از گروه‌بندی سطح بالا `A, B, C` است.
- `tHT` نشان‌دهنده Hash از اسکریپت مربوط به 64-byte `Tapret` است.


طبق قوانین Taproot، هر شاخه/برگ باید بر اساس ترتیب لغت‌نامه‌ای Hash ترکیب شود. دو حالت ممکن وجود دارد:




- `tHT` > `tHABC`**: تاپرت Commitment به سمت راست درخت حرکت می‌کند. اثبات یکتایی فقط به `tHABC` و `P` نیاز دارد؛
- `tHT` < `tHABC`**: Tapret Commitment در سمت چپ قرار دارد. برای اثبات اینکه هیچ Tapret Commitment دیگری در سمت راست وجود ندارد، باید `tHAB` و `tHC` فاش شوند تا عدم وجود هرگونه اسکریپت دیگر از این نوع نشان داده شود.


مثال بصری برای حالت اول (`tHABC < tHT`):


![RGB-Bitcoin](assets/fr/051.webp)


مثال برای حالت دوم (`tHABC > tHT`):


![RGB-Bitcoin](assets/fr/052.webp)


#### بهینه‌سازی با Nonce


برای بهبود محرمانگی، می‌توانیم مقدار `<Nonce>` (اصطلاح دقیق‌تر "bruteforcing" خواهد بود) را "استخراج" کنیم (آخرین بایت از `Tapret` ۶۴ بایتی) تا تلاش کنیم یک `tHT` از Hash به دست آوریم به‌طوری‌که `tHABC < tHT`. در این حالت، Commitment در سمت راست قرار می‌گیرد و کاربر را از افشای کل محتوای اسکریپت‌های موجود برای اثبات یکتایی Tapret بی‌نیاز می‌کند.


به طور خلاصه، `Tapret` یک روش مجزا و قطعی برای وارد کردن یک Commitment به یک تراکنش Taproot ارائه می‌دهد، در حالی که نیاز به یکتایی و عدم ابهام که برای منطق Client-side Validation و Single-Use Seal در RGB ضروری است را رعایت می‌کند.


#### خروج‌های معتبر


برای تراکنش‌های RGB Commitment، نیاز اصلی برای یک طرح معتبر Bitcoin Commitment به شرح زیر است: تراکنش (*Witness Transaction*) باید به طور قابل اثبات شامل یک Commitment باشد. این نیاز باعث می‌شود که ساخت یک تاریخچه جایگزین برای داده‌های اعتبارسنجی شده در سمت مشتری در همان تراکنش غیرممکن شود. این بدان معناست که پیامی که _مهر یک‌بار مصرف_ حول آن بسته می‌شود، منحصر به فرد است.


برای رعایت این اصل، و بدون توجه به تعداد خروجی‌ها در یک تراکنش، ما نیاز داریم که **فقط و فقط یک خروجی** می‌تواند شامل یک Commitment باشد. برای هر یک از طرح‌های استفاده شده (*Opret* یا *Tapret*)، تنها خروجی‌های معتبری که می‌توانند شامل یک _تعهد_ RGB باشند به شرح زیر هستند:




- اولین خروجی `OP_RETURN` (در صورت وجود) برای طرح *Opret*؛
- اولین خروجی Taproot (در صورت وجود) برای طرح *Tapret*.


توجه داشته باشید که این امکان وجود دارد که یک تراکنش شامل یک `Opret` Commitment و یک `Tapret` Commitment در دو خروجی جداگانه باشد. به لطف ماهیت قطعی Seal Definition، این دو تعهد سپس به دو قطعه داده متمایز که در سمت مشتری اعتبارسنجی شده‌اند، مربوط می‌شوند.


### تحلیل و انتخاب‌های عملی در RGB


وقتی RGB را شروع کردیم، همه این روش‌ها را بررسی کردیم تا تعیین کنیم که کجا و چگونه یک _تعهد_ را به صورت قطعی در یک تراکنش قرار دهیم. ما برخی معیارها را تعریف کردیم:




- سازگاری با سناریوهای مختلف (مثلاً Multisig، Lightning، کیف‌پول‌های سخت‌افزاری، و غیره)؛
- تأثیر بر فضای On-Chain؛
- مشکل در پیاده‌سازی و نگهداری؛
- محرمانگی و مقاومت در برابر سانسور.


| Method                                             | On-chain trace & size | Client-side size | Wallet Integration | Hardware Compatibility | Lightning Compatibility | Taproot Compatibility |
| -------------------------------------------------- | --------------------- | ---------------- | ------------------ | ---------------------- | ---------------------- | --------------------- |
| Keytweak (deterministic P2C)                      | 🟢                     | 🟡                 | 🔴                   | 🟠                     | 🔴 BOLT, 🔴 Bifrost     | 🟠 Taproot, 🟢 MuSig  |
| Sigtweak (deterministic S2C)                      | 🟢                     | 🟢                 | 🟠                   | 🔴                     | 🔴 BOLT, 🔴 Bifrost     | 🟠 Taproot, 🔴 MuSig  |
| Opret (OP_RETURN)                                 | 🔴                     | 🟢                 | 🟢                   | 🟠                     | 🔴 BOLT, 🟠 Bifrost     | -                     |
| Tapret Algorithm: top-left node                   | 🟠                     | 🔴                 | 🟠                   | 🟢                     | 🔴 BOLT, 🟢 Bifrost     | 🟢 Taproot, 🟢 MuSig  |
| Tapret Algorithm #4: any node + proof             | 🟢                     | 🟠                 | 🟠                   | 🟢                     | 🔴 BOLT, 🟢 Bifrost     | 🟢 Taproot, 🟢 MuSig  |
| Deterministic Commitment Scheme                               | Standard       | On-Chain Cost                                                                                                          | Proof Size on Client Side                                                                                       |
| ------------------------------------------------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| Keytweak (Deterministic P2C)                                  | LNPBP-1, 2     | 0 bytes                                                                                                                | 33 bytes (non-tweaked key)                                                                                       |
| Sigtweak (Deterministic S2C)                                  | WIP (LNPBP-39) | 0 bytes                                                                                                                | 0 bytes                                                                                                          |
| Opret (OP_RETURN)                                             | -              | 36 (v)bytes (additional TxOut)                                                                                         | 0 bytes                                                                                                          |
| Tapret Algorithm: top-left node                               | LNPBP-6        | 32 bytes in the witness (8 vbytes) for any n-of-m multisig and spending through script path                           | 0 bytes on scriptless scripts taproot ~270 bytes in a single script case, ~128 bytes if multiple scripts       |
| Tapret Algorithm #4: any node + uniqueness proof              | LNPBP-6        | 32 bytes in the witness (8 vbytes) for single script cases, 0 bytes in the witness in most other cases                | 0 bytes on scriptless scripts taproot, 65 bytes until the Taptree contains a dozen scripts                      |


| Layer                          | On-Chain Cost (Bytes/vbytes) | On-Chain Cost (Bytes/vbytes) | On-Chain Cost (Bytes/vbytes) | On-Chain Cost (Bytes/vbytes) | On-Chain Cost (Bytes/vbytes) | Client-Side Cost (Bytes) | Client-Side Cost (Bytes) | Client-Side Cost (Bytes) | Client-Side Cost (Bytes) | Client-Side Cost (Bytes) |
| ------------------------------ | ---------------------------- | ---------------------------- | ---------------------------- | ---------------------------- | ---------------------------- | ------------------------ | ------------------------ | ------------------------ | ------------------------ | ------------------------ |
| **Type**                       | **Tapret**                   | **Tapret #4**                | **Keytweak**                 | **Sigtweak**                 | **Opret**                    | **Tapret**               | **Tapret #4**            | **Keytweak**             | **Sigtweak**             | **Opret**                |
| Single-sig                     | 0                            | 0                            | 0                            | 0                            | 32                           | 0                        | 0                        | 32                       | 0?                       | 0                        |
| MuSig (n-of-n)                 | 0                            | 0                            | 0                            | 0                            | 32                           | 0                        | 0                        | 32                       | ? > 0                    | 0                        |
| Multi-sig 2-of-3               | 32/8                         | 32/8 or 0                    | 0                            | n/a                          | 32                           | ~270                     | 65                       | 32                       | n/a                      | 0                        |
| Multi-sig 3-of-5               | 32/8                         | 32/8 or 0                    | 0                            | n/a                          | 32                           | ~340                     | 65                       | 32                       | n/a                      | 0                        |
| Multi-sig 2-of-3 with timeouts | 32/8                         | 0                            | 0                            | n/a                          | 32                           | 64                       | 65                       | 32                       | n/a                      | 0                        |


| Layer                            | On-Chain Cost (vbytes) | On-Chain Cost (vbytes) | On-Chain Cost (vbytes) | Client-Side Cost (bytes) | Client-Side Cost (bytes) |
| -------------------------------- | ---------------------- | ---------------------- | ---------------------- | ------------------------ | ------------------------ |
| **Type**                         | **Base**               | **Tapret #2**          | **Tapret #4**          | **Tapret #2**            | **Tapret #4**            |
| MuSig (n-of-n)                   | 16.5                   | 0                      | 0                      | 0                        | 0                        |
| FROST (n-of-m)                   | ?                      | 0                      | 0                      | 0                        | 0                        |
| Multi_a (n-of-m)                 | 1+16n+8m               | 8                      | 8                      | 33 * m                   | 65                       |
| Branch MuSig / Multi_a (n-of-m)  | 1+16n+8n+8xlog(n)      | 8                      | 0                      | 64                       | 65                       |
| With timeouts (n-of-m)           | 1+16n+8n+8xlog(n)      | 8                      | 0                      | 64                       | 65                       |
| Method                                    | Privacy & Scalability | Interoperability | Compatibility | Portability | Complexity |
| ----------------------------------------- | ---------------------- | ---------------- | ------------- | ----------- | ---------- |
| Keytweak (Deterministic P2C)              | 🟢                      | 🔴               | 🔴            | 🟡          | 🟡         |
| Sigtweak (Deterministic S2C)              | 🟢                      | 🔴               | 🔴            | 🟢          | 🔴         |
| Opret (OP_RETURN)                         | 🔴                      | 🟠               | 🔴            | 🟢          | 🟢         |
| Algo Tapret: Top-left node                | 🟠                      | 🟢               | 🟢            | 🔴          | 🟠         |
| Algo Tapret #4: Any node + proof          | 🟢                      | 🟢               | 🟢            | 🟠          | 🔴         |






در طول مطالعه، مشخص شد که هیچ‌یک از طرح‌های Commitment به‌طور کامل با استاندارد فعلی Lightning سازگار نیستند (که از Taproot، _muSig2_ یا پشتیبانی اضافی _commitment_ استفاده نمی‌کند). تلاش‌هایی در جریان است تا ساختار کانال Lightning (*BiFrost*) اصلاح شود تا امکان درج تعهدات RGB فراهم شود. این حوزه دیگری است که نیاز به بررسی ساختار تراکنش، کلیدها و نحوه امضای به‌روزرسانی‌های کانال داریم.


تجزیه و تحلیل نشان داد که در واقع، روش‌های دیگر (تغییر کلید، تغییر امضا، تغییر شاهد، و غیره) اشکال دیگری از پیچیدگی را ارائه دادند:




- یا ما حجم زیادی از On-Chain داریم؛
- یا یک ناسازگاری اساسی با کد موجود Wallet وجود دارد؛
- یا راه‌حل در Multisig غیرهمکارانه قابل اجرا نیست.


برای RGB، دو روش به‌ویژه برجسته هستند: ***Opret*** و ***Tapret***، هر دو به‌عنوان "خروجی تراکنش" طبقه‌بندی شده‌اند و با حالت TxO2 که توسط پروتکل استفاده می‌شود، سازگار هستند.


### تعهدات چند پروتکلی - MPC


در این بخش، ما بررسی می‌کنیم که چگونه **RGB** تجمیع چندین قرارداد (یا به‌طور دقیق‌تر، _بسته‌های انتقال_ آن‌ها) را در یک Commitment (*Commitment*) که در یک تراکنش Bitcoin از طریق یک طرح قطعی (طبق `Opret` یا `Tapret`) ثبت شده است، مدیریت می‌کند. برای دستیابی به این هدف، ترتیب مرکل‌سازی قراردادهای مختلف در ساختاری به نام **درخت MPC** (_درخت چند پروتکل Commitment_) انجام می‌شود. در این بخش، ما به ساختار این درخت MPC، نحوه به‌دست آوردن ریشه آن، و چگونگی اشتراک‌گذاری محرمانه و بدون ابهام چندین قرارداد در یک تراکنش خواهیم پرداخت.


Multi Protocol Commitment (MPC) برای پاسخگویی به دو نیاز طراحی شده است:




- ساخت `mpc::Commitment` Hash: این در Bitcoin Blockchain بر اساس یک طرح `Opret` یا `Tapret` گنجانده خواهد شد و باید تمام تغییرات وضعیت را که باید تأیید شوند، منعکس کند؛
- ذخیره‌سازی همزمان چندین قرارداد در یک _تعهد_، که امکان به‌روزرسانی‌های جداگانه بر روی چندین دارایی یا قراردادهای RGB را در یک تراکنش Bitcoin فراهم می‌کند.


به زبان ساده، هر _بسته انتقال_ به یک Contract خاص تعلق دارد. تمام این اطلاعات در یک **درخت MPC** قرار می‌گیرد که ریشه آن (`mpc::Root`) سپس دوباره هش می‌شود تا `mpc::Commitment` را بدهد. این آخرین Hash است که در تراکنش Bitcoin (_تراکنش شاهد_) قرار می‌گیرد، بر اساس روش تعیین‌شده‌ی قطعی.


![RGB-Bitcoin](assets/fr/042.webp)


#### MPC Root Hash


مقدار واقعاً نوشته شده On-Chain (در `Opret` یا `Tapret`) به عنوان `mpc::Commitment` نامیده می‌شود. این مقدار به صورت [BIP-341](https://github.com/Bitcoin/bips/blob/master/bip-0341.mediawiki) و بر اساس فرمول زیر محاسبه می‌شود:


```txt
mpc::Commitment = SHA-256(SHA-256(mpc_tag) || SHA-256(mpc_tag) || depth || cofactor || mpc::Root )
```


کجا:




- `mpc_tag` یک برچسب است: `urn:ubideco:mpc:Commitment#2024-01-31`، که بر اساس [کنوانسیون‌های برچسب‌گذاری RGB](https://github.com/RGB-WG/RGB-core/blob/master/doc/Commitments.md) انتخاب شده است؛
- `depth` (1 بایت) نشان‌دهنده عمق *درخت MPC* است؛
- کوفاکتور` (16 بیت، به صورت Little Endian) پارامتری است که برای ارتقاء یکتایی موقعیت‌های اختصاص داده شده به هر Contract در درخت استفاده می‌شود؛
- `mpc::Root` ریشه *MPC Tree* است که بر اساس فرآیند توضیح داده شده در بخش بعدی محاسبه می‌شود.


![RGB-Bitcoin](assets/fr/044.webp)


#### ساخت درخت MPC


برای ساخت این درخت MPC، باید اطمینان حاصل کنیم که هر Contract به یک موقعیت برگ منحصر به فرد مربوط می‌شود. فرض کنید ما داریم:




- `c` قراردادهایی که باید گنجانده شوند، با شاخص `i` در `i` = {0,1,..,C-1};
- برای هر Contract `c_i`، یک شناسه داریم `ContractId(i) = c_i`.


سپس یک درخت با عرض `w` و عمق `d` می‌سازیم به طوری که `2^d = w` و `w > C`، به گونه‌ای که هر Contract می‌تواند در یک _برگ_ جداگانه قرار گیرد. موقعیت `pos(c_i)` هر Contract در درخت توسط:


```txt
pos(c_i) = c_i mod (w - cofactor)
```


جایی که `ضریب` یک عدد صحیح است که احتمال به دست آوردن موقعیت‌های متمایز برای هر Contract را افزایش می‌دهد. در عمل، ساخت و ساز از یک فرآیند تکراری پیروی می‌کند:




- ما از یک عمق حداقل (`d=3` به‌طور قراردادی برای پنهان کردن تعداد دقیق قراردادها) شروع می‌کنیم؛
- ما عوامل `cofactors` مختلفی را امتحان می‌کنیم (تا `w/2`، یا حداکثر ۵۰۰ به دلایل عملکردی)؛
- اگر نتوانیم همه قراردادها را بدون برخورد موقعیت‌دهی کنیم، `d` را افزایش می‌دهیم و دوباره شروع می‌کنیم.


هدف این است که از درختانی که بیش از حد بلند هستند اجتناب شود، در حالی که خطر برخورد به حداقل برسد. توجه داشته باشید که پدیده برخورد از یک منطق توزیع تصادفی پیروی می‌کند که به [پارادوکس سالگرد](https://en.wikipedia.org/wiki/Birthday_problem) مرتبط است.


#### برگ‌های مسکونی


هنگامی که `C` موقعیت متمایز `pos(c_i)` برای قراردادها `i = {0,1,..,C-1}` به دست آمد، هر برگه با یک تابع Hash (*برچسب‌گذاری شده Hash*) پر می‌شود:


```txt
tH_MPC_LEAF(c_i) = SHA-256(SHA-256(merkle_tag) || SHA-256(merkle_tag) || 0x10 || c_i || BundleId(c_i))
```


کجا:




- `merkle_tag = urn:ubideco:merkle:node#2024-01-31`، همیشه بر اساس قراردادهای مرکل RGB انتخاب می‌شود؛
- `0x10` یک _برگ قرارداد_ را شناسایی می‌کند؛
- `c_i` شناسه ۳۲ بایتی Contract است (مشتق شده از Genesis Hash)؛
- `bundleId(c_i)` یک Hash به طول 32 بایت است که مجموعه‌ای از `State Transitions` مربوط به `c_i` را توصیف می‌کند (که در یک *Transition Bundle* جمع‌آوری شده‌اند).


#### برگ‌های بی‌سکنه


برگ‌های باقی‌مانده که به Contract اختصاص داده نشده‌اند (یعنی برگ‌های `w - C`)، با یک مقدار "ساختگی" پر می‌شوند (_برگ آنتروپی_):


```txt
tH_MPC_LEAF(j) = SHA-256(SHA-256(merkle_tag) || SHA-256(merkle_tag) || 0x11 || entropy || j )
```


کجا:




- `merkle_tag = urn:ubideco:merkle:node#2024-01-31`، همیشه مطابق با قراردادهای مرکل RGB انتخاب می‌شود؛
- `0x11` نشان‌دهنده یک _برگ آنتروپی_ است؛
- `entropy` یک مقدار تصادفی ۶۴ بایتی است که توسط فردی که درخت را می‌سازد انتخاب می‌شود؛
- `j` موقعیت (در 32 بیت Little Endian) این برگ در درخت است.


#### گره‌های MPC


پس از تولید برگ‌های `w` (مسکونی یا غیرمسکونی)، به مرکل‌سازی ادامه می‌دهیم. هر گره داخلی به صورت زیر هش می‌شود:


```txt
tH_MPC_BRANCH(tH1 || tH2) = SHA-256(SHA-256(merkle_tag) || SHA-256(merkle_tag) || b || d || w || tH1 || tH2)
```


کجا:




- `merkle_tag = urn:ubideco:merkle:node#2024-01-31`، همیشه بر اساس قراردادهای مرکل RGB انتخاب می‌شود؛
- b` عامل _انشعاب_ است (8 بیت). اغلب، `b=0x02` زیرا درخت دودویی و کامل است؛
- d` عمق گره در درخت است؛
- `w` عرض درخت است (در باینری Little Endian 256 بیتی)؛
- `tH1` و `tH2` هش‌های نودهای فرزند (یا برگ‌ها) هستند که قبلاً همان‌طور که در بالا نشان داده شده است محاسبه شده‌اند.


با پیشرفت به این روش، ما ریشه `mpc::Root` را به دست می‌آوریم. سپس می‌توانیم `mpc::Commitment` (همان‌طور که در بالا توضیح داده شد) را محاسبه کرده و آن را در On-Chain وارد کنیم.


برای توضیح این موضوع، بیایید مثالی را تصور کنیم که در آن `C=3` (سه قرارداد) باشد. موقعیت‌های آن‌ها به صورت `pos(c_0)=7`، `pos(c_1)=4`، `pos(c_2)=2` فرض شده‌اند. برگ‌های دیگر (موقعیت‌های 0، 1، 3، 5، 6) _برگ‌های آنتروپی_ هستند. نمودار زیر توالی هش‌ها به ریشه را نشان می‌دهد با:




- `BUNDLE_i` که نشان‌دهنده‌ی `BundleId(c_i)` است؛
- `tH_MPC_LEAF(A)` و غیره، که نشان‌دهنده برگ‌ها هستند (برخی برای قراردادها، برخی دیگر برای آنتروپی)؛
- هر شاخه `tH_MPC_BRANCH(...)` هش‌های دو فرزند خود را ترکیب می‌کند.


نتیجه نهایی **mpc::Root** است، سپس `mpc::Commitment`.


![RGB-Bitcoin](assets/fr/053.webp)


#### بررسی شفت MPC


هنگامی که یک تأییدکننده می‌خواهد اطمینان حاصل کند که یک `c_i` Contract (و `BundleId` آن) در `mpc::Commitment` نهایی گنجانده شده است، به سادگی یک اثبات مرکل دریافت می‌کند. این اثبات نشان‌دهنده گره‌های مورد نیاز برای ردیابی برگ‌ها (در این مورد، _برگ قرارداد_ `c_i`) به ریشه است. نیازی به افشای کل *درخت MPC* نیست: این امر محرمانگی قراردادهای دیگر را حفظ می‌کند.


در این مثال، یک تأییدکننده `c_2` تنها به یک Hash میانی (`tH_MPC_LEAF(D)`)، دو `tH_MPC_BRANCH(...)`، اثبات موقعیت `pos(c_2)` و مقدار `cofactor` نیاز دارد. سپس می‌تواند به صورت محلی ریشه را بازسازی کند، سپس `mpc::Commitment` را مجدداً محاسبه کرده و آن را با مورد نوشته شده در تراکنش Bitcoin (درون `Opret` یا `Tapret`) مقایسه کند.


![RGB-Bitcoin](assets/fr/054.webp)


این مکانیزم اطمینان حاصل می‌کند که:




- وضعیت نسبت به `c_2` واقعاً در بلوک اطلاعات تجمیعی (سمت مشتری) گنجانده شده است؛
- هیچ‌کس نمی‌تواند یک تاریخ جایگزین با همان تراکنش بسازد، زیرا _تعهد_ On-Chain به یک ریشه MPC واحد اشاره دارد.


#### خلاصه‌ای از ساختار MPC


Multi Protocol Commitment* (MPC) اصل اساسی است که به RGB اجازه می‌دهد تا چندین قرارداد را به یک تراکنش Bitcoin واحد تجمیع کند، در حالی که یکتایی تعهدات و محرمانگی در مقابل سایر شرکت‌کنندگان را حفظ می‌کند. به لطف ساختار قطعی درخت، هر Contract به یک موقعیت منحصر به فرد اختصاص داده می‌شود و حضور برگ‌های "جعلی" (*Entropy Leaves*) به‌طور جزئی تعداد کل قراردادهای شرکت‌کننده در تراکنش را پنهان می‌کند.


کل Merkle Tree هرگز در سمت مشتری ذخیره نمی‌شود. ما به سادگی یک _مسیر مرکل_ برای هر Contract مربوطه generate می‌کنیم، تا به گیرنده منتقل شود (که سپس می‌تواند Commitment را اعتبارسنجی کند). در برخی موارد، ممکن است چندین دارایی داشته باشید که از همان UTXO عبور کرده‌اند. در این صورت می‌توانید چندین _مسیر مرکل_ را در یک _بلوک Commitment چند پروتکلی_ ادغام کنید، تا از تکرار بیش از حد داده‌ها جلوگیری شود.


هر _اثبات مرکل_ بنابراین سبک است، به‌ویژه از آنجا که عمق درخت در RGB از ۳۲ تجاوز نخواهد کرد. همچنین مفهومی به نام "بلوک مرکل" وجود دارد که اطلاعات بیشتری (مقطع، آنتروپی، و غیره) را حفظ می‌کند و برای ترکیب یا جدا کردن چندین شاخه مفید است.


به همین دلیل طول کشید تا RGB نهایی شود. ما از سال ۲۰۱۹ چشم‌انداز کلی را داشتیم: قرار دادن همه چیز در سمت مشتری، گردش توکن‌های off-chain. اما برای جزئیاتی مانند شاردینگ برای قراردادهای متعدد، ساختار Merkle Tree، نحوه مدیریت برخوردها و ادغام اثبات‌ها... همه این‌ها نیاز به تکرار داشت.


### لنگرها: یک گردهمایی جهانی


در ادامه ساخت تعهدات ما (`Opret` یا `Tapret`) و MPC ما (*Multi Protocol Commitment*)، نیاز داریم تا مفهوم **Anchor** را در پروتکل RGB پیاده‌سازی کنیم. یک Anchor ساختاری است که در سمت مشتری اعتبارسنجی می‌شود و Elements مورد نیاز برای تأیید اینکه یک Bitcoin Commitment واقعاً حاوی اطلاعات قراردادی خاصی است را گرد هم می‌آورد. به عبارت دیگر، یک Anchor تمام داده‌های مورد نیاز برای اعتبارسنجی _تعهدات_ توصیف شده در بالا را خلاصه می‌کند.


یک Anchor شامل سه فیلد مرتب است:




- `txid`
- `اثبات MPC`
- اثبات تراکنش اضافی - ETP


هر یک از این زمینه‌ها نقشی در فرآیند اعتبارسنجی ایفا می‌کند، چه این مسئله بازسازی تراکنش Bitcoin باشد یا اثبات وجود یک Commitment مخفی (به‌ویژه در مورد `Tapret`).


#### txid


فیلد `txid` با شناسه ۳۲ بایتی تراکنش Bitcoin که شامل `Opret` یا `Tapret` Commitment است، مطابقت دارد.


از نظر تئوری، می‌توان با ردیابی زنجیره انتقالات حالت که خود به هر Witness Transaction اشاره می‌کنند، این `txid` را پیدا کرد و از منطق مهر و موم‌های یک‌بار مصرف پیروی کرد. با این حال، برای تسهیل و تسریع در تأیید، این `txid` به سادگی در Anchor گنجانده شده است و بدین ترتیب اعتبارسنج را از نیاز به بازگشت به کل تاریخچه off-chain نجات می‌دهد.


#### اثبات MPC


فیلد دوم، `MPC Proof`، به اثباتی اشاره دارد که این Contract خاص (مثلاً `c_i`) در _Multi Protocol Commitment_ گنجانده شده است. این ترکیبی است از:




- `pos_i`، موقعیت این Contract در درخت MPC؛
- عامل مشترک، مقداری که برای حل برخوردهای موقعیت تعریف شده است؛
- `اثبات مرکل`، یعنی مجموعه‌ای از نودها و هش‌ها که برای بازسازی ریشه MPC و تأیید اینکه شناسه Contract و `Transition Bundle` آن به ریشه متعهد شده‌اند، استفاده می‌شود.


این مکانیزم در بخش قبلی درباره ساخت *MPC Tree* توضیح داده شد، جایی که هر Contract به لطف موارد زیر یک برگ منحصر به فرد به دست می‌آورد:


```txt
pos(c_i) = c_i mod (w - cofactor)
```


سپس، یک طرح مرکل‌سازی قطعی برای تجمیع تمام برگ‌ها (قراردادها + آنتروپی) استفاده می‌شود. در نهایت، `MPC Proof` اجازه می‌دهد تا ریشه به صورت محلی بازسازی شده و با `mpc::Commitment` شامل On-Chain مقایسه شود.


#### اثبات تراکنش اضافی - ETP


سومین فیلد، **ETP**، به نوع Commitment استفاده شده بستگی دارد. اگر Commitment از نوع `Opret` باشد، هیچ مدرک اضافی لازم نیست. اعتبارسنج خروجی اول `OP_RETURN` تراکنش را بررسی می‌کند و `mpc::Commitment` را مستقیماً در آنجا می‌یابد.


**اگر Commitment از نوع `Tapret` باشد**، یک مدرک اضافی به نام *اثبات تراکنش اضافی - ETP* باید ارائه شود. این شامل:




- کلید عمومی داخلی (`P`) خروجی Taproot که در آن *Commitment* تعبیه شده است؛
- گره‌های شریک `Script Path Spend` (هنگامی که Tapret *Commitment* در یک اسکریپت درج می‌شود)، به منظور اثبات مکان دقیق این اسکریپت در درخت Taproot:
 - اگر `Tapret` *Commitment* در شاخه سمت راست باشد، گره سمت چپ را آشکار می‌کنیم (مثلاً `tHABC`)،
 - اگر `Tapret` *Commitment* در سمت چپ است، باید 2 گره (مثلاً `tHAB` و `tHC`) را افشا کنید تا ثابت کنید که هیچ *Commitment* دیگری در سمت راست وجود ندارد.
- `Nonce` ممکن است برای "استخراج" بهترین پیکربندی استفاده شود و به *Commitment* اجازه دهد که در سمت راست درخت قرار گیرد (بهینه‌سازی اثبات).


این اثبات اضافی ضروری است زیرا، برخلاف `Opret`، `Tapret` Commitment در ساختار یک اسکریپت Taproot یکپارچه شده است که نیاز به آشکارسازی بخشی از درخت Taproot دارد تا مکان *Commitment* به درستی اعتبارسنجی شود.


![RGB-Bitcoin](assets/fr/045.webp)


بنابراین **Anchors** تمام اطلاعات مورد نیاز برای اعتبارسنجی یک Bitcoin Commitment در زمینه RGB را در بر می‌گیرند. آن‌ها هم تراکنش مربوطه (`txid`) و هم اثبات موقعیت‌یابی Contract (`MPC Proof`) را نشان می‌دهند، در حالی که اثبات اضافی (`ETP`) را در صورت `Tapret` مدیریت می‌کنند. به این ترتیب، یک Anchor با اطمینان از اینکه همان تراکنش نمی‌تواند برای داده‌های قراردادی دیگر تفسیر مجدد شود، از یکپارچگی و یکتایی حالت off-chain محافظت می‌کند.


### نتیجه‌گیری


در این فصل، ما پوشش دادیم:




- چگونه مفهوم مهر و موم‌های یکبار مصرف را در Bitcoin اعمال کنیم (به‌ویژه از طریق یک _outpoint_);
- روش‌های مختلف برای درج قطعی یک _تعهد_ در یک تراکنش (تغییر امضا، تغییر کلید، تغییر شاهد، OP_RETURN، Taproot/Tapret)؛
- دلایلی که RGB بر تعهدات Tapret تمرکز دارد؛
- مدیریت Multi-Contract از طریق _تعهدات چند پروتکلی_، ضروری است اگر نمی‌خواهید یک ایالت کامل یا قراردادهای دیگر را زمانی که می‌خواهید یک نکته خاص را اثبات کنید، افشا کنید؛
- ما همچنین نقش _Anchors_ را دیده‌ایم که همه چیز را در یک بسته واحد گرد هم می‌آورد (تراکنش txid، اثبات Merkle Tree و اثبات Taproot).


در عمل، پیاده‌سازی فنی بین چندین Rust _crates_ اختصاصی (در _client_side_validation_، _commit-verify_، _bp_core_، و غیره) تقسیم شده است. مفاهیم بنیادی در آنجا هستند:


![RGB-Bitcoin](assets/fr/046.webp)


در فصل بعدی، به مؤلفه صرفاً off-chain از RGB، یعنی منطق Contract خواهیم پرداخت. خواهیم دید که چگونه قراردادهای RGB، که به عنوان _ماشین‌های حالت متناهی_ به‌طور جزئی تکرار شده سازماندهی شده‌اند، به بیان‌پذیری بسیار بالاتری نسبت به اسکریپت‌های Bitcoin دست می‌یابند، در حالی که محرمانگی داده‌های خود را حفظ می‌کنند.


## مقدمه‌ای بر قراردادهای هوشمند و وضعیت‌های آن‌ها


<chapterId>04a9569f-3563-5382-bf53-0c7069343ba0</chapterId>


:::video id=db4ee09f-1352-4ad1-9f7a-c962df7ea9fa:::


در این فصل و فصل بعدی، به مفهوم **Smart contract** در محیط RGB خواهیم پرداخت و روش‌های مختلفی که این قراردادها می‌توانند *وضعیت* خود را تعریف و تکامل دهند، بررسی خواهیم کرد. خواهیم دید که چرا معماری RGB، با استفاده از توالی مرتب مهرهای یک‌بار مصرف، امکان اجرای انواع مختلف ***عملیات Contract*** را به صورت مقیاس‌پذیر و بدون نیاز به رجیستری مرکزی فراهم می‌کند. همچنین به نقش اساسی ***Business Logic*** در چارچوب‌بندی تکامل Contract State خواهیم پرداخت.


### قراردادهای هوشمند و حقوق دیجیتال دارنده


هدف RGB ارائه زیرساختی برای پیاده‌سازی قراردادهای هوشمند بر روی Bitcoin است. منظور از "Smart contract" توافقی بین چندین طرف است که به صورت خودکار و محاسباتی اجرا می‌شود، بدون دخالت انسانی برای اجرای بندها. به عبارت دیگر، قانون Contract توسط نرم‌افزار اجرا می‌شود، نه توسط یک شخص ثالث مورد اعتماد.


این اتوماسیون سوال تمرکززدایی را مطرح می‌کند: چگونه می‌توانیم خود را از یک رجیستری متمرکز (مثلاً یک پلتفرم یا پایگاه داده مرکزی) برای مدیریت عملکرد Ownership و Contract آزاد کنیم؟ ایده اصلی، که توسط RGB مطرح شده است، بازگشت به حالتی از Ownership است که به عنوان "ابزارهای حامل" شناخته می‌شود. به طور تاریخی، برخی از اوراق بهادار (اوراق قرضه، سهام، و غیره) به صورت حامل صادر می‌شدند و به هر کسی که به صورت فیزیکی سند را در اختیار داشت، اجازه می‌دادند حقوق خود را اعمال کند.


![RGB-Bitcoin](assets/fr/055.webp)


RGB این مفهوم را به دنیای دیجیتال اعمال می‌کند: حقوق (و تعهدات) در داده‌هایی که توسط off-chain دستکاری می‌شوند، محصور شده‌اند و وضعیت این داده‌ها توسط خود شرکت‌کنندگان تأیید می‌شود. این امر، به طور پیش‌فرض، درجه بسیار بیشتری از محرمانگی و استقلال را نسبت به آنچه که توسط رویکردهای دیگر مبتنی بر ثبت‌های عمومی ارائه می‌شود، فراهم می‌کند.


### مقدمه‌ای بر وضعیت Smart contract RGB


یک Smart contract در RGB می‌تواند به عنوان یک ماشین حالت دیده شود، تعریف شده توسط:




- یک **وضعیت**، یعنی مجموعه‌ای از اطلاعات که پیکربندی فعلی Contract را منعکس می‌کند؛
- مجموعه قوانین **Business Logic** که شرایط و افرادی را که می‌توانند وضعیت را تغییر دهند، توصیف می‌کند.


![RGB-Bitcoin](assets/fr/056.webp)


مهم است که درک کنیم این قراردادها به انتقال ساده توکن‌ها محدود نمی‌شوند. آن‌ها می‌توانند طیف وسیعی از کاربردها را در بر گیرند: از دارایی‌های سنتی (توکن‌ها، سهام، اوراق قرضه) تا مکانیک‌های پیچیده‌تر (حقوق استفاده، شرایط تجاری، و غیره). برخلاف بلاکچین‌های دیگر، که در آن کد Contract برای همه قابل دسترسی و اجرا است، رویکرد RGB دسترسی و دانش Contract را به شرکت‌کنندگان ("***شرکت‌کنندگان Contract***") محدود می‌کند. چندین نقش وجود دارد:




- صادرکننده** یا خالق Contract، که Genesis از Contract و متغیرهای اولیه آن را تعریف می‌کند؛
- طرف‌های دارای حقوق** (*Ownership*) یا قابلیت‌های اجرایی دیگر؛
- ناظران**، که ممکن است به دیدن اطلاعات خاصی محدود باشند، اما نمی‌توانند تغییراتی را ایجاد کنند.


این جداسازی نقش‌ها به مقاومت در برابر سانسور کمک می‌کند، با اطمینان از اینکه تنها افراد مجاز می‌توانند با وضعیت قراردادی تعامل داشته باشند. همچنین به RGB توانایی مقیاس‌پذیری افقی را می‌دهد: اکثر اعتبارسنجی‌ها خارج از Blockchain انجام می‌شوند و تنها لنگرهای رمزنگاری (تعهدات) بر روی Bitcoin ثبت می‌شوند.


### وضعیت و Business Logic در RGB


از دیدگاه عملی، **Business Logic** دستگاه Contract به شکل قوانین و اسکریپت‌ها تعریف می‌شود، همان‌طور که در **Schema** توسط RGB نامیده می‌شود. Schema رمزگذاری می‌کند:




- ساختار دولت (کدام زمینه‌ها عمومی هستند؟ کدام زمینه‌ها متعلق به کدام احزاب هستند؟)
- شرایط اعتبار (چه مواردی باید قبل از تأیید به‌روزرسانی وضعیت بررسی شوند؟)؛
- مجوزها (چه کسی می‌تواند یک *State Transition* را آغاز کند؟ چه کسی فقط می‌تواند مشاهده کند؟).


به طور همزمان، **Contract State** اغلب به دو جزء تقسیم می‌شود:




- یک **Global State**: بخش عمومی، که به‌طور بالقوه توسط همه قابل مشاهده است (بسته به پیکربندی)؛
- ایالت‌های متعلق**: بخش‌های خصوصی، به‌طور خاص از طریق UTXOها که در منطق Contract ارجاع داده شده‌اند، به مالکان تخصیص داده شده‌اند.


همان‌طور که در فصل‌های بعدی خواهیم دید، هر به‌روزرسانی وضعیت (*Contract Operation*) باید به یک _تعهد_ Bitcoin متصل شود (از طریق `Opret` یا `Tapret`) و با اسکریپت‌های *Business Logic* مطابقت داشته باشد تا معتبر تلقی شود.


### عملیات Contract: ایجاد و تکامل دولت


در جهان RGB، ***Contract Operation*** هر رویدادی است که Contract را از **وضعیت قدیمی** به **وضعیت جدید** تغییر می‌دهد. این عملیات از منطق زیر پیروی می‌کنند:




- ما وضعیت فعلی Contract را یادداشت می‌کنیم؛
- ما قانون یا عملیات (یک ***State Transition***، یک ***Genesis*** اگر اولین حالت باشد، یا یک ***State Extension*** اگر یک *Valency* عمومی برای بازفعال‌سازی وجود داشته باشد) را اعمال می‌کنیم؛
- ما Anchor اصلاح را از طریق یک _تعهد_ جدید بر روی Blockchain انجام دادیم، یک _مهر یکبار مصرف_ را بستیم و دیگری را ایجاد کردیم؛
- دارندگان حقوق مربوطه به صورت محلی (*سمت مشتری*) تأیید می‌کنند که انتقال با *Schema* مطابقت دارد و اینکه تراکنش Bitcoin مرتبط به عنوان On-Chain ثبت شده است.


![RGB-Bitcoin](assets/fr/057.webp)


نتیجه نهایی یک Contract به‌روز شده است که اکنون دارای وضعیت متفاوتی است. این انتقال نیازی به نگرانی کل شبکه Bitcoin از جزئیات ندارد، زیرا تنها یک اثر انگشت رمزنگاری کوچک (تعهد) در Blockchain ثبت می‌شود. توالی مهرهای یک‌بار مصرف از هر گونه Double-spending یا استفاده دوگانه از وضعیت جلوگیری می‌کند.


### زنجیره عملیات: از Genesis به وضعیت نهایی


برای قرار دادن این در چشم‌انداز، یک RGB Smart contract با یک **Genesis**، اولین حالت، شروع می‌شود. پس از آن، عملیات‌های مختلف Contract یکی پس از دیگری دنبال می‌شوند و یک DAG (*Directed Acyclic Graph*) از عملیات‌ها را تشکیل می‌دهند:




- هر انتقال بر اساس یک حالت قبلی است (یا چندین حالت، در مورد انتقال‌های همگرا)؛
- ترتیب زمانی با گنجاندن هر انتقال در یک Bitcoin Anchor تضمین می‌شود، که به لطف اجماع توسط Proof-of-Work، دارای برچسب زمانی و غیرقابل تغییر است؛
- هنگامی که هیچ عملیات دیگری در حال انجام نیست، یک **وضعیت نهایی** به دست می‌آید: جدیدترین و کامل‌ترین وضعیت Contract.


![RGB-Bitcoin](assets/fr/012.webp)


این توپولوژی DAG (به جای یک زنجیره خطی ساده) نشان‌دهنده امکان تکامل بخش‌های مختلف Contract به صورت موازی است، تا زمانی که با یکدیگر تناقض نداشته باشند. سپس RGB با تأیید *سمت کلاینت* هر شرکت‌کننده درگیر، از بروز هرگونه ناسازگاری جلوگیری می‌کند.


### خلاصه


قراردادهای هوشمند در RGB مدلی از ابزارهای دیجیتال حامل معرفی می‌کنند که غیرمتمرکز هستند اما در Bitcoin برای زمان‌سنجی و تضمین ترتیب تراکنش‌ها لنگر انداخته‌اند. اجرای خودکار این قراردادها بر اساس:




- یک **Contract State**، نشان‌دهنده پیکربندی فعلی Contract (حقوق، ترازها، متغیرها و غیره)؛
- یک **Business Logic** (*Schema*), تعریف می‌کند که کدام انتقال‌ها مجاز هستند و چگونه باید اعتبارسنجی شوند؛
- عملیات Contract**، که این وضعیت را گام به گام به‌روزرسانی می‌کند، به لطف تعهداتی که در تراکنش‌های Bitcoin تثبیت شده‌اند.


در فصل بعد، به جزئیات بیشتری درباره نمایش ملموس این ***حالت‌ها*** و ***انتقال‌های حالت*** در سطح off-chain خواهیم پرداخت و اینکه چگونه با UTXOها و مهر و موم‌های یک‌بار مصرف تعبیه‌شده در Bitcoin مرتبط هستند. این فرصتی خواهد بود تا ببینیم چگونه مکانیک داخلی RGB، بر اساس Client-side Validation، موفق به حفظ انسجام قراردادهای هوشمند در عین حفظ محرمانگی داده‌ها می‌شود.


## عملیات RGB Contract


<chapterId>78c44e88-50c4-5ec4-befe-456c1a9f080b</chapterId>


:::video id=1caec34d-f214-425b-a1a4-0a40ae7d3e0e:::


در این فصل، ما به بررسی نحوه عملکرد عملیات در قراردادهای هوشمند و انتقال حالت‌ها، دوباره در چارچوب پروتکل RGB خواهیم پرداخت. هدف همچنین درک چگونگی همکاری چندین شرکت‌کننده برای انتقال Ownership از یک دارایی خواهد بود.


### انتقال‌های حالت و مکانیک آن‌ها


اصل کلی همچنان همان Client-side Validation است، جایی که داده‌های حالت توسط مالک نگهداری شده و توسط گیرنده اعتبارسنجی می‌شود. با این حال، ویژگی خاص در اینجا با RGB در این واقعیت نهفته است که Bob، به عنوان گیرنده، از Alice می‌خواهد تا اطلاعات خاصی را در داده‌های Contract بگنجاند تا از طریق یک مرجع مخفی به یکی از UTXOهای خود، کنترل واقعی بر دارایی دریافت‌شده داشته باشد.


برای نشان دادن فرآیند *State Transition* (که یکی از عملیات‌های اساسی ***Contract Operations*** در RGB است)، بیایید یک مثال گام به گام از انتقال دارایی بین Alice و Bob را بررسی کنیم:


**وضعیت اولیه:**


Alice دارای یک ***Stash RGB*** از داده‌های محلی معتبر شده (*سمت مشتری*) است. این Stash به یکی از UTXOهای او در Bitcoin اشاره دارد. این بدان معناست که یک _تعریف مهر_ در این داده به یک UTXO متعلق به Alice اشاره می‌کند. ایده این است که او بتواند حقوق دیجیتال خاصی که به یک دارایی مرتبط است (مثلاً توکن‌های RGB) را به Bob منتقل کند.


![RGB-Bitcoin](assets/fr/058.webp)


**Bob همچنین دارای UTXOها است:**


از سوی دیگر، Bob حداقل یک UTXO از خود دارد، بدون هیچ ارتباط مستقیمی با Alice. در صورتی که Bob هیچ UTXO نداشته باشد، همچنان می‌توان انتقال را با استفاده از *Witness Transaction* به او انجام داد: خروجی این تراکنش سپس شامل Commitment (_تعهد_) خواهد بود و به طور ضمنی Ownership از Contract جدید را با Bob مرتبط می‌کند.


![RGB-Bitcoin](assets/fr/059.webp)


**ساخت ملک جدید (*ایالت جدید*):**


Bob اطلاعات را به صورت یک ***Invoice*** به Alice ارسال می‌کند (در فصل‌های بعدی به جزئیات بیشتری درباره ساخت Invoice خواهیم پرداخت)، و از او می‌خواهد که یک حالت جدید ایجاد کند که با قوانین Contract مطابقت داشته باشد. این حالت شامل یک *Seal Definition* جدید خواهد بود که به یکی از UTXOهای Bob اشاره می‌کند. به این ترتیب، Bob مالکیت Ownership دارایی‌های تعریف‌شده در این حالت جدید را به دست می‌آورد، به عنوان مثال مقدار معینی از توکن‌های RGB.


![RGB-Bitcoin](assets/fr/060.webp)


**آماده‌سازی تراکنش نمونه:**


سپس Alice یک تراکنش Bitcoin ایجاد می‌کند که UTXO اشاره شده در Seal قبلی (همان که او را به عنوان دارنده مشروعیت بخشید) را خرج می‌کند. در خروجی این تراکنش، یک *Commitment* (از طریق `Opret` یا `Tapret`) به Anchor حالت جدید RGB وارد می‌شود. تعهدات `Opret` یا `Tapret` از یک *درخت MPC* مشتق می‌شوند (همان‌طور که در فصل‌های قبلی دیده شد)، که می‌تواند چندین انتقال از قراردادهای مختلف را تجمیع کند.


**انتقال *Consignment* به Bob:**


قبل از پخش تراکنش، Alice یک ***Consignment*** حاوی تمام داده‌های لازم *سمت مشتری* (یعنی *Stash* او) و اطلاعات وضعیت جدید به نفع Bob ارسال می‌کند. در این مرحله، Bob قوانین اجماع RGB را اعمال می‌کند:




- تمام داده‌های RGB موجود در *Consignment* را تأیید می‌کند، از جمله وضعیت جدیدی که به آن Ownership دارایی را اعطا می‌کند؛
- با تکیه بر *Anchors* موجود در *Consignment*، ترتیب زمانی تراکنش‌های شاهد (از Genesis تا جدیدترین انتقال) را بررسی کرده و تعهدات مربوطه در Blockchain را تأیید می‌کند.


**تکمیل انتقال:**


اگر Bob راضی باشد، ممکن است تأیید خود را بدهد (برای مثال، با امضای *Consignment*). سپس Alice می‌تواند تراکنش نمونه آماده شده را پخش کند. پس از تأیید، این کار Seal که قبلاً توسط Alice نگه داشته شده بود را می‌بندد و Ownership را توسط Bob رسمی می‌کند. امنیت ضد Double-spending سپس بر اساس همان مکانیزم در Bitcoin است: UTXO خرج می‌شود و ثابت می‌کند که Alice دیگر نمی‌تواند از آن استفاده مجدد کند.


![RGB-Bitcoin](assets/fr/061.webp)


ایالت جدید اکنون به Bob از UTXO اشاره می‌کند و Bob را به Ownership که قبلاً توسط Alice نگه داشته شده بود، می‌دهد. خروجی Bitcoin که داده‌های RGB در آن لنگر انداخته است، به اثبات غیرقابل انکار انتقال Ownership تبدیل می‌شود.


مثالی از یک DAG حداقلی (*Directed Acyclic Graph*) که شامل دو عملیات Contract (ابتدا یک **Genesis** سپس یک ***State Transition***) است، می‌تواند نشان دهد که چگونه حالت RGB (*سمت مشتری* Layer، به رنگ قرمز) به Bitcoin Blockchain (*Commitment* Layer، به رنگ نارنجی) متصل می‌شود.


![RGB-Bitcoin](assets/fr/062.webp)


نشان می‌دهد که یک Genesis یک Seal (*Seal Definition*) را تعریف می‌کند، سپس یک *State Transition* این Seal را می‌بندد تا یک مورد جدید در یک UTXO دیگر ایجاد کند.


در این زمینه، در اینجا چند یادآوری از اصطلاحات آمده است:




- یک ***Assignment*** ترکیب می‌کند موارد زیر را:
    - یک ***Seal Definition*** (که به یک UTXO اشاره می‌کند)؛
    - ایالت‌های مالکیت‌شده**، یعنی داده‌های مرتبط با Ownership (برای مثال، مقدار توکن‌های منتقل‌شده).
- یک **Global State** ویژگی‌های عمومی Contract را که برای همه قابل مشاهده است، گرد هم می‌آورد و انسجام جهانی تحولات را تضمین می‌کند.


**انتقال‌های حالت**، که در فصل قبلی توضیح داده شد، شکل اصلی Contract Operation هستند. آن‌ها به یک یا چند حالت قبلی (از Genesis یا یک State Transition دیگر) اشاره می‌کنند و آن‌ها را به یک حالت جدید به‌روزرسانی می‌کنند.


![RGB-Bitcoin](assets/fr/063.webp)


این نمودار نشان می‌دهد که چگونه در یک *State Transition Bundle*، چندین مهر و موم می‌توانند در یک تراکنش نمونه بسته شوند، در حالی که به طور همزمان مهر و موم‌های جدیدی باز می‌شوند. در واقع، یک ویژگی جالب از پروتکل RGB توانایی آن در مقیاس‌پذیری است: چندین انتقال می‌توانند در یک Transition Bundle تجمیع شوند، که هر تجمیع با یک برگ متمایز از *درخت MPC* (یک شناسه بسته منحصر به فرد) مرتبط است. به لطف مکانیزم *Deterministic Bitcoin Commitment* (DBC)، کل پیام در یک خروجی `Tapret` یا `Opret` وارد می‌شود، در حالی که مهر و موم‌های قبلی بسته می‌شوند و احتمالاً مهر و موم‌های جدیدی تعریف می‌شوند. `Anchor` به عنوان یک پیوند مستقیم بین Commitment ذخیره شده در Blockchain و ساختار Client-side Validation (*سمت مشتری*) عمل می‌کند.


در فصل‌های بعدی، به تمام اجزا و فرآیندهای درگیر در ساخت و اعتبارسنجی یک State Transition خواهیم پرداخت. بیشتر این Elements بخشی از اجماع RGB هستند که در **کتابخانه هسته RGB** پیاده‌سازی شده‌اند.


### Transition Bundle


در RGB، امکان بسته‌بندی انتقال‌های حالت مختلف که به همان Contract تعلق دارند (یعنی اشتراک‌گذاری همان **ContractId**، مشتق شده از **OpId** Genesis) وجود دارد. در ساده‌ترین حالت، همان‌طور که بین Alice و Bob در مثال بالا نشان داده شده است، یک **Transition Bundle** فقط شامل یک انتقال است. اما پشتیبانی از عملیات چندپرداختی (مانند کوین‌جوین‌ها، باز کردن کانال‌های Lightning و غیره) به این معناست که چندین کاربر می‌توانند انتقال‌های حالت خود را در یک بسته ترکیب کنند.


پس از جمع‌آوری، این انتقال‌ها (توسط مکانیزم MPC + DBC) در یک تراکنش Bitcoin واحد تثبیت می‌شوند:




- هر State Transition هش شده و به یک Transition Bundle گروه‌بندی می‌شود؛
- خود Transition Bundle هش شده و در برگ درخت MPC که به این Contract (یک BundleId) مربوط است، وارد می‌شود؛
- درخت MPC سرانجام از طریق `Opret` یا `Tapret` در Witness Transaction فعال می‌شود، که به این ترتیب مهرهای مصرف‌شده را می‌بندد و مهرهای جدید را تعریف می‌کند.


از نظر فنی، **BundleId** وارد شده در برگه MPC از یک Hash برچسب‌گذاری شده به دست می‌آید که به سریال‌سازی دقیق فیلد *InputMap* بسته اعمال شده است:


```txt
BundleId = SHA256( SHA256(bundle_tag) || SHA256(bundle_tag) || InputMap )
```


که در آن `bundle_tag = urn:lnp-bp:RGB:bundle#2024-02-03` برای مثال.


*InputMap* یک ساختار داده است که برای هر ورودی `i` از تراکنش نمونه، مرجع *OpId* مربوط به State Transition را فهرست می‌کند. برای مثال:


```txt
InputMap =
N               input_0    OpId(input_0)    input_1    OpId(input_1)   ...    input_N-1  OpId(input_N-1)
|____________________| |_________||______________| |_________||______________|       |__________||_______________|
16-bit Little Endian   32-bit LE   32-byte hash
|_________________________| |_________________________|  ...  |___________________________|

MapElement1                MapElement2                       MapElementN
```




- `N` تعداد کل ورودی‌ها در تراکنش است که به یک `OpId` اشاره دارند؛
- `opId(input_j)` شناسه عملیات یکی از انتقال‌های حالت موجود در بسته است.


با ارجاع به هر ورودی تنها یک‌بار و به‌صورت منظم، از خرج شدن دوباره همان Seal در دو انتقال حالت همزمان جلوگیری می‌کنیم.


### تولید حالت و حالت فعال


بنابراین می‌توان از انتقالات حالت برای انتقال Ownership یک دارایی از یک شخص به شخص دیگر استفاده کرد. با این حال، آنها تنها عملیات ممکن در پروتکل RGB نیستند. پروتکل سه **عملیات Contract** را تعریف می‌کند:




- State Transition**;
- Genesis**;
- State Extension**.


در میان این‌ها، **Genesis** و **State Extension** گاهی اوقات "*عملیات تولید حالت*" نامیده می‌شوند، زیرا آن‌ها حالت‌های جدیدی ایجاد می‌کنند بدون اینکه بلافاصله هیچ‌کدام را ببندند. این نکته بسیار مهم است: **Genesis** و **State Extension** شامل بستن یک Seal نمی‌شوند. بلکه، آن‌ها یک Seal جدید تعریف می‌کنند که سپس باید توسط یک **State Transition** بعدی خرج شود تا واقعاً در تاریخچه Blockchain معتبر شود.


![RGB-Bitcoin](assets/fr/064.webp)


حالت **فعال** یک Contract اغلب به عنوان مجموعه‌ای از جدیدترین حالت‌ها تعریف می‌شود که از تاریخچه (DAG) تراکنش‌ها ناشی می‌شود، با شروع از Genesis و دنبال کردن تمام انکرها در Bitcoin Blockchain. هر حالت قدیمی که دیگر منسوخ شده است (یعنی به UTXOهای خرج‌شده متصل است) دیگر فعال در نظر گرفته نمی‌شود، اما برای بررسی سازگاری تاریخچه همچنان ضروری است.


### Genesis


Genesis نقطه شروع هر RGB Contract است. این توسط صادرکننده Contract ایجاد شده و پارامترهای اولیه را مطابق با **Schema** تعریف می‌کند. در مورد یک RGB token، Genesis ممکن است به عنوان مثال مشخص کند:




- تعداد توکن‌های ایجاد شده در ابتدا و صاحبان آن‌ها؛
- سقف کل ممکن برای مسئله؛
- هرگونه قوانین صدور مجدد و اینکه کدام شرکت‌کنندگان واجد شرایط هستند.


به عنوان اولین تراکنش در Contract، Genesis به هیچ وضعیت قبلی اشاره نمی‌کند و هیچ Seal را نیز نمی‌بندد. با این حال، برای ظاهر شدن در تاریخچه و تأیید شدن، Genesis باید توسط اولین State Transition **مصرف** (بسته) شود (اغلب یک تراکنش اسکن/خودکار به خود صادرکننده یا توزیع اولیه به کاربران).


### State Extension


**توسعه‌های ایالتی** ویژگی منحصربه‌فردی برای قراردادهای هوشمند ارائه می‌دهند. آن‌ها امکان Redeem حقوق دیجیتال خاصی (*Valencies*) را که در تعریف Contract پیش‌بینی شده است، بدون بستن فوری Seal فراهم می‌کنند. اغلب، این موضوع مربوط به:




- توزیع مشکلات token؛
- مکانیسم‌های مبادله دارایی؛
- انتشار مجدد مشروط (که ممکن است شامل از بین بردن سایر دارایی‌ها و غیره باشد).


از نظر فنی، یک State Extension به *Redeem* (نوع خاصی از ورودی RGB) اشاره دارد که به یک *Valency* که قبلاً تعریف شده است (برای مثال، در Genesis یا یک State Transition دیگر) مربوط می‌شود. این یک Seal جدید را تعریف می‌کند که برای شخص یا شرایطی که از آن بهره‌مند می‌شود، در دسترس است. برای اینکه این Seal مؤثر شود، باید توسط یک State Transition بعدی مصرف شود.


![RGB-Bitcoin](assets/fr/065.webp)


به عنوان مثال: Genesis یک حق انتشار ایجاد می‌کند (*Valency*). این حق می‌تواند توسط یک عامل مجاز اعمال شود، که سپس یک State Extension می‌سازد:




- این به Valency (Redeem) اشاره دارد؛
- این یک *Assignment* جدید ایجاد می‌کند (داده‌های جدید *Owned State*) که به یک UTXO اشاره دارد؛
- یک State Transition آینده، که توسط مالک این UTXO جدید صادر شده است، در واقع توکن‌های جدید صادر شده را انتقال یا توزیع خواهد کرد.


### اجزای Contract Operation


اکنون می‌خواهم نگاهی دقیق به هر یک از اجزای Elements از یک **Contract Operation** در RGB بیندازم. یک Contract Operation عملی است که وضعیت یک Contract را تغییر می‌دهد و به صورت قطعی، توسط گیرنده قانونی، در سمت مشتری اعتبارسنجی می‌شود. به‌ویژه، خواهیم دید که چگونه Contract Operation از یک سو، **وضعیت قدیمی** Contract و از سوی دیگر، تعریف یک **وضعیت جدید** را در نظر می‌گیرد.


```txt
+---------------------------------------------------------------------------------------------------------------------+
|  Contract Operation                                                                                                 |
|                                                                                                                     |
|  +-----+     +-----------------------+      +--------------------------------+      +---------+     +------------+  |
|  | Ffv |     | ContractId | SchemaId |      | TransitionType | ExtensionType |      | Testnet |     | AltLayers1 |  |
|  +-----+     +-----------------------+      +--------------------------------+      +---------+     +------------+  |
|                                                                                                                     |
|  +-----------------------------------------------+  +------------------------------------------------------------+  |
|  | Metadata                                      |  | Global State                                               |  |
|  |                                               |  | +----------------------------------+                       |  |
|  | +-------------------------------------+       |  | | +-------------------+ +--------+ |                       |  |
|  | |          Structured Data            |       |  | | |  GlobalStateType  | |  Data  | |     ...     ...       |  |
|  | +-------------------------------------+       |  | | +-------------------+ +--------+ |                       |  |
|  |                                               |  | +----------------------------------+                       |  |
|  +-----------------------------------------------+  +------------------------------------------------------------+  |         +------+
|                                                                                                                     +---------> OpId |
|  +-----------------------------------------------+  +------------------------------------------------------------+  |         +------+
|  | Inputs                                        |  | Assignments                                                |  |
|  |                                               |  |                                                            |  |
|  | +-------------------------------------------+ |  | +--------------------------------------------------------+ |  |
|  | | Input #1                                  | |  | | Assignment #1                                          | |  |
+------+       |  | | +----------+ +----------------+ +-------+ | |  | | +----------------+ +-------------+ +-----------------+ | |  |       +--------------+
| OpId +--------------> PrevOpId | | AssignmentType | | Index | | |  | | | AssignmentType | | Owned State | | Seal Definition +--------------> Bitcoin UTXO |
+------+       |  | | +----------+ + ---------------+ +-------+ | |  | | +----------------+ +-------------+ +-----------------+ | |  |       +--------------+
|  | +-------------------------------------------+ |  | +--------------------------------------------------------+ |  |
|  |                                               |  |                                                            |  |
|  | +-------------------------------------------+ |  | +--------------------------------------------------------+ |  |
|  | | Input #2                                  | |  | | Assignment #2                                          | |  |
+------+       |  | | +----------+ +----------------+ +-------+ | |  | | +----------------+ +-------------+ +-----------------+ | |  |       +--------------+
| OpId +--------------> PrevOpId | | AssignmentType | | Index | | |  | | | AssignmentType | | Owned State | | Seal Definition +--------------> Bitcoin UTXO |
+------+       |  | | +----------+ +----------------+ +-------+ | |  | | +----------------+ +-------------+ +-----------------+ | |  |       +--------------+
|  | +-------------------------------------------+ |  | +--------------------------------------------------------+ |  |
|  |                                               |  |                                                            |  |
|  |       ...           ...          ...          |  |     ...          ...             ...                       |  |
|  |                                               |  |                                                            |  |
|  +-----------------------------------------------+  +------------------------------------------------------------+  |
|                                                                                                                     |
|  +-----------------------------------------------+  +------------------------------------------------------------+  |
|  | Redeems                                       |  | Valencies                                                  |  |
|  |                                               |  |                                                            |  |
|  | +------------------------------+              |  |                                                            |  |
+------+       |  | | +----------+ +-------------+ |              |  |  +-------------+  +-------------+                          |  |
| OpId +--------------> PrevOpId | | ValencyType | |  ...   ...   |  |  | ValencyType |  | ValencyType |         ...              |  |
+------+       |  | | +----------+ +-------------+ |              |  |  +-------------+  +-------------+                          |  |
|  | +------------------------------+              |  |                                                            |  |
|  |                                               |  |                                                            |  |
|  +-----------------------------------------------+  +------------------------------------------------------------+  |
|                                                                                                                     |

+---------------------------------------------------------------------------------------------------------------------+
```


اگر به نمودار بالا نگاه کنیم، می‌بینیم که یک Contract Operation شامل Elements است که به **وضعیت جدید** اشاره دارد و دیگران به **وضعیت قدیمی** به‌روزرسانی‌شده اشاره می‌کنند.


Elements های **ایالت جدید** عبارتند از:




- تکالیف**، که در آن تعریف شده‌اند:
 - **Seal Definition**؛
 - **Owned State**.
- **Global State**، که می‌تواند تغییر داده شود یا غنی‌سازی شود؛
- ظرفیت‌ها**، احتمالاً در State Transition یا Genesis تعریف شده‌اند.


**ایالت قدیمی** از طریق زیر ارجاع داده می‌شود:




- ورودی‌ها**، که به *تکالیف* انتقالات حالت قبلی اشاره می‌کنند (در Genesis موجود نیستند)؛
- بازخریدها**، که به ظرفیت‌های از پیش تعریف‌شده اشاره دارند (فقط در توسعه‌های ایالتی).


علاوه بر این، یک Contract Operation شامل فیلدهای عمومی‌تری است که به عملیات خاص مربوط می‌شود:




- `ffv` (*نسخه سریع به جلو*): عدد صحیح 2 بایتی که نشان‌دهنده نسخه Contract است؛
- `transitionType` یا `ExtensionType`: عدد صحیح 16 بیتی که نوع Transition یا Extension را مطابق با Business Logic مشخص می‌کند؛
- `ContractId`: عدد 32 بایتی که به *OpId* از Contract Genesis اشاره دارد. در انتقال‌ها و تمدیدها گنجانده شده است، اما در Genesis نیست؛
- schemaId: فقط در Genesis موجود است، این 32 بایت Hash است که ساختار (*Schema*) Contract را نشان می‌دهد؛
- `Testnet`: بولین نشان‌دهنده اینکه آیا شما در شبکه Testnet یا Mainnet هستید. فقط Genesis؛
- `altlayers1`: متغیری که جایگزین Layer (Sidechain یا دیگر) را برای داده‌های Anchor علاوه بر Bitcoin شناسایی می‌کند. فقط در Genesis موجود است؛
- `metadata`: فیلدی که می‌تواند اطلاعات موقتی را ذخیره کند، مفید برای اعتبارسنجی Contract پیچیده، اما نباید در تاریخچه وضعیت نهایی ثبت شود.


در نهایت، تمام این فیلدها توسط یک فرآیند هشینگ سفارشی فشرده می‌شوند تا یک اثر انگشت منحصر به فرد، یعنی `OpId` تولید شود. سپس این `OpId` در Transition Bundle ادغام می‌شود و امکان احراز هویت و اعتبارسنجی آن در پروتکل را فراهم می‌کند.


هر *Contract Operation* بنابراین توسط یک Hash به طول ۳۲ بایت به نام `OpId` شناسایی می‌شود. این Hash با استفاده از یک Hash SHA256 از تمام Elements هایی که عملیات را تشکیل می‌دهند، محاسبه می‌شود. به عبارت دیگر، هر *Contract Operation* دارای یک Commitment رمزنگاری خاص خود است که شامل تمام داده‌های لازم برای تأیید اصالت و سازگاری عملیات می‌باشد.


سپس یک RGB Contract با یک `ContractId` شناسایی می‌شود که از `OpId` Genesis مشتق شده است (زیرا هیچ عملیات قبل از Genesis وجود ندارد). به طور مشخص، ما `OpId` Genesis را می‌گیریم، ترتیب بایت‌ها را معکوس می‌کنیم و یک کدگذاری Base58 اعمال می‌کنیم. این کدگذاری باعث می‌شود که `ContractId` راحت‌تر مدیریت و شناسایی شود.

### روش‌ها و قوانین به‌روزرسانی وضعیت


**Contract State** مجموعه‌ای از اطلاعات را نشان می‌دهد که پروتکل RGB باید برای یک Contract معین پیگیری کند. این مجموعه شامل:




- یک Global State**: این بخش عمومی و جهانی Contract است که برای همه قابل مشاهده است؛
- یک یا چند حالت مالکیت‌شده**: هر Owned State با یک Seal منحصر به فرد (و بنابراین یک UTXO بر روی Bitcoin) مرتبط است. تمایزی بین موارد زیر قائل می‌شود:
    - **ایالت‌های** تحت مالکیت عمومی،
    - **ایالت‌های** خصوصی تحت مالکیت.


![RGB-Bitcoin](assets/fr/066.webp)


*Global State* به‌طور مستقیم به‌عنوان یک بلوک واحد در *Contract Operation* گنجانده شده است. *ایالت‌های متعلق* در هر *Assignment* همراه با *Seal Definition* تعریف شده‌اند.


یک ویژگی عمده RGB نحوه‌ای است که در آن Global State و ایالت‌های مالکیت‌شده تغییر می‌کنند. به طور کلی دو نوع رفتار وجود دارد:




- قابل تغییر**: وقتی یک عنصر حالت به عنوان قابل تغییر توصیف می‌شود، هر عملیات جدید حالت قبلی را با یک حالت جدید جایگزین می‌کند. سپس داده‌های قدیمی منسوخ تلقی می‌شوند؛
- انباشتن**: زمانی که یک عنصر حالت به عنوان انباشتن تعریف می‌شود، هر عملیات جدید اطلاعات جدیدی را به حالت قبلی اضافه می‌کند، بدون اینکه آن را بازنویسی کند. نتیجه یک نوع تاریخچه انباشته است.


اگر در Contract، یک عنصر حالت به‌عنوان قابل تغییر یا تجمعی تعریف نشده باشد، این عنصر برای عملیات‌های بعدی خالی باقی می‌ماند (به عبارت دیگر، نسخه‌های جدیدی برای این فیلد وجود ندارد). این Contract Schema (یعنی Business Logic کدگذاری‌شده) است که تعیین می‌کند آیا یک حالت (جهانی یا مالکیت‌شده) قابل تغییر، تجمعی یا ثابت است. هنگامی که Genesis تعریف شد، این ویژگی‌ها تنها در صورتی قابل تغییر هستند که خود Contract اجازه دهد، به‌عنوان مثال از طریق یک State Extension خاص.


جدول زیر نشان می‌دهد که چگونه هر نوع از Contract Operation می‌تواند Global State و Owned State را دستکاری کند (یا نکند):


|                              | Genesis | State Extension | State Transition |
| ---------------------------- |:-----: |:-------------: |:--------------: |
| **Addition of Global State** |    +    |        -        |        +         |
| **Mutation of Global State** |   n/a   |        -        |        +         |
| **Addition of Owned State**  |    +    |        -        |        +         |
| **Mutation of Owned State**  |   n/a   |       No        |        +         |
| **Addition of Valencies**    |    +    |        +        |        +         |


**`+`**: اقدام ممکن است اگر Contract اجازه دهد Schema.


**`-`**: عملیات باید توسط State Transition بعدی تأیید شود (State Extension به تنهایی Single-Use Seal را نمی‌بندد).


علاوه بر این، دامنه زمانی و حقوق به‌روزرسانی هر نوع داده را می‌توان در جدول زیر متمایز کرد:


|                        | Metadata                                | Global State                                     | Owned State                                                                                              |
| ---------------------- | --------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| **Scope**              | Defined for a single Contract Operation | Defined globally for the contract                | Defined for each seal (*Assignment*)                                                                     |
| **Who can update it?** | Non-updatable (ephemeral data)          | Operation issued by actors (issuer, etc.)        | Depends on the legitimate holder who owns the seal (the one who can spend it in a following transaction) |
| **Temporal Scope**     | Only for the current operation          | State is established at the end of the operation | State is defined before the operation (by the *Seal Definition* of the previous operation)               |


### Global State


Global State اغلباً به‌عنوان "هیچ‌کس مالک نیست، همه می‌دانند" توصیف می‌شود. این شامل اطلاعات عمومی درباره Contract است که به‌صورت عمومی قابل مشاهده است. به‌عنوان مثال، در یک token-صادرکننده Contract، ممکن است شامل:




- نماد (مخفف نمادین token): `ticker`;
- نام کامل token: `name`;
- دقت (تعداد ارقام اعشار): `precision`;
- پیشنهاد اولیه (و/یا حداکثر محدودیت token): `issuedSupply`;
- تاریخ صدور: `ایجاد شده`;
- داده‌های قانونی یا اطلاعات عمومی دیگر: `data`.


این Global State می‌تواند بر روی منابع عمومی (وب‌سایت‌ها، IPFS، Nostr، تورنت، و غیره) قرار گیرد و به جامعه توزیع شود. همچنین، انگیزه اقتصادی (نیاز به نگهداری و انتقال این توکن‌ها و غیره) به طور طبیعی کاربران Contract را ترغیب می‌کند تا این داده‌ها را خودشان نگهداری و منتشر کنند.


### تکالیف


*Assignment* ساختار پایه برای تعریف است:




- Seal (*Seal Definition*), که به یک UTXO خاص اشاره می‌کند؛
- *Owned State*، یعنی دارایی یا داده مرتبط با این Seal.


یک *Assignment* را می‌توان به عنوان معادل خروجی تراکنش Bitcoin در نظر گرفت، اما با انعطاف‌پذیری بیشتر. در اینجا منطق انتقال مالکیت نهفته است: *Assignment* یک نوع خاص از دارایی یا حق (`AssignmentType`) را با یک Seal مرتبط می‌کند. هر کسی که کلید خصوصی UTXO مرتبط با این Seal را در اختیار دارد (یا هر کسی که می‌تواند این UTXO را خرج کند) به عنوان مالک این *Owned State* در نظر گرفته می‌شود.


یکی از نقاط قوت بزرگ RGB در توانایی *آشکارسازی* یا پنهان کردن (*مخفی کردن*) فیلدهای *Seal Definition* و *Owned State* به دلخواه است. این ویژگی ترکیبی قدرتمند از محرمانگی و انتخاب‌پذیری ارائه می‌دهد. به عنوان مثال، می‌توانید ثابت کنید که یک انتقال معتبر است بدون اینکه تمام داده‌ها را فاش کنید، با ارائه نسخه آشکار شده به فردی که باید آن را تأیید کند، در حالی که اشخاص ثالث فقط نسخه پنهان (یک Hash) را می‌بینند. در عمل، `OpId` یک انتقال همیشه از داده‌های *مخفی شده* محاسبه می‌شود.


![RGB-Bitcoin](assets/fr/067.webp)


#### Seal Definition


*Seal Definition*، در شکل آشکار شده خود، دارای چهار فیلد اساسی است: `txptr`، `vout`، `blinding` و `method`:




- txptr**: این یک ارجاع به UTXO در Bitcoin است:
    - در مورد **Genesis Seal**، مستقیماً به یک UTXO موجود اشاره می‌کند (همان که با Genesis مرتبط است)؛
    - در مورد **گراف Seal**، می‌توانیم داشته باشیم:
        - یک `txid` ساده، اگر به یک UTXO خاص اشاره کند،
        - یا یک `WitnessTx`، که به یک مرجع خود اشاره می‌کند: Seal به خود تراکنش اشاره دارد. این به‌ویژه زمانی مفید است که هیچ UTXO خارجی در دسترس نباشد، مثلاً در تراکنش‌های باز کردن کانال Lightning، یا اگر گیرنده هیچ UTXO نداشته باشد.
- vout**: شماره خروجی تراکنش که توسط `txptr` مشخص شده است. فقط برای یک Graph Seal استاندارد موجود است (نه برای `WitnessTx`);
- کورکننده**: یک عدد تصادفی ۸ بایتی، برای تقویت محرمانگی و جلوگیری از تلاش‌های حمله جستجوی فراگیر بر روی هویت UTXO؛
- روش**: روش لنگر انداختن استفاده شده را نشان می‌دهد (`Tapret` یا `Opret`).


فرم *پنهان* Seal Definition یک SHA256 Hash (برچسب‌گذاری شده) از الحاق این 4 فیلد است، با یک برچسب خاص برای RGB.


![RGB-Bitcoin](assets/fr/068.webp)


#### ایالت‌های تحت مالکیت


مولفه دوم *Assignment*، Owned State است. برخلاف Global State، می‌تواند به صورت عمومی یا خصوصی وجود داشته باشد:




- عمومی Owned State**: همه داده‌های مرتبط با Seal را می‌دانند. به عنوان مثال، یک تصویر عمومی؛
- خصوصی Owned State**: داده‌ها مخفی هستند و فقط برای مالک (و در صورت لزوم برای اعتبارسنج) شناخته شده‌اند. به عنوان مثال، تعداد توکن‌های نگهداری شده.


RGB چهار نوع حالت ممکن (*StateTypes*) را برای یک Owned State تعریف می‌کند:




- اعلامی**: حاوی هیچ داده عددی نیست، فقط یک حق اعلامی (مثلاً حق رأی دادن). فرم‌های پنهان و آشکار یکسان هستند؛
- قابل تعویض**: نشان‌دهنده یک مقدار قابل تعویض (مانند توکن‌ها) است. در فرم آشکار، ما `amount` و `blinding` داریم. در فرم پنهان، ما یک *Pedersen commitment* داریم که مقدار و پنهان‌سازی را مخفی می‌کند؛
- ساختار‌یافته**: داده‌های ساختار‌یافته را ذخیره می‌کند (تا 64 کیلوبایت). در فرم آشکار، این داده به صورت یک بلوک است. در فرم پنهان، این یک Hash برچسب‌گذاری‌شده از این بلوک است:


```txt
SHA-256(SHA-256(tag_data) || SHA-256(tag_data) || blob)
```


با، برای مثال:


```txt
tag_data = urn:lnp-bp:rgb:state-data#2024-02-12
```




- پیوست‌ها**: یک فایل (صوتی، تصویری، باینری و غیره) را به Owned State پیوند می‌دهد، فایل Hash `file_hash`، نوع MIME `media type` و یک نمک رمزنگاری `salt` را ذخیره می‌کند. خود فایل در جای دیگری میزبانی می‌شود. به صورت مخفی، این یک Hash است که با سه مورد داده قبلی برچسب‌گذاری شده است:


```txt
SHA-256(SHA-256(tag_attachment) || SHA-256(tag_attachment) || file_hash || media_type || salt)
```


با، برای مثال:


```txt
tag_attachment = urn:rgb:state-attach#2024-02-12
```


به طور خلاصه، در اینجا ۴ نوع حالت ممکن در فرم عمومی و پنهان آورده شده است:


```txt
State                      Concealed form                              Revealed form

+---------------------------------------------------------------------------------------------------------

+--------------------------------------------------------------------------------+
|                                                                                |
Declarative        |                              < void >                                          |
|                                                                                |
+--------------------------------------------------------------------------------+

+---------------------------------------------------------------------------------------------------------

+--------------------------+             +---------------------------------------+
| +----------------------+ |             |         +--------+ +----------+       |
Fungible           | | Pedersen Commitement | | <========== |         | Amount | | Blinding |       |
| +----------------------+ |             |         +--------+ +----------+       |
+--------------------------+             +---------------------------------------+

+---------------------------------------------------------------------------------------------------------

+--------------------------+             +---------------------------------------+
| +----------------------+ |             |         +--------------------+        |
Structured         | |     Tagged Hash      | | <========== |         |     Data Blob      |        |
| +----------------------+ |             |         +--------------------+        |
+--------------------------+             +---------------------------------------+

+---------------------------------------------------------------------------------------------------------

+--------------------------+             +---------------------------------------+
| +----------------------+ |             | +-----------+ +------------+ +------+ |
Attachments        | |     Tagged Hash      | | <========== | | File Hash | | Media Type | | Salt | |
| +----------------------+ |             | +-----------+ +------------+ +------+ |

+--------------------------+             +---------------------------------------+

```



| **Element**     | **Declarative** | **Fungible**                      | **Structured**       | **Attachments**        |
| --------------- | --------------- | --------------------------------- | -------------------- | ---------------------- |
| **Data**        | None            | Signed or unsigned 64-bit integer | Any strict data type | Any file               |
| **Info Type**   | None            | Signed or unsigned                | Strict types         | MIME Type              |
| **Privacy**     | Not required    | Pedersen commitment               | Hash with blinding   | Hashed file identifier |
| **Size Limits** | N/A             | 256 bytes                         | Up to 64 KB          | Up to ~500 GB          |


### ورودی‌ها


ورودی‌های یک *Contract Operation* به *تکالیف*ی اشاره دارد که در این عملیات جدید صرف می‌شوند. یک ورودی نشان می‌دهد:




- `prevOpId`: شناسه (`OpId`) عملیات قبلی که *Assignment* در آن قرار داشت؛
- `assignmentType`: نوع *Assignment* (برای مثال، `assetOwner` برای یک token)؛
- `شاخص`: شاخص *Assignment* در فهرست مرتبط با `OpId` قبلی، که پس از مرتب‌سازی لغت‌نامه‌ای مهرهای مخفی تعیین شده است.


ورودی‌ها هرگز در Genesis ظاهر نمی‌شوند، زیرا هیچ تکلیف قبلی وجود ندارد. همچنین در توسعه‌های ایالتی ظاهر نمی‌شوند (زیرا توسعه‌های ایالتی مهرها را نمی‌بندند؛ بلکه مهرهای جدیدی بر اساس ظرفیت‌ها تعریف می‌کنند).


وقتی که ما دارای ایالت‌های نوع `Fungible` هستیم، منطق اعتبارسنجی (از طریق اسکریپت AluVM ارائه شده در Schema) سازگاری مجموع‌ها را بررسی می‌کند: مجموع توکن‌های ورودی (*Inputs*) باید برابر با مجموع توکن‌های خروجی (در *Assignments* جدید) باشد.


### فراداده


فیلد **Metadata** می‌تواند تا ۶۴ کیلوبایت باشد و برای شامل کردن داده‌های موقتی که برای اعتبارسنجی مفید هستند، اما در حالت دائمی Contract ادغام نمی‌شوند، استفاده می‌شود. به عنوان مثال، متغیرهای محاسباتی واسطه برای اسکریپت‌های پیچیده می‌توانند در اینجا ذخیره شوند. این فضا برای ذخیره‌سازی در تاریخچه جهانی در نظر گرفته نشده است، به همین دلیل خارج از محدوده Owned States یا Global State است.


### ظرفیت‌ها


**ظرفیت‌ها** یک مکانیزم پروتکل اصلی RGB هستند. آن‌ها را می‌توان در Genesis، انتقالات حالت یا گسترش‌های حالت یافت. آن‌ها نمایانگر حقوق عددی هستند که می‌توانند توسط یک State Extension (از طریق *بازخریدها*) فعال شوند و سپس توسط یک انتقال بعدی نهایی شوند. هر Valency با یک `ValencyType` (16 بیت) شناسایی می‌شود. معنای آن (حق انتشار مجدد، مبادله token، حق سوزاندن، و غیره) در Schema تعریف شده است.


به زبان ساده، می‌توانیم تصور کنیم که یک Genesis "حق صدور مجدد" Valency را تعریف می‌کند. یک State Extension آن را مصرف خواهد کرد (*Redeem*) اگر شرایط خاصی برآورده شود، به منظور معرفی مقدار جدیدی از توکن‌ها. سپس، یک State Transition که از دارنده Seal ایجاد شده است می‌تواند این توکن‌های جدید را انتقال دهد.


### بازخرید می‌کند


بازخریدها معادل Valency برای ورودی‌ها در تکالیف هستند. آن‌ها فقط در افزونه‌های ایالتی ظاهر می‌شوند، زیرا اینجا جایی است که یک Valency که قبلاً تعریف شده فعال می‌شود. یک Redeem شامل دو فیلد است:




- `PrevOpId`: `OpId` عملیات که در آن Valency مشخص شده است؛
- `ValencyType`: نوع Valency که می‌خواهید فعال کنید (هر `ValencyType` فقط یک بار توسط State Extension قابل استفاده است).


مثال: یک Redeem می‌تواند با اجرای CoinSwap مطابقت داشته باشد، بسته به آنچه که در Valency کدگذاری شده است.


### وضعیت ویژگی‌های RGB


اکنون قصد داریم نگاهی به چند ویژگی اساسی ایالت در RGB بیندازیم. به‌ویژه، به موارد زیر خواهیم پرداخت:




- سیستم نوع سختگیرانه، که یک سازماندهی دقیق و نوع‌بندی شده از داده‌ها را تحمیل می‌کند؛
- اهمیت جدا کردن **اعتبارسنجی** از **Ownership**؛
- سیستم **تکامل توافق** در RGB، که شامل مفاهیم *پیش‌برد سریع* و *عقب‌راندن* است.


همان‌طور که همیشه، به خاطر داشته باشید که همه چیز مربوط به وضعیت Contract در سمت مشتری بر اساس قوانین اجماعی که در پروتکل تعیین شده است، اعتبارسنجی می‌شود و مرجع نهایی رمزنگاری آن در تراکنش‌های Bitcoin لنگر انداخته است.


#### سیستم نوع سختگیرانه


RGB از یک *سیستم نوع سختگیرانه* و یک حالت سریال‌سازی قطعی (*کدگذاری سختگیرانه*) استفاده می‌کند. این سازماندهی به منظور تضمین بازتولید و دقت کامل در تعریف، مدیریت و اعتبارسنجی داده‌های Contract طراحی شده است.


در بسیاری از محیط‌های برنامه‌نویسی (JSON، YAML...)، ساختار داده می‌تواند انعطاف‌پذیر باشد، حتی بیش از حد مجاز. از سوی دیگر، در RGB، ساختار و نوع هر فیلد با محدودیت‌های صریح تعریف شده‌اند. برای مثال:




- هر متغیر دارای نوع خاصی است (برای مثال، یک عدد صحیح بدون علامت ۸ بیتی `u8`، یا یک عدد صحیح علامت‌دار ۱۶ بیتی، و غیره)؛
- نوع‌ها می‌توانند ترکیب شوند (نوع‌های تو در تو). این بدان معناست که می‌توانید نوعی را بر اساس نوع‌های دیگر تعریف کنید (مثلاً یک نوع تجمعی که شامل یک فیلد `u8`، یک فیلد `bool` و غیره است)؛
- مجموعه‌ها را نیز می‌توان مشخص کرد: لیست‌ها (*list*)، مجموعه‌ها (*set*) یا دیکشنری‌ها (*map*)، با ترتیب پیشرفت معین؛
- هر فیلد محدود است (*حد پایین* / *حد بالا*). ما همچنین محدودیت‌هایی بر تعداد Elements در مجموعه‌ها (شامل شدن) اعمال می‌کنیم؛
- داده‌ها به صورت بایت‌تراز شده هستند و سریال‌سازی به طور دقیق تعریف شده و بدون ابهام است.


با تشکر از این پروتکل رمزگذاری سختگیرانه:




- ترتیب فیلدها همیشه یکسان است، بدون توجه به پیاده‌سازی یا زبان برنامه‌نویسی استفاده‌شده؛
- هش‌های محاسبه‌شده بر روی همان مجموعه داده‌ها بنابراین قابل بازتولید و یکسان هستند (به‌طور دقیق *تعهدات* تعیین‌کننده)؛
- مرزها از رشد کنترل‌نشده اندازه داده‌ها جلوگیری می‌کنند (مثلاً تعداد بیش از حد فیلدها)؛
- این شکل از رمزگذاری، تأیید رمزنگاری را تسهیل می‌کند، زیرا هر شرکت‌کننده دقیقاً می‌داند چگونه داده‌ها را سریال‌سازی و Hash کند.


در عمل، ساختار (*Schema*) و کد حاصل (*Interface* و منطق مرتبط) کامپایل می‌شوند. یک زبان توصیفی برای تعریف Contract (انواع، فیلدها، قوانین) و generate یک فرمت باینری سخت‌گیرانه استفاده می‌شود. هنگامی که کامپایل می‌شود، نتیجه به صورت زیر است:




- یک *چیدمان حافظه* برای هر فیلد؛
- شناسه‌های معنایی (که نشان می‌دهند آیا تغییر نام یک متغیر بر منطق تأثیر می‌گذارد، حتی اگر ساختار حافظه ثابت بماند).


سیستم نوع سخت‌گیر همچنین امکان نظارت دقیق بر تغییرات را فراهم می‌کند: هرگونه تغییر در ساختار (حتی تغییر نام فیلد) قابل تشخیص است و می‌تواند منجر به تغییر در اثر کلی شود.


در نهایت، هر کامپایل یک اثر انگشت تولید می‌کند، یک شناسه رمزنگاری که نسخه دقیق کد (داده‌ها، قوانین، اعتبارسنجی) را تأیید می‌کند. به عنوان مثال، یک شناسه به شکل:


```txt
BEiLYE-am9WhTW1-oK8cpvw4-FEMtzMrf-mKocuGZn-qWK6YF#ginger-parking-nirvana
```


این امکان را فراهم می‌کند تا به‌روزرسانی‌های اجماع یا پیاده‌سازی را مدیریت کنید، در حالی که ردیابی دقیق نسخه‌های استفاده‌شده در شبکه را تضمین می‌کند.


برای جلوگیری از پیچیده شدن وضعیت یک RGB Contract به‌گونه‌ای که اعتبارسنجی آن در سمت مشتری دشوار شود، یک قانون اجماع حداکثر اندازه `2^16` بایت (64 کیلوبایت) را برای هر داده‌ای که در محاسبات اعتبارسنجی دخیل است، اعمال می‌کند. این قانون برای هر متغیر یا ساختار اعمال می‌شود: نه بیشتر از 65536 بایت، یا معادل آن در اعداد (32768 عدد صحیح 16 بیتی، و غیره). این قانون همچنین برای مجموعه‌ها (لیست‌ها، مجموعه‌ها، نقشه‌ها) اعمال می‌شود که نباید از `2^16` Elements تجاوز کنند.


این محدودیت تضمین می‌کند:




- حداکثر اندازه داده‌ای که در طول یک State Transition باید دستکاری شود را کنترل می‌کند؛
- سازگاری با ماشین مجازی (*AluVM*) که برای اجرای اسکریپت‌های اعتبارسنجی استفاده می‌شود.


#### اعتبارسنجی != Ownership پارادایم


یکی از نوآوری‌های عمده RGB جداسازی دقیق بین دو مفهوم است:




- اعتبارسنجی**: بررسی اینکه آیا یک State Transition قوانین Contract (Business Logic، تاریخچه، و غیره) را رعایت می‌کند؛
- **Ownership** (Ownership، یا کنترل): واقعیت داشتن Bitcoin UTXO که اجازه می‌دهد Single-Use Seal خرج شود (یا بسته شود)، و بنابراین State Transition اتفاق بیفتد.


**اعتبارسنجی** در سطح پشته نرم‌افزاری RGB (کتابخانه‌ها، پروتکل *تعهدات* و غیره) انجام می‌شود. نقش آن اطمینان از رعایت قوانین داخلی Contract (مقادیر، مجوزها و غیره) است. ناظران یا سایر شرکت‌کنندگان نیز می‌توانند تاریخچه داده‌ها را اعتبارسنجی کنند.


**Ownership**، از سوی دیگر، به طور کامل به امنیت Bitcoin متکی است. داشتن کلید خصوصی یک UTXO به معنای کنترل توانایی راه‌اندازی یک انتقال جدید (بستن Single-Use Seal) است. بنابراین، حتی اگر کسی بتواند داده‌ها را ببیند یا تأیید کند، نمی‌تواند وضعیت را تغییر دهد اگر مالک UTXO مربوطه نباشد.


![RGB-Bitcoin](assets/fr/069.webp)


این رویکرد محدودیت‌هایی برای آسیب‌پذیری‌های کلاسیک که در بلاکچین‌های پیچیده‌تر مشاهده می‌شود، ایجاد می‌کند (جایی که تمام کد یک Smart contract عمومی و قابل تغییر توسط هر کسی است، که گاهی منجر به هک شده است). در RGB، یک مهاجم نمی‌تواند به سادگی با وضعیت On-Chain تعامل کند، زیرا حق اقدام بر روی وضعیت (*Ownership*) توسط Bitcoin Layer محافظت می‌شود.


علاوه بر این، این جداسازی به RGB اجازه می‌دهد تا به طور طبیعی با Lightning Network یکپارچه شود. کانال‌های Lightning می‌توانند برای درگیر کردن و جابجایی دارایی‌های RGB بدون درگیر کردن *تعهدات* On-Chain در هر بار استفاده شوند. ما در فصل‌های بعدی دوره به بررسی دقیق‌تر این یکپارچگی RGB بر روی Lightning خواهیم پرداخت.


#### تحولات اجماعی در RGB


علاوه بر نسخه‌بندی کد معنایی، RGB شامل سیستمی برای تکامل یا به‌روزرسانی قوانین اجماع Contract در طول زمان است. دو شکل اصلی تکامل وجود دارد:




- جلو بردن**
- عقب‌نشینی** (به فرانسوی)


هنگامی که یک قانون قبلاً نامعتبر معتبر می‌شود، یک پیش‌روی سریع رخ می‌دهد. به عنوان مثال، اگر Contract تکامل یابد تا نوع جدیدی از `AssignmentType` یا یک فیلد جدید را مجاز کند:




- این را نمی‌توان با یک هاردفورک کلاسیک Blockchain مقایسه کرد، زیرا RGB در Client-side Validation کار می‌کند و بر سازگاری کلی Blockchain تأثیری نمی‌گذارد؛
- از نظر عملی، این نوع تغییر با فیلد `Ffv` (*نسخه سریع به جلو*) در Contract Operation نشان داده می‌شود؛
- دارندگان فعلی آسیب نمی‌بینند: وضعیت آن‌ها معتبر باقی می‌ماند؛
- ذینفعان جدید (یا کاربران جدید)، از سوی دیگر، نیاز دارند نرم‌افزار خود (Wallet آن‌ها) را به‌روزرسانی کنند تا قوانین جدید را شناسایی کنند.


یک عقب‌نشینی به این معناست که یک قانون که قبلاً معتبر بوده، نامعتبر می‌شود. بنابراین، این یک "سخت‌گیری" قوانین است، اما به‌طور دقیق نمی‌توان آن را یک سافت‌فورک نامید:




- دارندگان فعلی ممکن است تحت تأثیر قرار گیرند (آن‌ها ممکن است با دارایی‌هایی مواجه شوند که در نسخه جدید منسوخ یا نامعتبر شده‌اند)؛
- ما می‌توانیم در نظر بگیریم که در واقع در حال ایجاد یک پروتکل جدید هستیم: هر کسی که قانون جدید را بپذیرد از قانون قدیمی فاصله می‌گیرد؛
- ناشر ممکن است تصمیم بگیرد دارایی‌ها را در این پروتکل جدید دوباره صادر کند و کاربران را مجبور کند که دو کیف پول جداگانه (یکی برای پروتکل قدیمی و دیگری برای جدید) نگه دارند، اگر بخواهند هر دو نسخه را مدیریت کنند.


در این فصل درباره عملیات RGB Contract، اصول بنیادی این پروتکل را بررسی کرده‌ایم. همان‌طور که متوجه شده‌اید، پیچیدگی ذاتی پروتکل RGB نیازمند استفاده از بسیاری از اصطلاحات فنی است. بنابراین، در فصل بعدی، یک واژه‌نامه ارائه خواهم داد که تمام مفاهیم پوشش داده شده در این بخش نظری اول را خلاصه می‌کند، با تعاریف تمام اصطلاحات فنی مربوط به RGB. سپس، در بخش بعدی، به‌صورت عملی به تعریف و اجرای قراردادهای RGB خواهیم پرداخت.


## RGB واژه‌نامه


<chapterId>545e16a4-3cca-44a3-9fd5-dbc5868abf97</chapterId>


اگر نیاز دارید به این واژه‌نامه کوتاه از اصطلاحات فنی مهمی که در دنیای RGB استفاده می‌شود (به ترتیب حروف الفبا فهرست شده‌اند) بازگردید، آن را مفید خواهید یافت. این فصل ضروری نیست اگر قبلاً همه چیزهایی که در بخش اول پوشش داده‌ایم را درک کرده‌اید.


#### AluVM


مخفف AluVM به معنای "_واحد منطق الگوریتمی ماشین مجازی_" است، یک ماشین مجازی مبتنی بر ثبات که برای اعتبارسنجی Smart contract و محاسبات توزیع‌شده طراحی شده است. این ماشین برای اعتبارسنجی قراردادهای RGB استفاده می‌شود (اما به‌طور انحصاری رزرو نشده است). اسکریپت‌ها یا عملیات‌های موجود در یک RGB Contract می‌توانند در محیط AluVM اجرا شوند.


برای اطلاعات بیشتر: [وب‌سایت رسمی AluVM](https://www.AluVM.org/)


#### Anchor


یک Anchor مجموعه‌ای از داده‌های سمت مشتری است که برای اثبات گنجاندن یک _تعهد_ منحصر به فرد در یک تراکنش استفاده می‌شود. در پروتکل RGB، یک Anchor شامل Elements‌های زیر است:




- شناسه تراکنش Bitcoin (txid) از **Witness Transaction**؛
- **Multi Protocol Commitment (MPC)**؛
- **Deterministic Bitcoin Commitment (DBC)**؛
- **اثبات تراکنش اضافی (ETP)** در صورت استفاده از مکانیزم **Tapret** Commitment (به بخش اختصاص داده شده به این مدل مراجعه کنید).


بنابراین Anchor به منظور ایجاد یک پیوند قابل تأیید بین یک تراکنش خاص Bitcoin و داده‌های خصوصی که توسط پروتکل RGB تأیید شده‌اند، عمل می‌کند. این تضمین می‌کند که این داده‌ها واقعاً در Blockchain گنجانده شده‌اند، بدون اینکه محتوای دقیق آن‌ها به‌طور عمومی فاش شود.


#### Assignment


در منطق RGB، یک Assignment معادل یک خروجی تراکنش است که ویژگی‌های خاصی را در وضعیت یک Contract تغییر، به‌روزرسانی یا ایجاد می‌کند. یک Assignment شامل دو Elements است:




- یک **Seal Definition** (ارجاع به یک UTXO خاص)؛
- یک **Owned State** (داده‌هایی که وضعیت مرتبط با این مالک جدید را توصیف می‌کنند).


بنابراین، Assignment نشان می‌دهد که بخشی از ایالت (برای مثال، یک دارایی) اکنون به یک دارنده خاص تخصیص داده شده است که از طریق یک Single-Use Seal مرتبط با یک UTXO شناسایی می‌شود.


#### Business Logic


Business Logic تمام قوانین و عملیات داخلی یک Contract را که توسط **Schema** آن توصیف شده است (یعنی ساختار خود Contract) گرد هم می‌آورد. این تعیین می‌کند که حالت Contract چگونه می‌تواند تکامل یابد و تحت چه شرایطی.


#### GW-۱۰۸۴


Client-side Validation به فرآیندی اشاره دارد که در آن هر طرف (مشتری) مجموعه‌ای از داده‌ها را که به‌صورت خصوصی مبادله شده‌اند، طبق قوانین یک پروتکل تأیید می‌کند. در مورد RGB، این داده‌های مبادله شده در چیزی که به عنوان **محموله‌ها** شناخته می‌شود، گروه‌بندی می‌شوند. برخلاف پروتکل Bitcoin که نیاز دارد تمام تراکنش‌ها در On-Chain منتشر شوند، RGB تنها اجازه می‌دهد _تعهدات_ (که در Bitcoin لنگر انداخته‌اند) به‌صورت عمومی ذخیره شوند، در حالی که اطلاعات اساسی Contract (انتقالات، تصدیق‌ها، اثبات‌ها) به صورت off-chain باقی می‌مانند و تنها بین کاربران مربوطه به اشتراک گذاشته می‌شوند.


#### Commitment


Commitment (به معنای رمزنگاری) یک شیء ریاضی است که با `C` نشان داده می‌شود و به صورت قطعی از یک عملیات بر روی داده‌های ساختاریافته `m` (پیام) و یک مقدار تصادفی `r` به دست می‌آید. ما می‌نویسیم:


$$
C = \text{commit}(m, r)
$$


این مکانیزم شامل دو عملیات اصلی است:




- Commit**: یک تابع رمزنگاری به یک پیام `m` و یک عدد تصادفی `r` اعمال می‌شود تا `C` تولید شود؛
- تأیید**: ما از `C`، پیام `m` و مقدار `r` برای بررسی صحت این Commitment استفاده می‌کنیم. این تابع `True` یا `False` را برمی‌گرداند.


یک Commitment باید دو ویژگی را رعایت کند:




- اتصال**: باید غیرممکن باشد که دو پیام متفاوت تولید کننده همان `C` باشند:


$$
m': \, | \,: m' \neq m \quad \text{and} \quad r': \, | \,: r' \neq r \quad
$$


مانند:


$$
\text{verify}(m, r, C) = \text{verify}(m', r', C) \rightarrow \text{True}
$$




- پنهان‌سازی**: دانش `C` نباید محتوای `m` را فاش کند.


در پروتکل RGB، یک Commitment در یک تراکنش Bitcoin گنجانده می‌شود تا وجود یک قطعه اطلاعات خاص را در یک زمان معین اثبات کند، بدون اینکه خود اطلاعات را فاش کند.


#### Consignment


یک **Consignment** داده‌های مبادله شده بین طرفین را که تحت Client-side Validation در RGB قرار دارند، گروه‌بندی می‌کند. دو دسته اصلی از Consignment وجود دارد:




- Contract Consignment**: توسط *صادرکننده* ارائه شده است (صادرکننده Contract)، و شامل اطلاعات اولیه مانند Schema، Genesis، Interface و Interface Implementation می‌باشد.
- انتقال Consignment**: ارائه شده توسط طرف پرداخت کننده (*پرداخت کننده*). این شامل کل تاریخچه انتقالات حالت تا Terminal Consignment (یعنی حالت نهایی دریافت شده توسط پرداخت کننده) است.


این محموله‌ها به صورت عمومی در Blockchain ثبت نمی‌شوند؛ آن‌ها به طور مستقیم بین طرف‌های مربوطه از طریق کانال ارتباطی مورد نظرشان مبادله می‌شوند.


#### Contract


یک Contract مجموعه‌ای از حقوق است که به صورت دیجیتالی بین چندین بازیگر از طریق پروتکل RGB اجرا می‌شود. این مجموعه دارای یک حالت فعال و یک Business Logic است که توسط یک Schema تعریف می‌شود و مشخص می‌کند که کدام عملیات مجاز هستند (انتقالات، تمدیدها، و غیره). حالت یک Contract و همچنین قوانین اعتبارسنجی آن در Schema بیان می‌شود. در هر زمان معین، Contract تنها مطابق با آنچه توسط این Schema و اسکریپت‌های اعتبارسنجی (که به عنوان مثال در AluVM اجرا می‌شوند) مجاز است، تکامل می‌یابد.


#### Contract Operation


به‌روزرسانی وضعیت Contract Operation یک به‌روزرسانی وضعیت Contract است که طبق قوانین Schema انجام می‌شود. عملیات‌های زیر در RGB وجود دارند:




- State Transition**;
- Genesis**;
- State Extension**.


هر عملیات با افزودن یا جایگزینی داده‌های خاص، وضعیت را تغییر می‌دهد (Global State, Owned State...).


#### Contract Participant


Contract Participant یک بازیگر است که در عملیات مربوط به Contract شرکت می‌کند. در RGB، تمایزی قائل می‌شود بین:




- صادرکننده Contract، که Genesis را ایجاد می‌کند (منشأ Contract)؛
- طرفین Contract، یعنی دارندگان حقوق مربوط به وضعیت Contract؛
- احزاب عمومی، که می‌توانند توسعه‌های دولتی را بسازند اگر Contract ظرفیت‌هایی قابل دسترسی برای عموم ارائه دهد.


#### Contract Rights


Contract Rights به حقوق مختلفی اشاره دارد که توسط افرادی که در یک RGB Contract درگیر هستند، می‌توانند اعمال شوند. این حقوق به چندین دسته تقسیم می‌شوند:




- Ownership حقوق**، مرتبط با Ownership یک UTXO خاص (از طریق یک _تعریف مهر_);
- حقوق اجرایی**، یعنی توانایی ساخت یک یا چند انتقال (انتقالات حالت) مطابق با Schema؛
- حقوق عمومی**، زمانی که Schema استفاده‌های عمومی خاصی را مجاز می‌داند، به عنوان مثال ایجاد یک State Extension از طریق بازخرید یک Valency.


#### Contract State


Contract State با وضعیت فعلی یک Contract در یک لحظه خاص مطابقت دارد. این می‌تواند شامل داده‌های عمومی و خصوصی باشد که وضعیت Contract را منعکس می‌کند. RGB تفاوت قائل می‌شود بین:




- **Global State**، که شامل ویژگی‌های عمومی Contract می‌شود (راه‌اندازی شده در Genesis یا اضافه شده از طریق به‌روزرسانی‌های مجاز)؛
- ایالت‌های متعلق**، که به صاحبان خاصی تعلق دارند و با UTXOهایشان شناسایی می‌شوند.


#### Deterministic Bitcoin Commitment - DBC


Deterministic Bitcoin Commitment (DBC) مجموعه قوانینی است که برای ثبت اثبات‌پذیر و منحصربه‌فرد یک _تعهد_ در یک تراکنش Bitcoin استفاده می‌شود. در پروتکل RGB، دو شکل اصلی از DBC وجود دارد:




- Opret**
- تاپرت**


این مکانیزم‌ها به‌طور دقیق تعریف می‌کنند که چگونه _تعهد_ در خروجی یا ساختار یک تراکنش Bitcoin رمزگذاری می‌شود، تا اطمینان حاصل شود که این Commitment به‌صورت قطعی قابل ردیابی و تأیید است.


#### Directed Acyclic Graph - DAG


یک DAG (یا *گراف هدایت‌شده بدون دور*) یک گراف بدون چرخه است که امکان زمان‌بندی توپولوژیکی را فراهم می‌کند. بلاکچین‌ها، مانند _شاردها_ی قراردادهای RGB، می‌توانند توسط DAGها نمایش داده شوند.


برای اطلاعات بیشتر: [Directed Acyclic Graph](https://en.wikipedia.org/wiki/Directed_acyclic_graph)


#### حکاکی


حکاکی یک رشته داده اختیاری است که صاحبان متوالی Contract می‌توانند در تاریخچه Contract وارد کنند. این ویژگی، به عنوان مثال، در **RGB21** Interface وجود دارد و امکان افزودن اطلاعات یادبود یا توصیفی به تاریخچه Contract را فراهم می‌کند.


#### اثبات تراکنش اضافی - ETP


ETP (*Extra Transaction Proof*) بخشی از Anchor است که شامل داده‌های اضافی مورد نیاز برای اعتبارسنجی **Tapret** *Commitment* (در زمینه _taproot_) می‌باشد. این شامل، در میان سایر موارد، کلید عمومی داخلی اسکریپت Taproot (_internal PubKey_) و اطلاعات خاص مربوط به _Script Path Spend_ است.


#### Genesis


Genesis به مجموعه داده‌ای اشاره دارد که تحت نظارت Schema، حالت اولیه هر Contract در RGB را تشکیل می‌دهد. این می‌تواند با مفهوم _بلوک پیدایش_ در Bitcoin یا مفهوم Coinbase Transaction مقایسه شود، اما در اینجا در سطح _سمت مشتری_ و RGB token است.


#### Global State


Global State مجموعه‌ای از ویژگی‌های عمومی است که در Contract State قرار دارد. این مجموعه در Genesis تعریف شده و بسته به قوانین Contract، می‌تواند توسط انتقالات مجاز به‌روزرسانی شود. برخلاف حالت‌های مالکیت‌شده، Global State به یک نهاد خاص تعلق ندارد؛ بلکه بیشتر شبیه به یک ثبت عمومی درون Contract است.


#### Interface


Interface مجموعه‌ای از دستورالعمل‌ها است که برای رمزگشایی داده‌های باینری که در عملیات Schema یا Contract و حالت‌های آن‌ها گردآوری شده‌اند، استفاده می‌شود تا آن‌ها را برای کاربر یا Wallet او قابل خواندن کند. این به عنوان یک Layer تفسیر عمل می‌کند.


#### Interface Implementation


Interface Implementation مجموعه‌ای از اعلامیه‌ها است که یک **Interface** را به یک **Schema** متصل می‌کند. این امکان را فراهم می‌کند که ترجمه معنایی توسط خود Interface انجام شود، به طوری که داده‌های خام یک Contract توسط کاربر یا نرم‌افزار مربوطه (کیف پول‌ها) قابل درک باشد.


#### Invoice


یک Invoice به شکل یک URL که به صورت [base58](https://en.wikipedia.org/wiki/Binary-to-text_encoding#Base58) کدگذاری شده است، ظاهر می‌شود و داده‌های لازم برای ساخت **State Transition** (توسط پرداخت‌کننده) را در خود جای می‌دهد. به عبارت دیگر، این یک Invoice است که به طرف مقابل (*پرداخت‌کننده*) امکان می‌دهد تا انتقال مربوطه را برای انتقال دارایی یا به‌روزرسانی وضعیت Contract ایجاد کند.


#### Lightning Network


Lightning Network یک شبکه غیرمتمرکز از کانال‌های پرداخت (یا _کانال‌های حالت_) بر روی Bitcoin است که از کیف‌پول‌های چندامضایی 2/2 تشکیل شده است. این شبکه امکان انجام تراکنش‌های سریع و کم‌هزینه _خارج از زنجیره_ را فراهم می‌کند، در حالی که در صورت لزوم برای داوری (یا بستن) به Layer 1 در Bitcoin متکی است.


برای اطلاعات بیشتر در مورد نحوه کار Lightning، توصیه می‌کنم این دوره دیگر را بگذرانید:


https://planb.network/courses/34bd43ef-6683-4a5c-b239-7cb1e40a4aeb

#### Multi Protocol Commitment - MPC


Multi Protocol Commitment (MPC) به ساختار Merkle Tree اشاره دارد که در RGB برای شامل کردن چندین **بسته انتقال** از قراردادهای مختلف در یک تراکنش Bitcoin استفاده می‌شود. ایده این است که چندین تعهد (که ممکن است به قراردادها یا دارایی‌های مختلف مربوط باشند) را در یک نقطه Anchor گرد هم آوریم تا اشغال فضای بلاک را بهینه کنیم.


#### Owned State


یک Owned State بخشی از یک Contract State است که در یک Assignment محصور شده و با یک نگهدارنده خاص (از طریق یک Single-Use Seal که به یک UTXO اشاره می‌کند) مرتبط است. این به عنوان مثال، یک دارایی دیجیتال یا یک حق قراردادی خاص اختصاص داده شده به آن شخص را نشان می‌دهد.


#### Ownership


Ownership به توانایی کنترل و خرج کردن یک UTXO که توسط یک Seal Definition ارجاع شده است، اشاره دارد. هنگامی که یک Owned State به یک UTXO متصل می‌شود، صاحب این UTXO حق دارد، به طور بالقوه، وضعیت مرتبط را طبق قوانین Contract انتقال دهد یا توسعه دهد.


#### Partially Signed Bitcoin Transaction - PSBT


یک PSBT (_تراکنش Bitcoin به‌صورت جزئی امضا شده_) یک تراکنش Bitcoin است که هنوز به‌طور کامل امضا نشده است. این تراکنش می‌تواند بین چندین نهاد به اشتراک گذاشته شود، که هر کدام می‌توانند برخی از Elements (امضاها، اسکریپت‌ها...) را اضافه یا تأیید کنند، تا زمانی که تراکنش برای توزیع On-Chain آماده تلقی شود.


برای اطلاعات بیشتر: [BIP-0174](https://github.com/Bitcoin/bips/blob/master/bip-0174.mediawiki)


#### Pedersen commitment


یک Pedersen commitment نوعی از Commitment رمزنگاری است که خاصیت **همومورفیک** نسبت به عمل جمع دارد. این بدان معناست که می‌توان مجموع دو تعهد را بدون افشای مقادیر فردی تأیید کرد.


رسمی، اگر:


$$
C1=\text{commit}(m1,r1) \quad C2=\text{commit}(m2,r2)
$$


سپس:


$$
C3=C1⋅C2=\text{commit}(m1+m2, r1+r2)
$$


این ویژگی مفید است، به عنوان مثال، برای پنهان کردن مقادیر توکن‌های مبادله شده، در حالی که هنوز قادر به تأیید مجموع‌ها هستید.


اطلاعات بیشتر: [Pedersen commitment](https://link.springer.com/chapter/10.1007/3-540-46766-1_9)


#### Redeem


در یک State Extension، یک Redeem به عمل بازپس‌گیری (یا بهره‌برداری) از یک **Valency** که قبلاً اعلام شده است، اشاره دارد. از آنجا که یک Valency یک حق عمومی است، Redeem به یک شرکت‌کننده مجاز اجازه می‌دهد تا یک Contract State Extension خاص را ادعا کند.


#### Schema


یک Schema در RGB یک قطعه کد اعلامی است که مجموعه‌ای از متغیرها، قوانین و Business Logic (*Business Logic*) را که بر عملکرد یک Contract حاکم هستند، توصیف می‌کند. Schema ساختار حالت، انواع انتقال‌های مجاز و شرایط اعتبارسنجی را تعریف می‌کند.


#### Seal Definition


Seal Definition بخشی از Assignment است که _تعهد_ را با UTXO متعلق به دارنده جدید مرتبط می‌کند. به عبارت دیگر، نشان می‌دهد که شرایط کجا قرار دارد (در کدام UTXO) و Ownership یک دارایی یا حق را تعیین می‌کند.


#### Shard


یک Shard نمایانگر شاخه‌ای در نمودار جهت‌دار غیرمدور (DAG) تاریخچه انتقالات حالت یک RGB Contract است. به عبارت دیگر، این یک زیرمجموعه منسجم از تاریخچه کلی Contract است که به عنوان مثال، به دنباله‌ای از انتقالات مربوط می‌شود که برای اثبات اعتبار یک دارایی خاص از زمان _آغاز_ لازم است.


#### Single-Use Seal


Single-Use Seal یک وعده رمزنگاری از Commitment به پیامی است که هنوز ناشناخته است و تنها در آینده فاش خواهد شد و باید توسط همه اعضای یک مخاطب خاص شناخته شود. هدف جلوگیری از ایجاد تعهدات رقابتی متعدد برای همان Seal است.


#### Stash


Stash مجموعه‌ای از داده‌های سمت کاربر است که یک کاربر برای یک یا چند قرارداد RGB ذخیره می‌کند، به منظور اعتبارسنجی (*Client-side Validation*). این شامل تاریخچه انتقال، محموله‌ها، اثبات‌های اعتبار و غیره می‌شود. هر دارنده تنها بخش‌هایی از تاریخچه را که نیاز دارد (*شاردها*) نگه می‌دارد.


#### State Extension


یک State Extension یک Contract Operation است که برای بازفعال‌سازی به‌روزرسانی‌های وضعیت با بازخرید **ظرفیت‌ها**یی که قبلاً اعلام شده‌اند، استفاده می‌شود. برای مؤثر بودن، یک State Extension باید سپس توسط یک State Transition بسته شود (که وضعیت نهایی Contract را به‌روزرسانی می‌کند).


#### State Transition


State Transition عملیاتی است که وضعیت RGB Contract را به یک وضعیت جدید تغییر می‌دهد. این عملیات می‌تواند داده‌های Global State و/یا Owned State را تغییر دهد. در عمل، هر انتقال توسط قوانین Schema تأیید شده و از طریق یک _تعهد_ در Bitcoin Blockchain لنگر انداخته می‌شود.


#### Taproot


به فرمت تراکنش SegWit v1 از Bitcoin اشاره دارد که توسط [BIP341](https://github.com/Bitcoin/bips/blob/master/bip-0341.mediawiki) و [BIP342](https://github.com/Bitcoin/bips/blob/master/bip-0342.mediawiki) معرفی شده است. Taproot محرمانگی و انعطاف‌پذیری اسکریپت‌ها را بهبود می‌بخشد، به‌ویژه با فشرده‌تر کردن تراکنش‌ها و سخت‌تر کردن تمایز آن‌ها از یکدیگر.


#### Terminal Consignment - Consignment Endpoint


Terminal Consignment (یا _نقطه پایانی محموله_) یک *انتقال Consignment* است که شامل وضعیت نهایی Contract می‌باشد، از جمله State Transition که از Invoice گیرنده (*دریافت‌کننده*) ایجاد شده است. بنابراین، این نقطه پایانی یک انتقال است، با داده‌های لازم برای اثبات اینکه Ownership یا وضعیت منتقل شده است.


#### Transition Bundle


یک Transition Bundle مجموعه‌ای از انتقالات حالت RGB (که به همان Contract تعلق دارند) است که همگی در همان ***Witness Transaction*** Bitcoin درگیر هستند. این امکان را فراهم می‌کند تا چندین به‌روزرسانی یا انتقال را در یک On-Chain Anchor واحد بسته‌بندی کنید.


#### UTXO


یک Bitcoin UTXO (*خروجی تراکنش خرج‌نشده*) توسط Hash یک تراکنش و شاخص خروجی (*vout*) تعریف می‌شود. این همچنین گاهی اوقات به عنوان _outpoint_ نامیده می‌شود. در پروتکل RGB، ارجاع به یک UTXO (از طریق یک **Seal Definition**) مکان **Owned State** را ممکن می‌سازد، یعنی ملکی که بر روی Blockchain نگهداری می‌شود.


#### Valency


یک Valency یک حق عمومی است که به ذخیره‌سازی دولتی به‌عنوان چنین نیازی ندارد، اما می‌تواند از طریق **State Extension** بازخرید شود. بنابراین، این یک شکل از امکان است که برای همه (یا برخی از بازیکنان) باز است و در منطق Contract اعلام شده است تا در تاریخ بعدی یک گسترش خاص انجام شود.


#### Witness Transaction


Witness Transaction تراکنش Bitcoin است که Single-Use Seal را در اطراف پیامی حاوی Multi Protocol Commitment (MPC) می‌بندد. این تراکنش یک UTXO را خرج می‌کند یا یکی ایجاد می‌کند، به طوری که Seal به Commitment مرتبط با پروتکل RGB می‌شود. این به عنوان یک اثبات On-Chain عمل می‌کند که نشان می‌دهد وضعیت در یک نقطه خاص از زمان تنظیم شده است.


# برنامه‌نویسی روی RGB


<partId>148a7436-d079-56d9-be08-aaa4c14c6b3a</partId>


## اجرای قراردادهای RGB


<chapterId>8333ea5f-51c7-5dd5-b1d7-47d491e58e51</chapterId>


:::video id=97d81b85-5a82-40a5-b111-7d96be5afd0f:::


در این فصل، نگاهی دقیق‌تر به چگونگی تعریف و پیاده‌سازی یک RGB Contract خواهیم داشت. خواهیم دید که اجزای یک RGB Contract چیستند، نقش‌های آن‌ها چیست و چگونه ساخته می‌شوند.


### اجزای یک RGB Contract


تا کنون، ما قبلاً در مورد **Genesis** صحبت کرده‌ایم، که نقطه شروع یک Contract را نشان می‌دهد، و دیده‌ایم که چگونه با منطق یک *Contract Operation* و وضعیت پروتکل هماهنگ می‌شود. با این حال، تعریف کامل یک RGB Contract به تنهایی به Genesis محدود نمی‌شود: این شامل سه جزء مکمل است که با هم، هسته اجرای آن را تشکیل می‌دهند.


اولین مؤلفه **Schema** نامیده می‌شود. این یک فایل است که ساختار بنیادی و Business Logic (*Business Logic*) از Contract را توصیف می‌کند. این فایل انواع داده‌های استفاده شده، قوانین اعتبارسنجی، عملیات مجاز (مانند صدور اولیه token، انتقالات، شرایط خاص، و غیره) را مشخص می‌کند - به طور خلاصه، چارچوب کلی که نحوه عملکرد Contract را تعیین می‌کند.


مولفه دوم **Interface** است. این مولفه بر نحوه تعامل کاربران (و به تبع آن، نرم‌افزارهای پرتفوی) با این Contract تمرکز دارد. این مولفه به توصیف معناشناسی، یعنی نمایش خوانا از فیلدها و اقدامات مختلف می‌پردازد. بنابراین، در حالی که Schema نحوه عملکرد فنی Contract را تعریف می‌کند، Interface نحوه ارائه و نمایش این قابلیت‌ها را تعریف می‌کند: نام روش‌ها، نمایش داده‌ها و غیره.


سومین مؤلفه **Interface Implementation** است که با ایفای نقش نوعی پل بین Schema و Interface، دو مؤلفه قبلی را تکمیل می‌کند. به عبارت دیگر، این مؤلفه معنایی که توسط Interface بیان شده را با قوانین زیربنایی تعریف‌شده در Schema مرتبط می‌سازد. این پیاده‌سازی است که به عنوان مثال، مدیریت تبدیل بین یک پارامتر وارد شده در Wallet و ساختار باینری تحمیل‌شده توسط پروتکل، یا کامپایل قوانین اعتبارسنجی به زبان ماشین را بر عهده خواهد داشت.


این ماژولار بودن یک ویژگی جالب از RGB است، زیرا به گروه‌های مختلف توسعه‌دهنده اجازه می‌دهد به‌طور جداگانه روی این جنبه‌ها (*Schema*، *Interface*، *Implementation*) کار کنند، به شرطی که از قوانین اجماع پروتکل پیروی کنند.


در مجموع، هر Contract شامل:




- Genesis**، که حالت اولیه Contract است (و می‌توان آن را به یک تراکنش ویژه تشبیه کرد که اولین Ownership یک دارایی، حق یا هر داده پارامتری دیگر را تعریف می‌کند)؛
- Schema**، که Contract's Business Logic را توصیف می‌کند (انواع داده، قوانین اعتبارسنجی، و غیره)؛
- Interface**، که یک Layer معنایی برای هر دو کیف پول و کاربران انسانی فراهم می‌کند، خواندن و اجرای تراکنش‌ها را روشن می‌سازد؛
- اجرای** Interface، که فاصله بین Business Logic و ارائه را پر می‌کند، تا اطمینان حاصل شود که تعریف Contract با تجربه کاربری سازگار است.


![RGB-Bitcoin](assets/fr/070.webp)


توجه به این نکته مهم است که برای اینکه یک Wallet بتواند یک دارایی RGB را مدیریت کند (خواه یک token قابل تعویض باشد یا یک حق از هر نوع)، باید تمام این Elements‌ها را گردآوری کرده باشد: *Schema*، *Interface*، *Interface Implementation* و *Genesis*. این از طریق یک ***Contract Consignment*** منتقل می‌شود، یعنی یک بسته داده که حاوی همه چیز لازم برای اعتبارسنجی Contract سمت مشتری است.


برای کمک به روشن شدن این مفاهیم، در اینجا یک جدول خلاصه ارائه شده است که اجزای RGB Contract را با مفاهیم از پیش شناخته شده در برنامه‌نویسی شیءگرا (OOP) یا در اکوسیستم اتریوم مقایسه می‌کند:


| RGB Contract Component       | Meaning                        | OOP Equivalent                                     | Ethereum Equivalent                |
| ---------------------------- | ------------------------------ | -------------------------------------------------- | ---------------------------------- |
| **Genesis**                  | Initial state of the contract  | Class constructor                                  | Contract constructor               |
| **Schema**                   | Business logic of the contract | Class                                              | Contract                           |
| **Interface**                | Semantics of the contract      | Interface (Java) / Trait (Rust) / Protocol (Swift) | ERC Standard                       |
| **Interface Implementation** | Mapping semantics and logic    | Impl (Rust) / Implements (Java)                    | Application Binary Interface (ABI) |


ستون سمت چپ Elements خاص پروتکل RGB را نشان می‌دهد. ستون میانی عملکرد مشخص هر مؤلفه را نشان می‌دهد. سپس، در ستون "معادل OOP"، معادل اصطلاح در برنامه‌نویسی شیءگرا را می‌یابیم:




- **Genesis** نقشی مشابه *سازنده کلاس* ایفا می‌کند: اینجا جایی است که وضعیت Contract مقداردهی اولیه می‌شود؛
- **Schema** توصیف یک کلاس است، یعنی تعریف ویژگی‌ها، متدها و منطق زیربنایی آن؛
- **Interface** به *interfaces* (جاوا)، *traits* (Rust) یا *protocols* (سوئیفت) مربوط می‌شود: این‌ها تعاریف عمومی توابع، رویدادها، فیلدها... هستند؛
- **Interface Implementation** با *Impl* در Rust یا *Implements* در جاوا مطابقت دارد، جایی که ما مشخص می‌کنیم کد چگونه واقعاً متدهای اعلام‌شده در Interface را اجرا خواهد کرد.


در زمینه اتریوم، Genesis به *سازنده Contract* نزدیک‌تر است، Schema به تعریف Contract، Interface به یک استاندارد مانند ERC-20 یا ERC-721، و Interface Implementation به ABI (*برنامه باینری Interface*) که فرمت تعاملات با Contract را مشخص می‌کند.


مزیت ماژولار بودن RGB همچنین در این واقعیت نهفته است که ذینفعان مختلف می‌توانند، به عنوان مثال، Interface Implementation خود را بنویسند، به شرطی که منطق *Schema* و معنای *Interface* را رعایت کنند. بنابراین، یک صادرکننده می‌تواند یک رابط کاربری جدید و کاربرپسندتر (Interface) توسعه دهد، بدون اینکه منطق Contract را تغییر دهد، یا برعکس، می‌توان Schema را برای افزودن قابلیت‌ها گسترش داد و نسخه جدیدی از Interface Implementation تطبیق‌یافته ارائه کرد، در حالی که پیاده‌سازی‌های قدیمی برای عملکردهای پایه معتبر باقی می‌مانند.


وقتی که ما یک Contract جدید را کامپایل می‌کنیم، یک Genesis (اولین گام در صدور یا توزیع دارایی) و همچنین اجزای آن (Schema، Interface، Interface Implementation) را generate می‌کنیم. پس از این، Contract به طور کامل عملیاتی شده و می‌تواند به کیف‌پول‌ها و کاربران منتقل شود. این روش، که در آن Genesis با این سه جزء ترکیب می‌شود، درجه بالایی از سفارشی‌سازی (هر Contract می‌تواند منطق خاص خود را داشته باشد)، تمرکززدایی (هر کسی می‌تواند به یک جزء خاص کمک کند)، و امنیت (اعتبارسنجی به‌طور دقیق توسط پروتکل تعریف می‌شود، بدون وابستگی به chain code دلخواه که اغلب در بلاک‌چین‌های دیگر رخ می‌دهد) را تضمین می‌کند.


اکنون می‌خواهم نگاهی دقیق‌تر به هر یک از این اجزا بیندازم: **Schema**، **Interface** و **Interface Implementation**.


### Schema


در بخش قبلی، دیدیم که در اکوسیستم RGB، یک Contract از چندین Elements تشکیل شده است: Genesis که وضعیت اولیه را تعیین می‌کند و چندین مؤلفه مکمل دیگر. هدف از Schema توصیف اعلامی تمام Business Logic های Contract است، یعنی ساختار داده، انواع استفاده شده، عملیات مجاز و شرایط آن‌ها. بنابراین، این یک عنصر بسیار مهم در عملیاتی کردن یک Contract در سمت مشتری است، زیرا هر شرکت‌کننده (مثلاً یک Wallet) باید بررسی کند که انتقال وضعیت‌هایی که دریافت می‌کند با منطق تعریف شده در Schema مطابقت دارد.


می‌توان Schema را به یک "کلاس" در برنامه‌نویسی شیءگرا (OOP) تشبیه کرد. به طور کلی، این به عنوان یک مدل برای تعریف اجزای Contract عمل می‌کند، مانند:




- انواع مختلف ایالت‌های مالکیت‌شده و وظایف؛
- ظرفیت‌ها، یعنی حقوق ویژه‌ای که می‌توانند برای عملیات خاصی (*بازخرید شوند*) فعال شوند؛
- Global State فیلدها، که ویژگی‌های جهانی، عمومی و مشترک Contract را توصیف می‌کنند؛
- ساختار Genesis (اولین عملیاتی که Contract را فعال می‌کند)؛
- اشکال مجاز انتقالات حالت و گسترش‌های حالت، و چگونگی تغییر این عملیات‌ها؛
- فراداده مرتبط با هر عملیات، برای ذخیره‌سازی اطلاعات موقت یا اضافی؛
- قوانینی که تعیین می‌کنند داده‌های داخلی Contract چگونه می‌توانند تکامل یابند (برای مثال، آیا یک فیلد قابل تغییر یا تجمعی است)؛
- دنباله‌هایی از عملیات که معتبر تلقی می‌شوند: به عنوان مثال، ترتیب انتقالاتی که باید رعایت شوند یا مجموعه‌ای از شرایط منطقی که باید برآورده شوند.


![RGB-Bitcoin](assets/fr/071.webp)


هنگامی که *صادرکننده* یک دارایی در RGB یک Contract منتشر می‌کند، Genesis و Schema مرتبط با آن را ارائه می‌دهد. کاربران یا کیف‌پول‌هایی که مایل به تعامل با دارایی هستند، این Schema را بازیابی می‌کنند تا منطق پشت Contract را درک کنند و بتوانند بعداً تأیید کنند که انتقالاتی که در آن شرکت خواهند کرد، قانونی هستند.


اولین گام برای هر کسی که اطلاعاتی درباره یک دارایی RGB (مثلاً انتقال token) دریافت می‌کند، اعتبارسنجی این اطلاعات در برابر Schema است. این شامل استفاده از گردآوری Schema برای:




- بررسی کنید که ایالت‌های مالکیت‌شده، وظایف و سایر Elements به‌درستی تعریف شده‌اند و اینکه آیا آن‌ها به انواع تحمیل‌شده (سیستم نوع سخت‌گیرانه) احترام می‌گذارند یا خیر؛
- بررسی کنید که قوانین انتقال (اسکریپت‌های اعتبارسنجی) رعایت شده باشند. این اسکریپت‌ها می‌توانند از طریق AluVM اجرا شوند، که در سمت مشتری موجود است و مسئول اعتبارسنجی سازگاری Business Logic (مقدار انتقال، شرایط ویژه، و غیره) می‌باشد.


در عمل، Schema کد اجرایی نیست، همان‌طور که در بلاک‌چین‌هایی که بر روی chain code ذخیره می‌شوند (EVM بر روی اتریوم) دیده می‌شود. برعکس، RGB، Business Logic (اعلامی) را از کد اجرایی بر روی Blockchain جدا می‌کند (که به لنگرهای رمزنگاری محدود است). بنابراین، Schema قوانین را تعیین می‌کند، اما اجرای این قوانین خارج از Blockchain، در محل هر شرکت‌کننده، بر اساس اصل Client-side Validation صورت می‌گیرد.


قبل از اینکه یک Schema بتواند توسط برنامه‌های RGB استفاده شود، باید کامپایل شود. این کامپایل یک فایل باینری (مثلاً `.RGB`) یا یک فایل باینری رمزگذاری شده (`.rgba`) تولید می‌کند. وقتی که Wallet این فایل را وارد می‌کند، می‌داند:




- هر نوع داده (اعداد صحیح، ساختارها، آرایه‌ها...) به لطف سیستم نوع دقیق چگونه به نظر می‌رسد؛
- چگونه Genesis باید ساختاردهی شود (برای درک اولیه‌سازی دارایی)؛
- انواع مختلف عملیات (انتقالات حالت، گسترش‌های حالت) و چگونگی تغییر حالت توسط آن‌ها؛
- قوانین اسکریپت‌نویسی (معرفی شده در Schema) که موتور AluVM برای بررسی اعتبار عملیات اعمال خواهد کرد.


همان‌طور که در فصل‌های قبلی توضیح داده شد، *سیستم نوع سخت‌گیرانه* به ما یک فرمت رمزگذاری پایدار و قطعی ارائه می‌دهد: تمام متغیرها، چه وضعیت‌های مالکیت، وضعیت‌های جهانی یا ظرفیت‌ها، به‌طور دقیق توصیف می‌شوند (اندازه، حدود پایین و بالا در صورت لزوم، نوع علامت‌دار یا بدون علامت، و غیره). همچنین امکان تعریف ساختارهای تو در تو وجود دارد، به عنوان مثال برای پشتیبانی از موارد استفاده پیچیده.


به صورت اختیاری، Schema می‌تواند به یک `SchemaId` ریشه اشاره کند که استفاده مجدد از یک ساختار پایه موجود (یک الگو) را تسهیل می‌کند. به این ترتیب، می‌توانید یک Contract را توسعه دهید یا تغییراتی (مثلاً یک نوع جدید از token) از یک الگوی اثبات‌شده ایجاد کنید. این ماژولار بودن نیاز به بازآفرینی قراردادهای کامل را از بین می‌برد و استانداردسازی بهترین شیوه‌ها را تشویق می‌کند.


نکته مهم دیگر این است که منطق تکامل وضعیت (انتقالات، به‌روزرسانی‌ها، و غیره) در Schema به صورت اسکریپت‌ها، قوانین و شرایط توصیف شده است. بنابراین، اگر طراح Contract بخواهد صدور مجدد را مجاز کند یا مکانیزم سوزاندن (نابودی توکن‌ها) را اعمال کند، می‌تواند اسکریپت‌های مربوطه را برای AluVM در بخش اعتبارسنجی Schema مشخص کند.


#### تفاوت با بلاک‌چین‌های قابل برنامه‌ریزی On-Chain


برخلاف سیستم‌هایی مانند اتریوم، که در آن کد Smart contract (قابل اجرا) در خود Blockchain نوشته می‌شود، RGB، Contract (منطق آن) off-chain را به صورت یک سند اعلامی کامپایل شده ذخیره می‌کند. این بدان معناست که:




- هیچ ماشین مجازی تورینگ کامل در هر گره شبکه Bitcoin اجرا نمی‌شود. قوانین یک RGB Contract بر روی Blockchain اجرا نمی‌شوند، بلکه در هر کاربری که مایل به اعتبارسنجی یک وضعیت است، اجرا می‌گردند؛
- داده‌های Contract، Blockchain را آلوده نمی‌کند: تنها شواهد رمزنگاری (*commitments*) در تراکنش‌های Bitcoin تعبیه شده‌اند (از طریق `Tapret` یا `Opret`);
- Schema می‌تواند به‌روزرسانی یا رد شود (*fast-forward*, *push-back* و غیره)، بدون نیاز به Fork بر روی Bitcoin Blockchain. کیف‌پول‌ها تنها نیاز دارند که Schema جدید را وارد کرده و با تغییرات اجماع سازگار شوند.


#### استفاده توسط صادرکننده و توسط کاربران


هنگامی که یک *صادرکننده* یک دارایی ایجاد می‌کند (برای مثال، یک token قابل تعویض غیر تورمی)، آن را آماده می‌کند:




- یک Schema که قوانین انتشار، انتقال، و غیره را توصیف می‌کند؛
- یک Genesis به این Schema تطبیق داده شده است (با تعداد کل توکن‌های صادر شده، هویت مالک اولیه، هرگونه ظرفیت ویژه برای صدور مجدد، و غیره).


سپس فایل کامپایل شده Schema (یک فایل `.RGB`) را در دسترس کاربران قرار می‌دهد، به طوری که هر کسی که انتقال این token را دریافت می‌کند، می‌تواند سازگاری عملیات را به صورت محلی بررسی کند. بدون این Schema، کاربر قادر نخواهد بود داده‌های وضعیت را تفسیر کند یا بررسی کند که آیا با قوانین Contract مطابقت دارد یا خیر.


بنابراین، زمانی که یک Wallet جدید می‌خواهد از یک دارایی پشتیبانی کند، تنها نیاز دارد که Schema مربوطه را یکپارچه کند. این مکانیزم امکان اضافه کردن سازگاری با انواع دارایی جدید RGB را فراهم می‌کند، بدون اینکه نیاز به تغییرات تهاجمی در پایه نرم‌افزاری Wallet باشد: تنها چیزی که لازم است، وارد کردن باینری Schema و درک ساختار آن است.


Schema در RGB، Business Logic را تعریف می‌کند. این سند قوانین تکامل یک Contract، ساختار داده‌های آن (ایالت‌های متعلق، Global State، ظرفیت‌ها) و اسکریپت‌های اعتبارسنجی مرتبط (قابل اجرا توسط AluVM) را فهرست می‌کند. به لطف این سند اعلامی، تعریف یک Contract (فایل کامپایل شده) به وضوح از اجرای واقعی قوانین (سمت کلاینت) جدا شده است. این جداسازی به RGB انعطاف‌پذیری زیادی می‌بخشد و امکان استفاده در موارد مختلف (توکن‌های قابل تعویض، NFT، قراردادهای پیچیده‌تر) را فراهم می‌کند، در حالی که از پیچیدگی و نقص‌های معمول بلاکچین‌های قابل برنامه‌ریزی On-Chain جلوگیری می‌کند.


#### Schema مثال


بیایید نگاهی به یک مثال مشخص از Schema برای یک RGB Contract بیندازیم. این یک استخراج در Rust از فایل `nia.rs` (مخفف "*دارایی‌های غیرقابل تورم*") است که مدلی برای توکن‌های قابل تعویض تعریف می‌کند که نمی‌توانند فراتر از Supply اولیه خود دوباره صادر شوند (یک دارایی غیرتورمی). این نوع token را می‌توان معادل، در دنیای RGB، با ERC20 در اتریوم دانست، یعنی توکن‌های قابل تعویض که به قوانین پایه‌ای خاصی احترام می‌گذارند (مثلاً در مورد انتقالات، راه‌اندازی Supply، و غیره).


قبل از پرداختن به کد، ارزش دارد که ساختار کلی یک RGB Schema را به یاد بیاوریم. یک سری اعلان‌ها وجود دارد که چارچوب‌بندی می‌کند:




- یک `SchemaId` ممکن است که نشان‌دهنده استفاده از یک Schema پایه دیگر به عنوان الگو باشد؛
- **ایالت‌های جهانی** و **ایالت‌های متعلق** (با انواع سخت‌گیرانه آن‌ها)؛
- ظرفیت‌ها** (در صورت وجود)؛
- عملیات **(Genesis، انتقالات حالت، گسترش‌های حالت)** که می‌توانند به این حالات و ظرفیت‌ها اشاره کنند؛
- سیستم نوع سختگیرانه برای توصیف و اعتبارسنجی داده‌ها استفاده می‌شود؛
- اسکریپت‌های اعتبارسنجی** (اجرا از طریق AluVM).


![RGB-Bitcoin](assets/fr/072.webp)


کد زیر تعریف کامل Rust Schema را نشان می‌دهد. ما آن را بخش به بخش، با توجه به توضیحات (1) تا (9) در زیر بررسی خواهیم کرد:


```rust
// ===== PART 1: Function Header and SubSchema =====
fn nia_schema() -> SubSchema {

// definitions of libraries and variables

// ===== PART 2: General Properties (ffv, subset_of, type_system) =====
Schema {
ffv: zero!(),
subset_of: None,
type_system: types.type_system(),

// ===== PART 3: Global States =====
global_types: tiny_bmap! {
GS_NOMINAL => GlobalStateSchema::once(types.get("RGBContract.DivisibleAssetSpec")),
GS_DATA => GlobalStateSchema::once(types.get("RGBContract.ContractData")),
GS_TIMESTAMP => GlobalStateSchema::once(types.get("RGBContract.Timestamp")),
GS_ISSUED_SUPPLY => GlobalStateSchema::once(types.get("RGBContract.Amount")),
},

// ===== PART 4: Owned Types =====
owned_types: tiny_bmap! {
OS_ASSET => StateSchema::Fungible(FungibleType::Unsigned64Bit),
},

// ===== PART 5: Valencies =====
valency_types: none!(),

// ===== PART 6: Genesis: Initial Operations =====
genesis: GenesisSchema {
metadata: Ty::<SemId>::UNIT.id(None),
globals: tiny_bmap! {
GS_NOMINAL => Occurrences::Once,
GS_DATA => Occurrences::Once,
GS_TIMESTAMP => Occurrences::Once,
GS_ISSUED_SUPPLY => Occurrences::Once,
},
assignments: tiny_bmap! {
OS_ASSET => Occurrences::OnceOrMore,
},
valencies: none!(),
},

// ===== PART 7: Extensions =====
extensions: none!(),

// ===== PART 8: Transitions: TS_TRANSFER =====
transitions: tiny_bmap! {
TS_TRANSFER => TransitionSchema {
metadata: Ty::<SemId>::UNIT.id(None),
globals: none!(),
inputs: tiny_bmap! {
OS_ASSET => Occurrences::OnceOrMore,
},
assignments: tiny_bmap! {
OS_ASSET => Occurrences::OnceOrMore,
},
valencies: none!(),
}
},

// ===== PART 9: Script AluVM and Entry Points =====
script: Script::AluVM(AluScript {
libs: confined_bmap! { alu_id => alu_lib },
entry_points: confined_bmap! {
EntryPoint::ValidateGenesis => LibSite::with(FN_GENESIS_OFFSET, alu_id),
EntryPoint::ValidateTransition(TS_TRANSFER) => LibSite::with(FN_TRANSFER_OFFSET, alu_id),
},
}),
}
}
```




- (1) - سرآیند تابع و زیرطرح**


تابع `nia_schema()` یک `SubSchema` را برمی‌گرداند، که نشان می‌دهد این Schema می‌تواند به‌طور جزئی از یک Schema عمومی‌تر به ارث ببرد. در اکوسیستم RGB، این انعطاف‌پذیری امکان استفاده مجدد از برخی استانداردهای Elements یک Schema اصلی را فراهم می‌کند و سپس قوانین خاصی را برای Contract مورد نظر تعریف می‌کند. در اینجا، ما تصمیم می‌گیریم که ارث‌بری را فعال نکنیم، زیرا `subset_of` برابر با `None` خواهد بود.




- (2) - خواص عمومی: ffv، subset_of، type_system**


ویژگی `ffv` به نسخه *fast-forward* از Contract مربوط می‌شود. مقدار `zero!()` در اینجا نشان می‌دهد که ما در نسخه 0 یا نسخه اولیه این Schema هستیم. اگر بعداً بخواهید قابلیت‌های جدیدی (نوع جدیدی از عملیات و غیره) اضافه کنید، می‌توانید این نسخه را افزایش دهید تا تغییر در اجماع را نشان دهید.


ویژگی `subset_of: None` عدم وجود وراثت را تأیید می‌کند. فیلد `type_system` به سیستم نوع سخت‌گیرانه‌ای اشاره دارد که قبلاً در کتابخانه `types` تعریف شده است. این خط نشان می‌دهد که تمام داده‌هایی که توسط Contract استفاده می‌شوند، از پیاده‌سازی سریال‌سازی سخت‌گیرانه‌ای که توسط کتابخانه مذکور ارائه شده است، استفاده می‌کنند.




- (3) - ایالت‌های جهانی


در بخش `global_types`، چهار Elements را اعلام می‌کنیم. ما از کلیدهایی مانند `GS_NOMINAL` یا `GS_ISSUED_SUPPLY` برای ارجاع به آن‌ها در مراحل بعدی استفاده می‌کنیم:




- `GS_NOMINAL` به نوع `DivisibleAssetSpec` اشاره دارد که فیلدهای مختلفی از token ایجاد شده را توصیف می‌کند (نام کامل، نماد، دقت...);
- `GS_DATA` نشان‌دهنده داده‌های عمومی است، مانند سلب مسئولیت، فراداده، یا متن‌های دیگر؛
- `GS_TIMESTAMP` به تاریخ صدور اشاره دارد؛
- `GS_ISSUED_SUPPLY` مجموع Supply را تعیین می‌کند، یعنی حداکثر تعداد توکن‌هایی که می‌توان ایجاد کرد.


کلمه کلیدی `once(...)` به این معنی است که هر یک از این فیلدها فقط می‌توانند یک بار ظاهر شوند.




- (4) - انواع مالکیت‌شده


در `owned_types`، ما `OS_ASSET` را اعلام می‌کنیم که یک حالت قابل تعویض را توصیف می‌کند. ما از `StateSchema::Fungible(FungibleType::Unsigned64Bit)` استفاده می‌کنیم که نشان می‌دهد مقدار دارایی‌ها (توکن‌ها) به عنوان یک عدد صحیح بدون علامت ۶۴ بیتی ذخیره می‌شود. بنابراین، هر تراکنش مقدار معینی از واحدهای این token را ارسال خواهد کرد که بر اساس این ساختار عددی به شدت تایپ شده اعتبارسنجی خواهد شد.




- (5) - **ظرفیت‌ها**


ما `valency_types: none!()` را نشان می‌دهیم، که به این معنی است که هیچ ظرفیت خاصی در این Schema وجود ندارد، به عبارت دیگر هیچ حقوق ویژه یا اضافی (مانند تجدید انتشار، سوزاندن مشروط، و غیره) وجود ندارد. اگر Schema شامل هر یک از این موارد بود، در این بخش اعلام می‌شد.




- (6) - Genesis: اولین عملیات‌ها


در اینجا وارد بخشی می‌شویم که عملیات Contract را اعلام می‌کند. Genesis به این صورت توصیف می‌شود:




- عدم وجود `metadata` (فیلد `metadata: Ty::<SemId>::UNIT.id(None)`);
- ایالت‌های جهانی که باید هر کدام یک بار حضور داشته باشند (`Once`);
- فهرست وظایف که در آن `OS_ASSET` باید `OnceOrMore` ظاهر شود. این بدان معناست که Genesis حداقل به یک `OS_ASSET` Assignment (یک نگهدارنده اولیه) نیاز دارد؛
- شماره Valency: `valencies: none!()`.


این‌گونه تعریف مسئله اولیه token را محدود می‌کنیم: باید اعلام کنیم که Supply صادر شده است (`GS_ISSUED_SUPPLY`)، به‌علاوه حداقل یک دارنده (یک Owned State از نوع `OS_ASSET`).




- (7) - افزونه‌ها


فیلد `extensions: none!()` نشان می‌دهد که هیچ State Extension در این Contract پیش‌بینی نشده است. این به این معنی است که هیچ عملیاتی برای Redeem یک حق دیجیتال (Valency) یا انجام یک State Extension قبل از یک انتقال وجود ندارد. همه چیز از طریق Genesis یا انتقالات حالت انجام می‌شود.




- (8) - انتقالات: TS_TRANSFER


در `transitions`، نوع عملیات `TS_TRANSFER` را تعریف می‌کنیم. ما توضیح می‌دهیم که:




- فاقد فراداده است؛
- این مورد Global State را تغییر نمی‌دهد (که قبلاً در Genesis تعریف شده است)؛
- ورودی آن یک یا چند `OS_ASSET` است. این بدان معناست که باید وضعیت‌های موجود مالکیت‌شده را مصرف کند؛
- این حداقل یک `OS_ASSET` جدید (`assignments`) ایجاد می‌کند (به عبارت دیگر، گیرنده یا گیرندگان توکن دریافت می‌کنند)؛
- این هیچ Valency جدیدی تولید نمی‌کند.


این مدل رفتار یک انتقال پایه را نشان می‌دهد که توکن‌ها را بر روی UTXO مصرف می‌کند، سپس وضعیت‌های مالکیت جدیدی به نفع گیرندگان ایجاد می‌کند و بدین ترتیب برابری مقدار کل بین ورودی‌ها و خروجی‌ها را حفظ می‌کند.




- (9) - اسکریپت AluVM و نقاط ورود** (به زبان فرانسوی)


در نهایت، ما یک اسکریپت AluVM را اعلام می‌کنیم (`Script::AluVM(AluScript { ... })`). این اسکریپت شامل:




- یک یا چند کتابخانه خارجی (`libs`) برای استفاده در طول اعتبارسنجی؛
- نقاط ورود اشاره‌کننده به جابجایی‌های تابع در کد AluVM، مربوط به اعتبارسنجی Genesis (`ValidateGenesis`) و هر انتقال اعلام‌شده (`ValidateTransition(TS_TRANSFER)`).


این کد اعتبارسنجی مسئول اعمال Business Logic است. به عنوان مثال، بررسی خواهد کرد:




- که `GS_ISSUED_SUPPLY` در طول Genesis تجاوز نکند؛
- مجموع `inputs` (توکن‌های خرج‌شده) برابر با مجموع `assignments` (توکن‌های دریافت‌شده) برای `TS_TRANSFER` است.


اگر این قوانین رعایت نشوند، انتقال نامعتبر تلقی خواهد شد.


این مثال از یک "*دارایی قابل تعویض غیر قابل تورم*" Schema به ما درک بهتری از ساختار یک RGB قابل تعویض ساده token Contract می‌دهد. ما می‌توانیم به وضوح جدایی بین توصیف داده‌ها (حالت‌های جهانی و مالکیت)، اعلام عملیات (Genesis، انتقالات، توسعه‌ها) و پیاده‌سازی اعتبارسنجی (اسکریپت‌های AluVM) را ببینیم. به لطف این مدل، یک token مانند یک token قابل تعویض کلاسیک رفتار می‌کند، اما در سمت مشتری اعتبارسنجی می‌شود و برای اجرای کد خود به زیرساخت On-Chain وابسته نیست. تنها تعهدات رمزنگاری در Bitcoin Blockchain لنگر انداخته می‌شوند.


### Interface


Interface همان Layer است که برای قابل خواندن و قابل دستکاری کردن Contract طراحی شده است، هم توسط کاربران (خواندن انسانی) و هم توسط پورتفولیوها (خواندن نرم‌افزاری). بنابراین، Interface نقشی مشابه با Interface در یک زبان برنامه‌نویسی شیءگرا (جاوا، ویژگی Rust، و غیره) ایفا می‌کند، به این صورت که ساختار عملکردی Contract را آشکار و روشن می‌سازد، بدون اینکه لزوماً جزئیات داخلی Business Logic را فاش کند.


برخلاف Schema، که به‌طور کامل اعلامی است و به یک فایل باینری که استفاده از آن به‌صورت مستقیم دشوار است، کامپایل می‌شود، Interface کلیدهای خواندنی مورد نیاز را فراهم می‌کند تا:




- فهرست و توصیف وضعیت‌های جهانی و وضعیت‌های مالکیت‌شده که در Contract گنجانده شده‌اند؛
- به نام‌ها و مقادیر هر فیلد دسترسی پیدا کنید، به‌طوری‌که بتوانند نمایش داده شوند (مثلاً برای یک token، نماد، حداکثر مقدار و غیره را پیدا کنید)؛
- تفسیر و ساخت عملیات Contract (Genesis، State Transition، یا State Extension) با مرتبط کردن داده‌ها به نام‌های قابل فهم (مثلاً انجام یک انتقال با مشخص کردن واضح "مبلغ" به جای یک شناسه باینری).


![RGB-Bitcoin](assets/fr/073.webp)


با تشکر از Interface، شما می‌توانید، به عنوان مثال، کدی در Wallet بنویسید که به جای دستکاری فیلدها، مستقیماً برچسب‌هایی مانند "تعداد توکن‌ها"، "نام دارایی"، و غیره را دستکاری کند. به این ترتیب، مدیریت Contract به صورت شهودی‌تر انجام می‌شود. به این صورت، مدیریت Contract شهودی‌تر می‌شود.


#### عملیات عمومی


این روش مزایای زیادی دارد:




- استانداردسازی:**


نوع مشابه Contract می‌تواند توسط یک Interface استاندارد پشتیبانی شود که بین چندین پیاده‌سازی Wallet به اشتراک گذاشته شده است. این امر سازگاری و استفاده مجدد از کد را تسهیل می‌کند.




- جداسازی واضح بین Schema و Interface:**


در طراحی RGB، Schema (Business Logic) و Interface (ارائه و دستکاری) دو موجودیت مستقل هستند. توسعه‌دهندگانی که منطق Contract را می‌نویسند می‌توانند بر روی Schema تمرکز کنند، بدون نگرانی در مورد ارگونومی یا نمایش داده‌ها، در حالی که تیم دیگری (یا همان تیم، اما در یک زمان‌بندی متفاوت) می‌تواند Interface را توسعه دهد.




- تکامل انعطاف‌پذیر:**


Interface می‌تواند پس از صدور دارایی تغییر داده شود یا به آن اضافه شود، بدون اینکه نیازی به تغییر خود Contract باشد. این تفاوت عمده‌ای با برخی از سیستم‌های On-Chain Smart contract دارد، جایی که Interface (که اغلب با کد اجرایی مخلوط می‌شود) در Blockchain منجمد می‌شود.




- قابلیت Multi-Interface


همان Contract می‌تواند از طریق رابط‌های مختلفی که برای نیازهای متمایز تطبیق داده شده‌اند، ارائه شود: یک Interface ساده برای کاربر نهایی، و دیگری پیشرفته‌تر برای صادرکننده‌ای که نیاز به مدیریت عملیات پیکربندی پیچیده دارد. سپس Wallet می‌تواند انتخاب کند که کدام Interface را وارد کند، بسته به استفاده‌اش.


![RGB-Bitcoin](assets/fr/074.webp)


در عمل، زمانی که Wallet یک RGB Contract را بازیابی می‌کند (از طریق یک فایل `.RGB` یا `.rgba`)، همچنین Interface مرتبط را که کامپایل شده است، وارد می‌کند. در زمان اجرا، Wallet می‌تواند، به عنوان مثال:




- فهرست ایالت‌ها را مرور کنید و نام‌های آن‌ها را بخوانید تا Ticker، مبلغ اولیه، تاریخ انتشار و غیره را بر روی کاربر Interface نمایش دهید، به جای یک شناسه عددی غیرقابل خواندن؛
- یک عملیات (مانند انتقال) با استفاده از نام‌های پارامتر صریح بسازید: به جای نوشتن `assignments { OS_ASSET => 1 }`، می‌تواند یک فیلد "مقدار" در یک فرم به کاربر ارائه دهد و این اطلاعات را به فیلدهای به شدت تایپ شده‌ای که توسط Contract انتظار می‌رود، ترجمه کند.


#### تفاوت با اتریوم و سیستم‌های دیگر


در اتریوم، Interface (توصیف شده از طریق ABI، *Application Binary Interface*) به طور کلی از کد ذخیره شده On-Chain (Smart contract) مشتق می‌شود. تغییر یک بخش خاص از Interface بدون دست زدن به خود Contract می‌تواند پرهزینه یا پیچیده باشد. با این حال، RGB بر اساس یک منطق کاملاً off-chain است، با داده‌هایی که در *commitments* بر روی Bitcoin لنگر انداخته‌اند. این طراحی امکان تغییر Interface (یا پیاده‌سازی آن) را بدون تأثیر بر امنیت بنیادی Contract فراهم می‌کند، زیرا اعتبارسنجی قوانین کسب‌وکار در Schema و کد مرجع AluVM باقی می‌ماند.


#### گردآوری Interface


همانند Schema، Interface در کد منبع (اغلب در Rust) تعریف شده و به یک فایل `.RGB` یا `.rgba` کامپایل می‌شود. این فایل باینری شامل تمام اطلاعات مورد نیاز توسط Wallet برای:




- شناسایی فیلدها با نام؛
- هر فیلد (و مقدار آن) را به نوع سیستم سخت‌گیرانه تعریف‌شده در Contract پیوند دهید؛
- بدانید که عملیات مختلف مجاز چیست و چگونه آنها را بسازید.


هنگامی که Interface وارد شد، Wallet می‌تواند به درستی Contract را نمایش دهد و تعاملاتی را به کاربر پیشنهاد دهد.


### رابط‌های استاندارد شده توسط انجمن LNP/BP


در اکوسیستم RGB، یک Interface برای ارائه معنای قابل خواندن و قابل دستکاری به داده‌ها و عملیات یک Contract استفاده می‌شود. بنابراین، Interface مکمل Schema است که Business Logic را به صورت داخلی توصیف می‌کند (انواع سختگیرانه، اسکریپت‌های اعتبارسنجی، و غیره). در این بخش، به رابط‌های استاندارد توسعه‌یافته توسط انجمن LNP/BP برای انواع رایج Contract (توکن‌های قابل تعویض، NFT، و غیره) نگاهی خواهیم انداخت.


به عنوان یک یادآوری، ایده این است که هر Interface نحوه نمایش و مدیریت یک Contract را در سمت Wallet توصیف می‌کند، به وضوح فیلدها (مانند `spec`، `ticker`، `issuedSupply`...) را نام‌گذاری کرده و عملیات ممکن (مانند `Transfer`، `Burn`، `Rename`...) را تعریف می‌کند. چندین رابط کاربری در حال حاضر عملیاتی هستند، اما در آینده تعداد بیشتری وجود خواهد داشت.


#### برخی رابط‌های آماده استفاده


**RGB20** استاندارد Interface برای دارایی‌های قابل تعویض است که می‌توان آن را با استاندارد ERC20 اتریوم مقایسه کرد. با این حال، یک قدم فراتر می‌رود و قابلیت‌های گسترده‌تری ارائه می‌دهد:




- به عنوان مثال، توانایی تغییر نام دارایی (تغییر *نماد* یا نام کامل) پس از انتشار آن، یا تنظیم دقت آن (*تقسیم سهام*);
- همچنین می‌تواند مکانیسم‌هایی برای صدور مجدد ثانویه (محدود یا نامحدود) و برای سوزاندن و سپس جایگزینی توصیف کند، به منظور مجاز کردن صادرکننده برای از بین بردن و سپس بازآفرینی دارایی‌ها تحت شرایط خاص؛


به عنوان مثال، RGB20 Interface می‌تواند به **طرح دارایی غیرقابل تورم (NIA)** متصل شود، که یک Supply اولیه غیرقابل تورم را تحمیل می‌کند، یا به طرح‌های پیشرفته‌تر دیگر در صورت نیاز متصل شود.


**RGB21** به قراردادهای نوع NFT مربوط می‌شود، یا به طور گسترده‌تر، هر محتوای دیجیتال منحصر به فرد، مانند نمایش رسانه‌های دیجیتال (تصاویر، موسیقی و غیره). علاوه بر توصیف صدور و انتقال یک دارایی واحد، ویژگی‌هایی مانند:




- پشتیبانی یکپارچه برای درج مستقیم یک فایل (تا 16 مگابایت) در Contract (برای بازیابی سمت مشتری)؛
- امکان برای مالک برای وارد کردن یک "*حکاکی*" در تاریخچه برای اثبات Ownership گذشته یک NFT.


**RGB25** یک استاندارد ترکیبی است که جنبه‌های قابل تعویض و غیرقابل تعویض را ترکیب می‌کند. این استاندارد برای دارایی‌های نیمه قابل تعویض طراحی شده است، مانند توکنیزاسیون املاک و مستغلات، جایی که می‌خواهید یک ملک را تقسیم کنید در حالی که ارتباط با یک دارایی ریشه‌ای واحد را حفظ می‌کنید (به عبارت دیگر، شما قطعات قابل تعویض یک خانه دارید که به یک خانه غیرقابل تعویض متصل هستند). از نظر فنی، این Interface می‌تواند به **دارایی قابل جمع‌آوری قابل تعویض* (CFA)** Schema متصل شود، که مفهوم تقسیم را در حالی که دارایی اصلی را ردیابی می‌کند، در نظر می‌گیرد.


#### رابط‌های در حال توسعه


رابط‌های دیگر برای استفاده‌های تخصصی‌تر برنامه‌ریزی شده‌اند، اما هنوز در دسترس نیستند:




- RGB22**، اختصاص‌یافته به هویت‌های دیجیتال، برای مدیریت شناسه‌ها و پروفایل‌های On-Chain در اکوسیستم RGB؛
- RGB23**، برای زمان‌سنجی پیشرفته، با استفاده از برخی ایده‌های *Opentimestamps*، اما با ویژگی‌های ردیابی؛
- RGB24**، که هدف آن معادل یک سیستم نام دامنه غیرمتمرکز (DNS) مشابه *Ethereum Name Service* است؛
- RGB26**، طراحی شده برای مدیریت DAOها (*سازمان خودگردان غیرمتمرکز*) در قالبی پیچیده‌تر (حاکمیت، رأی‌گیری، و غیره)؛
- RGB30**، بسیار شبیه به RGB20 است اما با ویژگی در نظر گرفتن انتشار اولیه غیرمتمرکز و استفاده از افزونه‌های دولتی. این برای دارایی‌هایی استفاده می‌شود که انتشار مجدد آن‌ها توسط چندین نهاد مدیریت می‌شود یا تحت شرایط دقیق‌تری قرار دارد.


البته، بسته به تاریخی که این دوره را مشاوره می‌کنید، این رابط‌ها ممکن است از قبل عملیاتی و قابل دسترسی باشند.


#### Interface مثال


این قطعه کد Rust یک [RGB20](https://github.com/RGB-WG/RGB-std/blob/master/src/Interface/rgb20.rs) Interface (دارایی قابل تعویض) را نشان می‌دهد. این کد از فایل `rgb20.rs` در کتابخانه استاندارد RGB گرفته شده است. بیایید نگاهی به آن بیندازیم تا ساختار یک Interface را درک کنیم و ببینیم چگونه پلی بین، از یک سو، Business Logic (تعریف شده در Schema) و از سوی دیگر، قابلیت‌های ارائه شده به کیف پول‌ها و کاربران فراهم می‌کند.


```rust
// ...
fn rgb20() -> Iface {
let types = StandardTypes::with(rgb20_stl());

Iface {
version: VerNo::V1,
name: tn!("RGB20"),
global_state: tiny_bmap! {
fname!("spec") => GlobalIface::required(types.get("RGBContract.DivisibleAssetSpec")),
fname!("data") => GlobalIface::required(types.get("RGBContract.ContractData")),
fname!("created") => GlobalIface::required(types.get("RGBContract.Timestamp")),
fname!("issuedSupply") => GlobalIface::one_or_many(types.get("RGBContract.Amount")),
fname!("burnedSupply") => GlobalIface::none_or_many(types.get("RGBContract.Amount")),
fname!("replacedSupply") => GlobalIface::none_or_many(types.get("RGBContract.Amount")),
},
assignments: tiny_bmap! {
fname!("inflationAllowance") => AssignIface::public(OwnedIface::Amount, Req::NoneOrMore),
fname!("updateRight") => AssignIface::public(OwnedIface::Rights, Req::Optional),
fname!("burnEpoch") => AssignIface::public(OwnedIface::Rights, Req::Optional),
fname!("burnRight") => AssignIface::public(OwnedIface::Rights, Req::NoneOrMore),
fname!("assetOwner") => AssignIface::private(OwnedIface::Amount, Req::NoneOrMore),
},
valencies: none!(),
genesis: GenesisIface {
metadata: Some(types.get("RGBContract.IssueMeta")),
global: tiny_bmap! {
fname!("spec") => ArgSpec::required(),
fname!("data") => ArgSpec::required(),
fname!("created") => ArgSpec::required(),
fname!("issuedSupply") => ArgSpec::required(),
},
assignments: tiny_bmap! {
fname!("assetOwner") => ArgSpec::many(),
fname!("inflationAllowance") => ArgSpec::many(),
fname!("updateRight") => ArgSpec::optional(),
fname!("burnEpoch") => ArgSpec::optional(),
},
valencies: none!(),
errors: tiny_bset! {
SUPPLY_MISMATCH,
INVALID_PROOF,
INSUFFICIENT_RESERVES
},
},
transitions: tiny_bmap! {
tn!("Transfer") => TransitionIface {
optional: false,
metadata: None,
globals: none!(),
inputs: tiny_bmap! {
fname!("previous") => ArgSpec::from_non_empty("assetOwner"),
},
assignments: tiny_bmap! {
fname!("beneficiary") => ArgSpec::from_non_empty("assetOwner"),
},
valencies: none!(),
errors: tiny_bset! {
NON_EQUAL_AMOUNTS
},
default_assignment: Some(fname!("beneficiary")),
},
tn!("Issue") => TransitionIface {
optional: true,
metadata: Some(types.get("RGBContract.IssueMeta")),
globals: tiny_bmap! {
fname!("issuedSupply") => ArgSpec::required(),
},
inputs: tiny_bmap! {
fname!("used") => ArgSpec::from_non_empty("inflationAllowance"),
},
assignments: tiny_bmap! {
fname!("beneficiary") => ArgSpec::from_many("assetOwner"),
fname!("future") => ArgSpec::from_many("inflationAllowance"),
},
valencies: none!(),
errors: tiny_bset! {
SUPPLY_MISMATCH,
INVALID_PROOF,
ISSUE_EXCEEDS_ALLOWANCE,
INSUFFICIENT_RESERVES
},
default_assignment: Some(fname!("beneficiary")),
},
tn!("OpenEpoch") => TransitionIface {
optional: true,
metadata: None,
globals: none!(),
inputs: tiny_bmap! {
fname!("used") => ArgSpec::from_required("burnEpoch"),
},
assignments: tiny_bmap! {
fname!("next") => ArgSpec::from_optional("burnEpoch"),
fname!("burnRight") => ArgSpec::required()
},
valencies: none!(),
errors: none!(),
default_assignment: Some(fname!("burnRight")),
},
tn!("Burn") => TransitionIface {
optional: true,
metadata: Some(types.get("RGBContract.BurnMeta")),
globals: tiny_bmap! {
fname!("burnedSupply") => ArgSpec::required(),
},
inputs: tiny_bmap! {
fname!("used") => ArgSpec::from_required("burnRight"),
},
assignments: tiny_bmap! {
fname!("future") => ArgSpec::from_optional("burnRight"),
},
valencies: none!(),
errors: tiny_bset! {
SUPPLY_MISMATCH,
INVALID_PROOF,
INSUFFICIENT_COVERAGE
},
default_assignment: None,
},
tn!("Replace") => TransitionIface {
optional: true,
metadata: Some(types.get("RGBContract.BurnMeta")),
globals: tiny_bmap! {
fname!("replacedSupply") => ArgSpec::required(),
},
inputs: tiny_bmap! {
fname!("used") => ArgSpec::from_required("burnRight"),
},
assignments: tiny_bmap! {
fname!("beneficiary") => ArgSpec::from_many("assetOwner"),
fname!("future") => ArgSpec::from_optional("burnRight"),
},
valencies: none!(),
errors: tiny_bset! {
NON_EQUAL_AMOUNTS,
SUPPLY_MISMATCH,
INVALID_PROOF,
INSUFFICIENT_COVERAGE
},
default_assignment: Some(fname!("beneficiary")),
},
tn!("Rename") => TransitionIface {
optional: true,
metadata: None,
globals: tiny_bmap! {
fname!("new") => ArgSpec::from_required("spec"),
},
inputs: tiny_bmap! {
fname!("used") => ArgSpec::from_required("updateRight"),
},
assignments: tiny_bmap! {
fname!("future") => ArgSpec::from_optional("updateRight"),
},
valencies: none!(),
errors: none!(),
default_assignment: Some(fname!("future")),
},
},
extensions: none!(),
error_type: types.get("RGB20.Error"),
default_operation: Some(tn!("Transfer")),
type_system: types.type_system(),
}
}
```


در این Interface، ما شباهت‌هایی با ساختار Schema مشاهده می‌کنیم: ما یک اعلامیه از Global State، ایالت‌های مالکیت شده، عملیات Contract (Genesis و انتقالات)، و همچنین مدیریت خطا پیدا می‌کنیم. اما Interface بر ارائه و دستکاری این Elements برای یک Wallet یا هر برنامه کاربردی دیگر تمرکز دارد.


تفاوت با Schema در ماهیت انواع است. Schema از انواع سخت‌گیرانه (مانند `FungibleType::Unsigned64Bit`) و شناسه‌های فنی‌تر استفاده می‌کند. Interface از نام‌های فیلد، ماکروها (`fname!()`, `tn!()`) و ارجاعات به کلاس‌های آرگومان (`ArgSpec`, `OwnedIface::Rights`...) استفاده می‌کند. هدف در اینجا تسهیل درک عملکردی و سازماندهی Elements برای Wallet است.


علاوه بر این، Interface می‌تواند عملکرد اضافی را به Schema پایه معرفی کند (مثلاً مدیریت یک حق `burnEpoch`)، تا زمانی که این با منطق نهایی تأیید شده سمت مشتری سازگار باقی بماند. بخش "script" در AluVM در Schema اعتبار رمزنگاری را تضمین می‌کند، در حالی که Interface توضیح می‌دهد که چگونه کاربر (یا Wallet) با این وضعیت‌ها و انتقال‌ها تعامل دارد.


#### Global State و وظایف


در بخش `global_state`، ما به فیلدهایی مانند `spec` (توضیحات دارایی)، `data`، `created`، `issuedSupply`، `burnedSupply`، `replacedSupply` برمی‌خوریم. این‌ها فیلدهایی هستند که Wallet می‌تواند بخواند و ارائه دهد. برای مثال:




- `spec` پیکربندی token را نمایش خواهد داد؛
- `issuedSupply` یا `burnedSupply` به ما تعداد کل توکن‌های صادر شده یا سوزانده شده و غیره را می‌دهد.


در بخش `assignments`، نقش‌ها یا حقوق مختلفی را تعریف می‌کنیم. برای مثال:




- `assetOwner` مربوط به نگهداری توکن‌ها است (این همان *Owned State* قابل تعویض است)؛
- `burnRight` با توانایی سوزاندن توکن‌ها مطابقت دارد؛
- `updateRight` مربوط به حق تغییر نام دارایی است.


کلمه کلیدی `public` یا `private` (مثلاً `AssignIface::public(...)`) نشان می‌دهد که آیا این وضعیت‌ها قابل مشاهده (`public`) یا محرمانه (`private`) هستند. در مورد `Req::NoneOrMore`، `Req::Optional`، آن‌ها وقوع مورد انتظار را نشان می‌دهند.


#### Genesis و انتقال‌ها


قسمت `Genesis` نحوه‌ی مقداردهی اولیه دارایی را توصیف می‌کند:




- فیلدهای `spec`، `data`، `created`، `issuedSupply` اجباری هستند (`ArgSpec::required()`);
- تکالیفی مانند `assetOwner` می‌توانند در نسخه‌های متعدد (`ArgSpec::many()`) حضور داشته باشند، که به توکن‌ها اجازه می‌دهد به چندین دارنده اولیه توزیع شوند؛
- فیلدهایی مانند `inflationAllowance` یا `burnEpoch` ممکن است (یا ممکن است) در Genesis گنجانده شوند.


سپس، برای هر انتقال (`Transfer`، `Issue`، `Burn`...)، Interface تعیین می‌کند که کدام فیلدها به عنوان ورودی انتظار می‌رود، کدام فیلدها به عنوان خروجی تولید می‌شوند، و هر گونه خطایی که ممکن است رخ دهد. برای مثال:


**گذار:**




- ورودی‌ها: `previous` → باید یک `assetOwner` باشد؛
- وظایف: `ذینفع` → به یک `مالک دارایی` جدید تبدیل خواهد شد؛
- خطا: `NON_EQUAL_AMOUNTS` (بنابراین Wallet قادر خواهد بود مواردی را که مجموع ورودی با مجموع خروجی مطابقت ندارد، مدیریت کند).


**انتقال `مشکل`:**




- اختیاری (`optional: true`)، زیرا انتشار اضافی لزوماً فعال نمی‌شود؛
- ورودی‌ها: `used` → یک `inflationAllowance`، یعنی اجازه برای افزودن توکن‌های بیشتر؛
- وظایف: `ذینفع` (توکن‌های جدید دریافت شده) و `آینده` (باقیمانده `inflationAllowance`);
- خطاهای احتمالی: `SUPPLY_MISMATCH`، `ISSUE_EXCEEDS_ALLOWANCE`، و غیره.


**انتقال سوختن:**




- ورودی‌ها: `used` → یک `burnRight`;
- متغیرهای عمومی: `burnedSupply` مورد نیاز است؛
- وظایف: `آینده` → یک ادامه ممکن از `burnRight` اگر همه چیز را نسوزانده باشیم؛
- خطاها: `SUPPLY_MISMATCH`, `INVALID_PROOF`, `INSUFFICIENT_COVERAGE`.


هر عملیات به گونه‌ای توصیف می‌شود که برای یک Wallet قابل خواندن باشد. این امکان را فراهم می‌کند تا یک Interface گرافیکی نمایش داده شود که در آن کاربر به وضوح می‌تواند ببیند: "شما حق سوزاندن دارید. آیا می‌خواهید مقدار معینی را بسوزانید؟ کد می‌داند که یک فیلد `burnedSupply` را پر کند و بررسی کند که `burnRight` معتبر است.


در مجموع، مهم است که به خاطر داشته باشید که یک Interface، هرچند کامل، به تنهایی منطق داخلی Contract را تعریف نمی‌کند. قلب کار توسط **Schema** انجام می‌شود که شامل انواع سخت‌گیرانه، ساختار Genesis، انتقال‌ها و غیره است. Interface به سادگی این Elements را به روشی شهودی‌تر و نام‌گذاری شده برای استفاده در یک برنامه کاربردی ارائه می‌دهد.


با تشکر از ماژولار بودن RGB، می‌توان Interface را ارتقا داد (برای مثال، با افزودن یک انتقال `Rename`، تصحیح نمایش یک فیلد، و غیره) بدون نیاز به بازنویسی کامل Contract. کاربران این Interface می‌توانند بلافاصله از این بهبودها بهره‌مند شوند، به محض اینکه فایل `.RGB` یا `.rgba` را به‌روزرسانی کنند.


اما زمانی که یک Interface را اعلام کردید، باید آن را به Schema مربوطه متصل کنید. این کار از طریق ***Interface Implementation*** انجام می‌شود، که مشخص می‌کند چگونه هر فیلد نام‌گذاری شده (مانند `fname!("assetOwner")`) را به شناسه‌ی دقیق (مانند `OS_ASSET`) تعریف‌شده در Schema نگاشت کنید. این اطمینان حاصل می‌کند، به عنوان مثال، زمانی که یک Wallet فیلد `burnRight` را دستکاری می‌کند، این همان حالتی است که در Schema توانایی سوزاندن توکن‌ها را توصیف می‌کند.


### Interface Implementation


در معماری RGB، مشاهده کرده‌ایم که هر مؤلفه (Schema، Interface، و غیره) می‌تواند به‌طور مستقل توسعه و کامپایل شود. با این حال، هنوز یک عنصر ضروری وجود دارد که این بلوک‌های ساختمانی مختلف را به هم متصل می‌کند: ***Interface Implementation***. این همان چیزی است که به‌طور صریح شناسه‌ها یا فیلدهای تعریف‌شده در Schema (در سمت Business Logic) را به نام‌های اعلام‌شده در Interface (در سمت ارائه و تعامل کاربر) نگاشت می‌کند. بنابراین، وقتی یک Wallet یک Contract را بارگذاری می‌کند، می‌تواند دقیقاً بفهمد که کدام فیلد به چه چیزی مربوط است و چگونه یک عملیات نام‌گذاری‌شده در Interface به منطق Schema مرتبط است.


نکته مهم این است که Interface Implementation لزوماً برای نمایش تمام قابلیت‌های Schema یا تمام فیلدهای Interface طراحی نشده است: می‌توان آن را به یک زیرمجموعه محدود کرد. در عمل، این امکان را فراهم می‌کند تا جنبه‌های خاصی از Schema را محدود یا فیلتر کنیم. به عنوان مثال، ممکن است یک Schema با چهار نوع عملیات داشته باشید، اما یک پیاده‌سازی Interface که فقط دو تای آن‌ها را در یک زمینه خاص نگاشت می‌کند. برعکس، اگر یک Interface نقاط پایانی اضافی پیشنهاد دهد، می‌توانیم انتخاب کنیم که آن‌ها را در اینجا پیاده‌سازی نکنیم.


در اینجا یک مثال کلاسیک از Interface Implementation است، جایی که ما یک *دارایی غیرقابل تورم* (NIA) Schema را با RGB20 Interface مرتبط می‌کنیم:


```rust
fn nia_rgb20() -> IfaceImpl {
let schema = nia_schema();
let iface = Rgb20::iface();

IfaceImpl {
version: VerNo::V1,
schema_id: schema.schema_id(),
iface_id: iface.iface_id(),
script: none!(),
global_state: tiny_bset! {
NamedField::with(GS_NOMINAL, fname!("spec")),
NamedField::with(GS_DATA, fname!("data")),
NamedField::with(GS_TIMESTAMP, fname!("created")),
NamedField::with(GS_ISSUED_SUPPLY, fname!("issuedSupply")),
},
assignments: tiny_bset! {
NamedField::with(OS_ASSET, fname!("assetOwner")),
},
valencies: none!(),
transitions: tiny_bset! {
NamedType::with(TS_TRANSFER, tn!("Transfer")),
},
extensions: none!(),
}
}
```


در این پیاده‌سازی Interface:




- ما به‌طور صریح به Schema از طریق `nia_schema()` و Interface از طریق `Rgb20::iface()` اشاره می‌کنیم. فراخوانی‌های `Schema.schema_id()` و `iface.iface_id()` برای Anchor کردن Interface Implementation در سمت کامپایل استفاده می‌شوند (این کار شناسه‌های رمزنگاری این دو مؤلفه را مرتبط می‌سازد)؛
- یک نگاشت بین Schema Elements و Interface Elements برقرار شده است. به عنوان مثال، فیلد `GS_NOMINAL` در Schema به رشته `"spec"` در سمت Interface مرتبط است (`NamedField::with(GS_NOMINAL, fname!("spec"))`). ما همین کار را برای عملیات‌ها انجام می‌دهیم، مانند `TS_TRANSFER` که آن را به `"Transfer"` در Interface مرتبط می‌کنیم...;
- می‌بینیم که هیچ ظرفیت (`valencies: none!()`) یا افزونه‌ای (`extensions: none!()`) وجود ندارد، که نشان‌دهنده این واقعیت است که این NIA Contract از این ویژگی‌ها استفاده نمی‌کند.


نتیجه پس از کامپایل، یک فایل جداگانه `.RGB` یا `.rgba` است که علاوه بر Schema و Interface به Wallet وارد می‌شود. بنابراین، نرم‌افزار می‌داند چگونه این NIA Contract (که منطق آن توسط Schema توصیف شده است) را به "RGB20" Interface (که نام‌های انسانی و یک حالت تعامل برای توکن‌های قابل تعویض ارائه می‌دهد) به طور مشخص متصل کند و این Interface Implementation را به عنوان یک دروازه بین این دو اعمال کند.


#### چرا Interface Implementation را جدا کنیم؟


جداسازی انعطاف‌پذیری را افزایش می‌دهد. یک Schema می‌تواند چندین پیاده‌سازی متمایز Interface داشته باشد که هر کدام مجموعه‌ای متفاوت از قابلیت‌ها را نقشه‌برداری می‌کنند. علاوه بر این، خود Interface Implementation می‌تواند تکامل یابد یا بازنویسی شود بدون اینکه نیازی به تغییر در Schema یا Interface باشد. این اصل ماژولار بودن RGB را حفظ می‌کند: هر جزء (Schema، Interface، Interface Implementation) می‌تواند به‌طور مستقل نسخه‌بندی و به‌روزرسانی شود، به شرطی که قوانین سازگاری تحمیل‌شده توسط پروتکل رعایت شوند (شناسه‌های یکسان، سازگاری انواع، و غیره).


در استفاده‌ی عملی، زمانی که Wallet یک Contract را بارگذاری می‌کند، باید:




- بارگذاری **Schema** کامپایل شده (برای آشنایی با ساختار Business Logic)؛
- بارگذاری کامپایل شده **Interface** (برای درک نام‌ها و عملیات سمت کاربر)؛
- بارگذاری کامپایل شده **Interface Implementation** (برای اتصال منطق Schema به نام‌های Interface، عملیات به عملیات، فیلد به فیلد).


این معماری مدولار امکان‌پذیر می‌سازد سناریوهای استفاده مانند:




- محدود کردن برخی عملیات‌ها برای کاربران خاص: ارائه یک پیاده‌سازی جزئی Interface که تنها دسترسی به انتقالات پایه را فراهم می‌کند، بدون ارائه عملکردهای سوزاندن یا به‌روزرسانی، به عنوان مثال؛
- تغییر ارائه: طراحی یک Interface Implementation که یک فیلد را در Interface تغییر نام می‌دهد یا به‌طور متفاوتی نگاشت می‌کند، بدون تغییر در اساس Contract؛
- پشتیبانی از طرح‌های متعدد: یک Wallet می‌تواند چندین پیاده‌سازی Interface را برای همان نوع Interface بارگذاری کند تا طرح‌های مختلف (منطق‌های مختلف token) را مدیریت کند، به شرطی که ساختار آن‌ها سازگار باشد.


در فصل بعدی، به نحوه کار انتقال Contract و چگونگی تولید فاکتورهای RGB خواهیم پرداخت.


## انتقالات Contract


<chapterId>f043a307-d420-5752-b0d7-ebfd845802c0</chapterId>


:::video id=75eb5a8d-1910-4155-b5e3-81204c9a8901:::


در این فصل، ما قصد داریم فرآیند انتقال Contract در اکوسیستم RGB را تحلیل کنیم. برای توضیح این موضوع، نگاهی خواهیم داشت به Alice و Bob، قهرمانان معمول ما، که می‌خواهند یک دارایی Exchange را به RGB منتقل کنند. همچنین برخی از گزیده‌های دستوری از ابزار خط فرمان `RGB` را نشان خواهیم داد تا ببینیم چگونه در عمل کار می‌کند.


### درک انتقال RGB Contract


بیایید مثالی از انتقال بین Alice و Bob را بررسی کنیم. در این مثال، فرض می‌کنیم که Bob تازه شروع به استفاده از RGB کرده است، در حالی که Alice از قبل دارایی‌های RGB را در Wallet خود دارد. خواهیم دید که چگونه Bob محیط خود را تنظیم می‌کند، Contract مربوطه را وارد می‌کند، سپس درخواست انتقال از Alice می‌دهد، و در نهایت چگونه Alice تراکنش واقعی را بر روی Bitcoin Blockchain انجام می‌دهد.


#### 1) نصب RGB Wallet


اول از همه، Bob نیاز دارد تا یک RGB Wallet نصب کند، یعنی نرم‌افزاری که با پروتکل سازگار باشد. این در ابتدا هیچ قراردادی را شامل نمی‌شود. Bob همچنین نیاز خواهد داشت:




- Bitcoin Wallet برای مدیریت UTXOهای شما؛
- اتصال به یک نود Bitcoin (یا به یک سرور Electrum)، به‌طوری که بتوانید UTXOهای خود را شناسایی کرده و تراکنش‌های خود را در شبکه منتشر کنید.


به عنوان یادآوری، **ایالت‌های مالکیت‌شده** در RGB به UTXOهای Bitcoin اشاره دارند. بنابراین ما باید همیشه قادر به مدیریت و خرج کردن UTXOها در یک تراکنش Bitcoin باشیم که تعهدات رمزنگاری (`Tapret` یا `Opret`) اشاره‌کننده به داده‌های RGB را در بر می‌گیرد.


#### ۲) کسب اطلاعات Contract


Bob سپس باید داده‌های Contract که به آن علاقه‌مند است را بازیابی کند. این داده‌ها می‌توانند از طریق هر کانالی گردش کنند: وب‌سایت، ایمیل، برنامه پیام‌رسان... در عمل، آن‌ها در یک ***Consignment***، یعنی یک بسته کوچک داده که شامل موارد زیر است، گروه‌بندی می‌شوند:




- **Genesis**، که حالت اولیه Contract را تعریف می‌کند؛
- **Schema**، که Business Logic (انواع سخت‌گیرانه، اسکریپت‌های اعتبارسنجی، و غیره) را توصیف می‌کند؛
- **Interface**، که ارائه Layer (نام‌های فیلد، عملیات قابل دسترسی) را تعریف می‌کند؛
- **Interface Implementation**، که به طور مشخص Schema را به Interface متصل می‌کند.


![RGB-Bitcoin](assets/fr/075.webp)


اندازه کل اغلب در حدود چند کیلوبایت است، زیرا هر جزء معمولاً کمتر از 200 بایت وزن دارد. همچنین ممکن است امکان پخش این Consignment در Base58، از طریق کانال‌های مقاوم در برابر سانسور (مانند Nostr یا از طریق Lightning Network، به عنوان مثال)، یا به صورت یک کد QR وجود داشته باشد.


#### ۳) واردات و اعتبارسنجی Contract


هنگامی که Bob، Consignment را دریافت کرد، آن را به RGB Wallet خود وارد می‌کند. این سپس:




- بررسی کنید که Genesis و Schema معتبر هستند؛
- بارگذاری Interface و Interface Implementation؛
- داده‌های سمت مشتری خود را به‌روزرسانی کنید Stash.


اکنون Bob می‌تواند دارایی را در Wallet خود ببیند (حتی اگر هنوز مالک آن نباشد) و بفهمد چه فیلدهایی در دسترس هستند، چه عملیات‌هایی ممکن است... سپس باید با شخصی که واقعاً مالک دارایی است برای انتقال تماس بگیرد. در مثال ما، این شخص Alice است.


فرآیند کشف اینکه چه کسی دارای دارایی RGB است مشابه یافتن یک پرداخت‌کننده Bitcoin است. جزئیات این ارتباط به استفاده (بازارها، کانال‌های چت خصوصی، صدور فاکتور، فروش کالا و خدمات، حقوق و دستمزد...) بستگی دارد.


#### ۴) صدور Invoice


برای شروع انتقال دارایی RGB، ابتدا Bob باید یک Invoice صادر کند. این Invoice برای موارد زیر استفاده می‌شود:




- به Alice نوع عملیات را که باید انجام شود بگویید (برای مثال، یک `انتقال` از یک RGB20 Interface)؛
- Alice را با *Seal Definition* Bob فراهم کنید (یعنی UTXO که او می‌خواهد دارایی را دریافت کند)؛
- مقدار ماده فعال مورد نیاز را مشخص کنید (مثلاً ۱۰۰ واحد).


Bob از ابزار `RGB` در خط فرمان استفاده می‌کند. فرض کنید او 100 واحد از یک token که `ContractId` آن شناخته شده است می‌خواهد، می‌خواهد به `Tapret` تکیه کند و UTXO آن را مشخص می‌کند (`456e3..dfe1:0`):


```bash
bob$ rgb invoice RGB20 100 <ContractId> tapret1st:456e3..dfe1:0
```


در پایان این فصل، نگاهی دقیق‌تر به ساختار فاکتورهای RGB خواهیم داشت.


#### ۵) انتقال Invoice


Invoice تولید شده (به عنوان مثال به صورت URL: `RGB:2WBcas9.../RGB20/100+utxob:...`) شامل تمام اطلاعاتی است که Alice برای آماده‌سازی انتقال نیاز دارد. همانند Consignment، می‌تواند به صورت فشرده (Base58 یا فرمت دیگری) کدگذاری شده و از طریق یک برنامه پیام‌رسان، ایمیل، Nostr... ارسال شود.


![RGB-Bitcoin](assets/fr/076.webp)


#### ۶) آماده‌سازی تراکنش در سمت Alice


Alice دریافت می‌کند Bob's Invoice. در RGB Wallet او، او دارای Stash است که حاوی دارایی برای انتقال است. برای خرج کردن UTXO حاوی دارایی، او ابتدا باید generate یک PSBT (*Partially Signed Bitcoin Transaction*)، یعنی یک تراکنش ناقص Bitcoin، با استفاده از UTXO که دارد:


```bash
alice$ wallet construct tx.psbt
```


این تراکنش پایه (در حال حاضر امضا نشده) برای Anchor رمزنگاری Commitment مرتبط با انتقال به Bob استفاده خواهد شد. UTXO از Alice به این ترتیب خرج خواهد شد و در خروجی، ما `Tapret` یا `Opret` Commitment برای Bob قرار خواهیم داد.


#### 7) تولید انتقال Consignment


سپس، Alice ***Terminal Consignment*** (که گاهی "انتقال Consignment" نامیده می‌شود) را از طریق فرمان می‌سازد:


```bash
alice$ rgb transfer tx.psbt <invoice> consignment.rgb
```


این فایل جدید `Consignment.RGB` شامل:




- تاریخچه کامل انتقالات ایالتی مورد نیاز برای اعتبارسنجی دارایی تا زمان حال (از زمان Genesis)؛
- State Transition جدید که دارایی‌ها را از Alice به Bob منتقل می‌کند، طبق Invoice که Bob صادر کرده است؛
- تراکنش ناقص Bitcoin (*Witness Transaction*) (`tx.PSBT`)، که Single-Use Seal از Alice را خرج می‌کند، به‌گونه‌ای اصلاح شده است که شامل Commitment تا Bob رمزنگاری شود.


در این مرحله، تراکنش هنوز در شبکه Bitcoin پخش نشده است. Consignment بزرگ‌تر از یک Consignment پایه است، زیرا شامل کل تاریخچه (*زنجیره اثبات*) برای اثبات مشروعیت دارایی می‌باشد.


#### ۸) Bob بررسی و پذیرش Consignment را انجام می‌دهد.


Alice این **Terminal Consignment** را به Bob منتقل می‌کند. سپس Bob:




- اعتبار State Transition را بررسی کنید (اطمینان حاصل کنید که تاریخچه سازگار است، قوانین Contract رعایت شده‌اند، و غیره)؛
- به Stash محلی خود اضافه کنید؛
- احتمالاً generate یک امضا (`sig:...`) بر روی Consignment است، برای اثبات اینکه بررسی و تأیید شده است (گاهی اوقات به آن "*فیش حقوقی*" گفته می‌شود).


```bash
bob$ rgb accept consignment.rgb
sig:DbwzvSu4BZU81jEpE9FVZ3xjcyuTKWWy2gmdnaxtACrS
```


![RGB-Bitcoin](assets/fr/077.webp)


#### 9) گزینه: Bob تأییدیه را به Alice ارسال می‌کند (*فیش حقوقی*)


اگر Bob بخواهد، می‌تواند این امضا را به Alice بازگرداند. این نشان می‌دهد:




- که گذار را به عنوان معتبر تشخیص دهد؛
- که او با پخش معامله Bitcoin موافقت کند.


این اجباری نیست، اما می‌تواند به Alice اطمینان دهد که هیچ اختلاف بعدی در مورد انتقال وجود نخواهد داشت.


#### ۱۰) Alice معامله را امضا و منتشر می‌کند


سپس Alice می‌تواند:




- امضای Bob را بررسی کنید (`RGB check <sig>`);
- فرم *Witness Transaction* را امضا کنید که هنوز یک PSBT است (`Wallet sign`);
- Witness Transaction را در شبکه Bitcoin منتشر کنید (`-publish`).


```bash
alice$ rgb check <sig>
alice$ wallet sign —publish tx.psbt
```


![RGB-Bitcoin](assets/fr/078.webp)


پس از تأیید، این تراکنش نشان‌دهنده پایان انتقال است. Bob مالک جدید دارایی می‌شود: او اکنون یک Owned State دارد که به UTXO تحت کنترل او اشاره می‌کند، که با حضور Commitment در تراکنش اثبات می‌شود.


به طور خلاصه، در اینجا فرآیند کامل انتقال آمده است:


![RGB-Bitcoin](assets/fr/079.webp)


### مزایای انتقالات RGB




- محرمانگی**:


فقط Alice و Bob به تمام داده‌های State Transition دسترسی دارند. آنها این اطلاعات را خارج از Blockchain، از طریق محموله‌ها Exchange می‌کنند. تعهدات رمزنگاری در تراکنش Bitcoin نوع دارایی یا مقدار را فاش نمی‌کنند، که محرمانگی بسیار بیشتری نسبت به سایر سیستم‌های On-Chain token تضمین می‌کند.




- اعتبارسنجی سمت مشتری**:


Bob می‌تواند با مقایسه *Consignment* با *لنگرها* در Bitcoin Blockchain، سازگاری انتقال را بررسی کند. او نیازی به تأیید شخص ثالث ندارد. Alice نیازی به انتشار تاریخچه کامل در Blockchain ندارد که این امر بار روی پروتکل پایه را کاهش داده و محرمانگی را بهبود می‌بخشد.




- اتمی بودن ساده‌شده**:


مبادلات پیچیده (مبادلات اتمی بین BTC و یک دارایی RGB، به عنوان مثال) می‌توانند در یک تراکنش واحد انجام شوند، که نیاز به اسکریپت‌های HTLC یا PTLC را از بین می‌برد. اگر توافق‌نامه پخش نشود، همه می‌توانند UTXOهای خود را به روش‌های دیگر استفاده کنند.


### نمودار خلاصه انتقال


قبل از بررسی دقیق‌تر فاکتورها، در اینجا یک نمودار خلاصه از جریان کلی انتقال RGB آمده است:




- Bob نصب یک RGB Wallet را انجام می‌دهد و Contract Consignment اولیه را به دست می‌آورد؛
- Bob یک Invoice صادر می‌کند که در آن به UTXO اشاره شده است که کجا دارایی را دریافت کند؛
- Alice دریافت‌کننده Invoice است، PSBT را می‌سازد و Terminal Consignment را تولید می‌کند؛
- Bob آن را می‌پذیرد، بررسی می‌کند، داده‌ها را به Stash خود اضافه می‌کند و در صورت لزوم (*فیش حقوقی*) را امضا می‌کند؛
- Alice معامله را در شبکه Bitcoin منتشر می‌کند؛
- تأیید تراکنش انتقال را رسمی می‌کند.


![RGB-Bitcoin](assets/fr/080.webp)


انتقال، تمام قدرت و انعطاف‌پذیری پروتکل RGB را نشان می‌دهد: یک Exchange خصوصی، که در سمت مشتری اعتبارسنجی شده، به صورت حداقلی و به‌طور نامحسوس بر روی Bitcoin Blockchain لنگر انداخته و بهترین امنیت پروتکل را حفظ می‌کند (بدون خطر Double-spending). این امر RGB را به یک اکوسیستم امیدوارکننده برای انتقال ارزش تبدیل می‌کند که نسبت به بلاکچین‌های برنامه‌پذیر On-Chain، محرمانه‌تر و مقیاس‌پذیرتر است.


### فاکتورها RGB


در این بخش، ما به‌طور مفصل توضیح خواهیم داد که **فاکتورها** در اکوسیستم RGB چگونه کار می‌کنند و چگونه عملیات (به‌ویژه انتقال‌ها) را با یک Contract امکان‌پذیر می‌سازند. ابتدا به شناسه‌های استفاده‌شده نگاه خواهیم کرد، سپس به نحوه رمزگذاری آن‌ها، و در نهایت به ساختار یک Invoice که به‌صورت یک URL بیان شده است (فرمتی که برای استفاده در کیف پول‌ها به اندازه کافی مناسب است) خواهیم پرداخت.


#### شناسه‌ها و رمزگذاری


یک شناسه منحصر به فرد برای هر یک از موارد زیر Elements تعریف شده است:




- یک RGB Contract؛
- آن Schema (Business Logic) است؛
- Interface و Interface Implementation است؛
- دارایی‌های آن (توکن‌ها، NFT، و غیره)،


این یکتایی بسیار مهم است، زیرا هر جزء از سیستم باید قابل تشخیص باشد. به عنوان مثال، یک Contract X نباید با یک Contract Y دیگر اشتباه گرفته شود، و دو رابط مختلف (مثلاً RGB20 در مقابل RGB21) باید شناسه‌های متمایز داشته باشند.


برای اینکه این شناسه‌ها هم کارآمد (اندازه کوچک) و هم خوانا باشند، ما استفاده می‌کنیم:




- کدگذاری Base58، که از استفاده از کاراکترهای گیج‌کننده (مثلاً `0` و حرف `O`) اجتناب می‌کند و رشته‌های نسبتاً کوتاهی ارائه می‌دهد؛
- پیشوندی که نشان‌دهنده ماهیت شناسه است، معمولاً به شکل `RGB:` یا یک URN مشابه.


به عنوان مثال، یک `ContractId` می‌تواند به صورت زیر نمایش داده شود:


```txt
rgb:2WBcas9-yjzEvGufY-9GEgnyMj7-beMNMWA8r-sPHtV1nPU-TMsGMQX
```


پیشوند `RGB:` تأیید می‌کند که این یک شناسه RGB است و نه یک لینک HTTP یا پروتکل دیگر. به لطف این پیشوند، کیف‌پول‌ها قادر به تفسیر صحیح رشته هستند.


#### بخش‌بندی شناسه


شناسه‌های RGB اغلب بسیار طولانی هستند، زیرا امنیت (رمزنگاری) زیرساخت ممکن است به فیلدهایی با ۲۵۶ بیت یا بیشتر نیاز داشته باشد. برای تسهیل خواندن و تأیید توسط انسان، ما این رشته‌ها را به چندین بلوک تقسیم می‌کنیم که با یک خط تیره (`-`) جدا می‌شوند. بنابراین، به جای داشتن یک رشته طولانی و بدون وقفه از کاراکترها، آن را به بلوک‌های کوتاه‌تر تقسیم می‌کنیم. این روش برای شماره‌های کارت اعتباری یا تلفن رایج است و در اینجا نیز برای سهولت تأیید به کار می‌رود. بنابراین، به عنوان مثال، می‌توان به یک کاربر یا شریک گفت: "*لطفاً بررسی کنید که بلوک سوم `9GEgnyMj7` باشد*" به جای اینکه مجبور باشند کل رشته را یکجا مقایسه کنند. بلوک آخر اغلب به عنوان **چکسام** استفاده می‌شود تا سیستمی برای تشخیص خطا یا اشتباهات تایپی وجود داشته باشد.


به عنوان مثال، یک `ContractId` که به صورت base58 کدگذاری و بخش‌بندی شده است، می‌تواند به این صورت باشد:


```txt
2WBcas9-yjzEvGufY-9GEgnyMj7-beMNMWA8r-sPHtV1nPU-TMsGMQX
```


هر یک از خط تیره‌ها رشته را به بخش‌هایی تقسیم می‌کند. این تنها بر ارائه تأثیر می‌گذارد و بر معنای کد تأثیری ندارد.


#### استفاده از URLها برای فاکتورها


یک RGB Invoice به عنوان یک URL ارائه می‌شود. این بدان معناست که می‌توان روی آن کلیک کرد یا به عنوان یک کد QR اسکن کرد، و یک Wallet می‌تواند به طور مستقیم آن را تفسیر کند تا یک تراکنش را انجام دهد. این سادگی تعامل با برخی دیگر از سیستم‌ها که در آن‌ها باید قطعات مختلف داده را در فیلدهای مختلف نرم‌افزار کپی و جای‌گذاری کنید، متفاوت است.


یک Invoice برای یک token قابل تعویض (مثلاً یک RGB20 token) ممکن است به این شکل باشد:


```txt
rgb:2WBcas9-yjzEvGufY-9GEgnyMj7-beMNMWA8r-sPHtV1nPU-TMsGMQX/RGB20/100+utxob:egXsFnw-5Eud7WKYn-7DVQvcPbc-rR69YmgmG-veacwmUFo-uMFKFb
```


بیایید این URL را تحلیل کنیم:




- `RGB:`** (پیشوند): نشان‌دهنده یک لینک است که پروتکل RGB را فراخوانی می‌کند (مشابه `http:` یا `Bitcoin:` در سایر زمینه‌ها)؛
- `2WBcas9-yjzEvGufY-9GEgnyMj7-beMNMWA8r-sPHtV1nPU-TMsGMQX`**: نشان‌دهنده `ContractId` از token است که می‌خواهید آن را تغییر دهید؛
- `/RGB20/100`**: نشان می‌دهد که `RGB20` Interface استفاده شده و 100 واحد از دارایی درخواست شده است. نحو به این صورت است: `/Interface/amount`;
- `+utxob:`**: مشخص می‌کند که اطلاعات مربوط به گیرنده UTXO (یا به طور دقیق‌تر، تعریف Single-Use Seal) اضافه شده است؛
- `egXsFnw-5Eud7WKYn-7DVQvcPbc-rR69YmgmG-veacwmUFo-uMFKFb`**: این *blinded* UTXO (یا Seal Definition) است. به عبارت دیگر، Bob دقیقاً UTXO خود را مخفی کرده است، بنابراین فرستنده (Alice) نمی‌داند که دقیقاً Address چیست. او فقط می‌داند که یک Seal معتبر وجود دارد که به یک UTXO که توسط Bob کنترل می‌شود، اشاره دارد.


این واقعیت که همه چیز در یک URL واحد جا می‌گیرد، زندگی را برای کاربر آسان‌تر می‌کند: یک کلیک یا اسکن ساده در Wallet، و عملیات آماده اجرا است.


می‌توان سیستم‌هایی را تصور کرد که در آن‌ها به جای `ContractId` از یک تیکر ساده (مثلاً `USDT`) استفاده شود. با این حال، این امر مشکلات عمده‌ای از نظر اعتماد و امنیت ایجاد می‌کند: یک تیکر مرجع یکتایی نیست (چندین قرارداد می‌توانند ادعا کنند که `USDT` نامیده می‌شوند). با RGB، ما یک شناسه رمزنگاری یکتا و بدون ابهام می‌خواهیم. از این رو، پذیرش رشته ۲۵۶ بیتی که به صورت base58 کدگذاری و بخش‌بندی شده است. کاربر می‌داند که دقیقاً در حال کار با Contract است که شناسه آن `2WBcas9-yjz...` است و نه هیچ چیز دیگری.


#### پارامترهای اضافی URL


می‌توانید پارامترهای اضافی را نیز به URL اضافه کنید، به همان روشی که با HTTP انجام می‌دهید، مانند:


```txt
rgb:2WBcas9-yjzEvGufY-9GEgnyMj7-beMNMWA8r-sPHtV1nPU-TMsGMQX/RGB20/100+utxob:egXsFnw-5Eud7WKYn-7DVQvcPbc-rR69YmgmG-veacwmUFo-uMFKFb?sig=6kzbKKffP6xftkxn9UP8gWqiC41W16wYKE5CYaVhmEve
```




- `?sig=...`: نشان‌دهنده، برای مثال، امضایی مرتبط با Invoice است که برخی کیف‌پول‌ها می‌توانند آن را تأیید کنند؛
- اگر یک Wallet این امضا را مدیریت نکند، به سادگی این پارامتر را نادیده می‌گیرد.


بیایید به مورد یک NFT از طریق RGB21 Interface بپردازیم. به عنوان مثال، می‌توانیم داشته باشیم:


```txt
rgb:7BKsac8-beMNMWA8r-3GEprtFh7-bjzEvGufY-aNLuU4nSN-MRsLOIK/RGB21/DbwzvSu-4BZU81jEp-E9FVZ3xj-cyuTKWWy-2gmdnaxt-ACrS+utxob:egXsFnw-5Eud7WKYn-7DVQvcPbc-rR69YmgmG-veacwmUFo-uMFKFb
```


اینجا می‌بینیم:




- `RGB:`**: پیشوند URL;
- `7BKsac8-beMNMWA8r-3GEprtFh7-bjzEvGufY-aNLuU4nSN-MRsLOIK`**: Contract ID (NFT);
- rGB21**: Interface برای دارایی‌های غیرقابل تعویض (NFT)؛
- `DbwzvSu-4BZU81jEp-...`**: یک مرجع صریح به بخش منحصر به فرد NFT، به عنوان مثال یک Hash از داده‌های blob (رسانه، فراداده...);
- `+utxob:egXsFnw-...`**: the Seal Definition.


ایده همان است: ارسال یک لینک منحصر به فرد که Wallet می‌تواند تفسیر کند و به وضوح دارایی منحصر به فردی که باید منتقل شود را شناسایی کند.


#### عملیات دیگر از طریق URL


RGB URLها فقط برای درخواست انتقال استفاده نمی‌شوند. آن‌ها می‌توانند عملیات پیشرفته‌تری مانند صدور توکن‌های جدید (*صدور*) را نیز رمزگذاری کنند. برای مثال:


```txt
rgb:2WBcas9-yjzEvGufY-9GEgnyMj7-beMNMWA8r-sPHtV1nPU-TMsGMQX/RGB20/issue/100000+utxob:egXsFnw-5Eud7WKYn-7DVQvcPbc-rR69YmgmG-veacwmUFo-uMFKFb
```


اینجا پیدا می‌کنیم:




- `RGB:`: پروتکل;
- `2WBcas9-...`: Contract ID;
- `/RGB20/issue/100000`: نشان می‌دهد که می‌خواهید انتقال "*Issue*" را برای ایجاد 100,000 توکن اضافی فراخوانی کنید؛
- `+utxob:`: Seal Definition.


به عنوان مثال، Wallet ممکن است بخواند: "از من خواسته شده است که یک عملیات `issue` را از `RGB20` Interface، بر روی فلان و بهمان Contract، برای 100,000 واحد، به نفع فلان و بهمان Single-Use Seal.*"


حالا که به برنامه‌نویسی اصلی Elements از RGB نگاه کرده‌ایم، شما را به فصل بعدی می‌برم که در مورد چگونگی ترسیم یک RGB Contract است.


## پیش‌نویس قراردادهای هوشمند


<chapterId>0e0a645c-0049-588d-8965-b8c536590cc9</chapterId>


:::video id=a3ad6dcd-90b8-4272-9dfc-76c85c859167:::


در این فصل، ما به صورت گام‌به‌گام به نوشتن یک Contract خواهیم پرداخت، با استفاده از ابزار خط فرمان `RGB`. هدف این است که نشان دهیم چگونه CLI را نصب و مدیریت کنیم، یک **Schema** را کامپایل کنیم، **Interface** و **Interface Implementation** را وارد کنیم، سپس یک دارایی را صادر کنیم (*issue*). همچنین به منطق زیربنایی، شامل کامپایل و اعتبارسنجی وضعیت، خواهیم پرداخت. تا پایان این فصل، شما باید قادر باشید این فرآیند را بازتولید کرده و قراردادهای RGB خود را ایجاد کنید.


به عنوان یک یادآوری، منطق داخلی RGB بر اساس کتابخانه‌های Rust است که شما به عنوان توسعه‌دهندگان می‌توانید آن‌ها را به پروژه‌های خود وارد کنید تا بخش Client-side Validation را مدیریت کنید. علاوه بر این، تیم انجمن LNP/BP در حال کار بر روی بایندینگ‌ها برای زبان‌های دیگر است، اما این هنوز نهایی نشده است. علاوه بر این، نهادهای دیگری مانند Bitfinex در حال توسعه استک‌های یکپارچه‌سازی خود هستند (در دو فصل آخر دوره درباره این‌ها صحبت خواهیم کرد). بنابراین، در حال حاضر، `RGB` CLI مرجع رسمی است، حتی اگر نسبتاً ناپولیش باقی بماند.


### نصب و ارائه ابزار RGB


دستور اصلی به سادگی `RGB` نامیده می‌شود. این طراحی شده است تا یادآور `git` باشد، با مجموعه‌ای از زیر-دستورات برای دستکاری قراردادها، فراخوانی آن‌ها، صدور دارایی‌ها و غیره. Bitcoin Wallet در حال حاضر یکپارچه نشده است، اما در نسخه قریب‌الوقوع (0.11) خواهد بود. این نسخه بعدی به کاربران امکان می‌دهد تا کیف پول‌های خود را (از طریق توصیف‌گرها) مستقیماً از `RGB` ایجاد و مدیریت کنند، از جمله تولید PSBT، سازگاری با سخت‌افزار خارجی (مثلاً یک Hardware Wallet) برای امضا، و قابلیت همکاری با نرم‌افزارهایی مانند Sparrow. این امر سناریوی صدور و انتقال دارایی را به‌طور کامل ساده می‌کند.


#### نصب از طریق Cargo


ما ابزار را در Rust نصب می‌کنیم با:


```bash
cargo install rgb-contracts --all-features
```


(توجه: جعبه به نام `RGB-contracts` نامیده می‌شود و فرمان نصب شده به نام `RGB` خواهد بود. اگر جعبه‌ای به نام `RGB` قبلاً وجود داشت، ممکن بود تداخلی رخ دهد، به همین دلیل این نام انتخاب شده است)


نصب تعداد زیادی از وابستگی‌ها را کامپایل می‌کند (به عنوان مثال، تجزیه فرمان، یکپارچه‌سازی Electrum، مدیریت اثبات‌های دانش صفر، و غیره).


پس از اتمام نصب،:


```bash
rgb
```


اجرای `RGB` (بدون آرگومان) لیستی از زیر-دستورات موجود را نمایش می‌دهد، مانند `interfaces`، `Schema`، `import`، `export`، `issue`، `Invoice`، `transfer` و غیره. شما می‌توانید دایرکتوری ذخیره‌سازی محلی (یک Stash که تمامی لاگ‌ها، شماتیک‌ها و پیاده‌سازی‌ها را نگه می‌دارد) را تغییر دهید، شبکه را انتخاب کنید (Testnet، Mainnet) یا سرور Electrum خود را پیکربندی کنید.


![RGB-Bitcoin](assets/fr/081.webp)


#### اولین مرور کنترل‌ها


هنگامی که فرمان زیر را اجرا می‌کنید، خواهید دید که یک `RGB20` Interface به‌طور پیش‌فرض از قبل یکپارچه شده است:


```bash
rgb interfaces
```


اگر این Interface یکپارچه نشده است، کلون کنید:


```bash
git clone https://github.com/RGB-WG/rgb-interfaces
```


ترکیبش کن:


```bash
cargo run
```


سپس Interface مورد نظر خود را وارد کنید:


```bash
rgb import interfaces/RGB20.rgb
```


![RGB-Bitcoin](assets/fr/082.webp)


از سوی دیگر، به ما گفته شده است که هنوز هیچ Schema به نرم‌افزار وارد نشده است. همچنین هیچ Contract در Stash وجود ندارد. برای مشاهده آن، فرمان زیر را اجرا کنید:


```bash
rgb schemata
```


سپس می‌توانید مخزن را کلون کنید تا برخی از شماتیک‌ها را بازیابی کنید:


```bash
git clone https://github.com/RGB-WG/rgb-schemata
```


![RGB-Bitcoin](assets/fr/083.webp)


این مخزن شامل چندین فایل Rust در دایرکتوری `src/` خود است (برای مثال `nia.rs`) که طرح‌واره‌ها را تعریف می‌کنند (NIA برای "*دارایی غیرقابل تورم*", UDA برای "*دارایی دیجیتال منحصر به فرد*" و غیره). برای کامپایل، می‌توانید سپس اجرا کنید:


```bash
cd rgb-schemata
cargo run
```


این چندین فایل `.RGB` و `.rgba` تولید می‌کند که با طرح‌های کامپایل شده مطابقت دارند. به عنوان مثال، شما `NonInflatableAsset.RGB` را پیدا خواهید کرد.


#### واردات Schema و Interface Implementation


اکنون می‌توانید شماتیک را به `RGB` وارد کنید:


```bash
rgb import schemata/NonInflatableAssets.rgb
```


![RGB-Bitcoin](assets/fr/084.webp)


این آن را به Stash محلی اضافه می‌کند. اگر فرمان زیر را اجرا کنیم، می‌بینیم که اکنون Schema ظاهر می‌شود:


```bash
rgb schemata
```


### Contract ایجاد (صدور)


دو رویکرد برای ایجاد یک دارایی جدید وجود دارد:




- یا از یک اسکریپت یا کدی در Rust استفاده می‌کنیم که یک Contract را با پر کردن فیلدهای Schema (Global State، Owned States، و غیره) می‌سازد و یک فایل `.RGB` یا `.rgba` تولید می‌کند؛
- یا از زیر فرمان `issue` به طور مستقیم استفاده کنید، با یک فایل YAML (یا TOML) که ویژگی‌های token را توصیف می‌کند.


می‌توانید مثال‌ها را در Rust در پوشه `examples` پیدا کنید، که نشان می‌دهند چگونه یک `ContractBuilder` بسازید، `Global State` (نام دارایی، نماد، Supply، تاریخ، و غیره) را پر کنید، Owned State را تعریف کنید (که به کدام UTXO اختصاص داده شده است)، سپس همه این‌ها را به یک *Contract Consignment* تبدیل کنید که می‌توانید آن را صادر، اعتبارسنجی و به یک Stash وارد کنید.


راه دیگر این است که به صورت دستی یک فایل YAML را ویرایش کنید تا `ticker`، `name`، `Supply` و غیره را سفارشی کنید. فرض کنید فایل `RGB20-demo.yaml` نام دارد. شما می‌توانید مشخص کنید:




- `spec`: نماد، نام، دقت;
- `terms`: یک فیلد برای اطلاعیه‌های قانونی؛
- `issuedSupply`: مقدار token صادر شده؛
- `assignments`: نشان‌دهنده Single-Use Seal (*Seal Definition*) و مقدار آزاد شده است.


در اینجا یک مثال از یک فایل YAML برای ایجاد آمده است:


```yaml
interface: RGB20Fixed

globals:
spec:
ticker: PBN
name: Plan B Network
details: "Pay attention: the asset has no value"
precision: 2
terms:
text: >
SUBJECT TO, AND WITHOUT IN ANY WAY LIMITING, THE REPRESENTATIONS AND WARRANTIES OF ANY SELLER. PROPERTY IS BEING SOLD “AS IS”...
media: ~
issuedSupply: 100000000

assignments:
assetOwner:
seal: tapret1st:b449f7eaa3f98c145b27ad0eeb7b5679ceb567faef7a52479bc995792b65f804:1
amount: 100000000 # this is 1 million (we have two digits for cents)
```


![RGB-Bitcoin](assets/fr/085.webp)


سپس به سادگی فرمان را اجرا کنید:


```bash
rgb issue '<SchemaID>' ssi:<Issuer> rgb20-demo.yaml
```


![RGB-Bitcoin](assets/fr/086.webp)


در مورد من، شناسه منحصر به فرد Schema (که باید در گیومه تک قرار گیرد) 'RDYhMTR!9gv8Y2GLv9UNBEK1hcrCmdLDFk9Qd5fnO8k' است و من هیچ صادرکننده‌ای قرار نداده‌ام. بنابراین سفارش من به این صورت است:


```txt
rgb issue 'RDYhMTR!9gv8Y2GLv9UNBEK1hcrCmdLDFk9Qd5fnO8k' ssi:anonymous rgb20-demo.yaml
```


اگر شناسه Schema را نمی‌دانید، فرمان زیر را اجرا کنید:


```bash
rgb schemata
```


CLI پاسخ می‌دهد که یک Contract جدید صادر شده و به Stash اضافه شده است. اگر فرمان زیر را تایپ کنیم، می‌بینیم که اکنون یک Contract اضافی وجود دارد که مربوط به همان تازه صادر شده است:


```bash
rgb contracts
```


![RGB-Bitcoin](assets/fr/087.webp)


سپس، فرمان بعدی وضعیت‌های جهانی (نام، نماد، Supply...) و لیست وضعیت‌های مالکیت شده، یعنی تخصیص‌ها (برای مثال، 1 میلیون توکن `PBN` تعریف شده در UTXO `b449f7eaa3f98c145b27ad0eeb7b5679ceb567faef7a52479bc995792b65f804:1`) را نمایش می‌دهد.


```bash
rgb state '<ContractId>'
```


![RGB-Bitcoin](assets/fr/088.webp)


### صادرات، واردات و اعتبارسنجی


برای به اشتراک‌گذاری این Contract با کاربران دیگر، می‌توان آن را از Stash به یک:


```bash
rgb export '<ContractId>' myContractPBN.rgb
```


![RGB-Bitcoin](assets/fr/089.webp)


فایل `myContractPBN.RGB` می‌تواند به کاربر دیگری منتقل شود، که می‌تواند آن را با فرمان زیر به Stash خود اضافه کند:


```bash
rgb import myContractPBN.rgb
```


در هنگام واردات، اگر یک *Contract Consignment* ساده باشد، پیغام "`Importing Consignment RGB`" را دریافت خواهیم کرد. اگر یک *State Transition Consignment* بزرگتر باشد، دستور متفاوت خواهد بود (`RGB accept`).


برای اطمینان از اعتبار، می‌توانید از تابع اعتبارسنجی محلی نیز استفاده کنید. به عنوان مثال، می‌توانید اجرا کنید:


```bash
rgb validate myContract.rgb
```


#### استفاده، تأیید و نمایش Stash


به عنوان یادآوری، Stash یک موجودی محلی از شِماها، رابط‌ها، پیاده‌سازی‌ها و قراردادها است (Genesis + انتقال‌ها). هر بار که "import" را اجرا می‌کنید، یک عنصر به Stash اضافه می‌کنید. این Stash را می‌توان با فرمان زیر به‌طور دقیق مشاهده کرد:


```bash
rgb dump
```


![RGB-Bitcoin](assets/fr/090.webp)


این generate پوشه‌ای با جزئیات کل Stash خواهد بود.


### انتقال و PSBT


برای انجام یک انتقال، باید یک Bitcoin Wallet محلی را برای مدیریت تعهدات `Tapret` یا `Opret` دستکاری کنید.


#### generate و Invoice


در اکثر موارد، تعامل بین شرکت‌کنندگان در یک Contract (مثلاً Alice و Bob) از طریق تولید یک Invoice صورت می‌گیرد. اگر Alice بخواهد Bob چیزی را اجرا کند (انتقال token، صدور مجدد، یک اقدام در یک DAO، و غیره)، Alice یک Invoice ایجاد می‌کند که دستورالعمل‌های خود را به Bob ارائه می‌دهد. بنابراین ما داریم:




- Alice** (صادرکننده Invoice)؛
- Bob** (که Invoice را دریافت و اجرا می‌کند).


برخلاف سایر اکوسیستم‌ها، یک RGB Invoice محدود به مفهوم پرداخت نیست. می‌تواند هر درخواستی که به Contract مرتبط است را در بر گیرد: لغو یک کلید، رأی‌گیری، ایجاد یک حکاکی (*حکاکی*) بر روی یک NFT، و غیره. عملیات مربوطه می‌تواند در Contract Interface توصیف شود. عملیات مربوطه می‌تواند در Contract Interface توصیف شود.


دستور زیر یک RGB Invoice تولید می‌کند:


```bash
$ rgb invoice $CONTRACT -i $INTERFACE $ACTION $STATE $SEAL
```


با:




- `$Contract`: شناسه Contract (*ContractId*);
- `$Interface`: Interface مورد استفاده قرار گیرد (به عنوان مثال `RGB20`);
- `$ACTION`: نام عملیات مشخص شده در Interface (برای انتقال ساده قابل تعویض token، این می‌تواند "انتقال" باشد). اگر Interface از قبل یک عملیات پیش‌فرض ارائه می‌دهد، نیازی به وارد کردن مجدد آن در اینجا نیست؛
- `$STATE`: داده وضعیت برای انتقال (برای مثال، مقدار توکن‌ها اگر یک token قابل تعویض منتقل شود)؛
- `$Seal`: Single-Use Seal (Alice) ذینفع، یعنی یک مرجع صریح به UTXO. Bob از این اطلاعات برای ساخت Witness Transaction استفاده خواهد کرد و خروجی مربوطه سپس به Alice تعلق خواهد داشت (در قالب *blinded UTXO* یا به صورت رمزنگاری نشده).


به عنوان مثال، با دستورات زیر


```bash
alice$ CONTRACT='iZgIN9EL-2H21UgQ-x!A3uJc-WwXhCSm-$9Lwcc1-v!mUkKY'
alice$ MY_UTXO=4960acc21c175c551af84114541eace09c14d3a1bb184809f7b80916f57f9ef8:1
alice$ rgb invoice $CONTRACT -i RGB20 --amount 100 $MY_UTXO
```


CLI به generate یک Invoice مانند:


```bash
rgb:iZgIN9EL-2H21UgQ-x!A3uJc-WwXhCSm-$9Lwcc1-v!mUkKY/RGB20/100+utxob:zlVS28Rb-...
```


می‌توان آن را از طریق هر کانالی (متن، کد QR، و غیره) به Bob منتقل کرد.


#### در حال انجام انتقال


برای انتقال از این Invoice:




- Bob (که توکن‌ها را در Stash خود نگه می‌دارد) دارای یک Bitcoin Wallet است. او نیاز دارد یک تراکنش Bitcoin (به صورت یک PSBT، مثلاً `tx.PSBT`) آماده کند که UTXOهایی را که توکن‌های مورد نیاز RGB در آن‌ها قرار دارند، به‌علاوه یک UTXO برای ارز (Exchange) خرج کند؛
- Bob فرمان زیر را اجرا می‌کند:


```bash
bob$ rgb transfer tx.psbt $INVOICE consignment.rgb
```




- این یک فایل `Consignment.RGB` تولید می‌کند که شامل:
 - تاریخچه انتقال که به Alice اثبات می‌کند که توکن‌ها معتبر هستند؛
 - انتقال جدیدی که توکن‌ها را به Alice منتقل می‌کند؛ Single-Use Seal؛
 - Witness Transaction (امضا نشده).
- Bob این فایل `Consignment.RGB` را به Alice ارسال می‌کند (از طریق ایمیل، یک سرور اشتراک‌گذاری یا پروتکل RGB-RPC، Storm، و غیره)؛
- Alice دریافت می‌کند `Consignment.RGB` و آن را در Stash خود می‌پذیرد:


```bash
alice$ rgb accept consignment.rgb
```




- CLI اعتبار انتقال را بررسی می‌کند و آن را به Stash از Alice اضافه می‌کند. اگر نامعتبر باشد، فرمان با پیام‌های خطای دقیق شکست می‌خورد. در غیر این صورت، موفق می‌شود و گزارش می‌دهد که تراکنش نمونه هنوز در شبکه Bitcoin پخش نشده است (Bob منتظر نور Green از Alice است)؛
- به‌عنوان تأیید، فرمان `accept` یک امضا (*فیش حقوقی*) برمی‌گرداند که Alice می‌تواند به Bob ارسال کند تا به او نشان دهد که او *Consignment* را تأیید کرده است؛
- سپس Bob می‌تواند تراکنش Bitcoin خود را امضا و منتشر کند (`--publish`):


```bash
bob$ rgb check <sig> && wallet sign --publish tx.psbt
```




- به محض تأیید این تراکنش On-Chain، Ownership دارایی به Alice منتقل شده تلقی می‌شود. Wallet متعلق به Alice، که Mining تراکنش را نظارت می‌کند، مشاهده می‌کند که Owned State جدید در Stash آن ظاهر می‌شود.


در فصل بعدی، نگاهی دقیق‌تر به یکپارچه‌سازی RGB با Lightning Network خواهیم داشت.


## RGB بر روی Lightning Network


<chapterId>0962980a-8f94-5d0f-9cd0-43d7f884a01d</chapterId>


:::video id=be25a165-6e23-488c-91d8-3dcfccc6eca1:::


در این فصل، قصد دارم بررسی کنم که چگونه می‌توان از RGB درون Lightning Network استفاده کرد تا دارایی‌های RGB (توکن‌ها، NFTها و غیره) را از طریق کانال‌های پرداخت off-chain یکپارچه و منتقل کرد.


ایده اصلی این است که RGB State Transition (*State Transition*) می‌تواند به یک تراکنش Bitcoin متعهد شود که به نوبه خود می‌تواند تا زمانی که کانال Lightning بسته شود، off-chain باقی بماند. بنابراین، هر بار که کانال به‌روزرسانی می‌شود، یک RGB State Transition جدید می‌تواند به تراکنش تعهدی جدید اضافه شود که در نتیجه انتقال قدیمی را باطل می‌کند. به این ترتیب، کانال‌های Lightning می‌توانند برای انتقال دارایی‌های RGB استفاده شوند و می‌توانند به همان روشی که پرداخت‌های معمولی Lightning مسیریابی می‌شوند، هدایت شوند.


### ایجاد و تأمین مالی کانال


برای ایجاد یک کانال Lightning که دارایی‌های RGB را حمل می‌کند، به دو Elements نیاز داریم:




- تأمین مالی Bitcoin برای ایجاد کانال 2/2 Multisig (UTXO پایه برای کانال)؛
- تأمین مالی RGB، که دارایی‌ها را به همان Multisig ارسال می‌کند.


در شرایط Bitcoin، تراکنش تأمین مالی باید وجود داشته باشد تا مرجع UTXO تعریف شود، حتی اگر فقط حاوی مقدار کمی از Sats باشد (این فقط مسئله‌ای است که هر خروجی در تراکنش‌های آینده Commitment بالاتر از حد Dust باقی بماند). به عنوان مثال، ممکن است Alice تصمیم بگیرد 10k Sats و 500 USDT (که به عنوان یک دارایی RGB صادر شده است) ارائه دهد. در تراکنش تأمین مالی، ما یک Commitment (`Opret` یا `Tapret`) اضافه می‌کنیم که RGB State Transition را لنگر می‌اندازد.


![RGB-Bitcoin](assets/fr/091.webp)


هنگامی که تراکنش تأمین مالی آماده شد (اما هنوز پخش نشده است)، تراکنش‌های Commitment ایجاد می‌شوند تا هر یک از طرفین بتوانند کانال را به صورت یک‌جانبه در هر زمان ببندند. این تراکنش‌ها شبیه به تراکنش‌های کلاسیک Commitment لایتنینگ هستند، با این تفاوت که ما یک خروجی اضافی حاوی RGB Anchor (OP_RETURN یا Taproot) مرتبط با State Transition جدید اضافه می‌کنیم.


سپس RGB State Transition دارایی‌ها را از 2/2 Multisig تأمین مالی به خروجی‌های Commitment Transaction منتقل می‌کند. مزیت این فرآیند این است که امنیت حالت RGB دقیقاً با مکانیک‌های تنبیهی Lightning مطابقت دارد: اگر Bob یک حالت کانال قدیمی را پخش کند، Alice می‌تواند او را تنبیه کند و خروجی را خرج کند تا هم توکن‌های Sats و هم RGB را بازیابی کند. بنابراین انگیزه حتی قوی‌تر از یک کانال Lightning بدون دارایی‌های RGB است، زیرا یک مهاجم می‌تواند نه تنها Sats، بلکه دارایی‌های RGB کانال را نیز از دست بدهد.


یک Commitment Transaction که توسط Alice امضا شده و به Bob ارسال شده است، بنابراین به این شکل خواهد بود:


![RGB-Bitcoin](assets/fr/092.webp)


و Commitment Transaction همراه، که توسط Bob امضا شده و به Alice ارسال شده است، به این شکل خواهد بود:


![RGB-Bitcoin](assets/fr/093.webp)


### به‌روزرسانی کانال


هنگامی که یک پرداخت بین دو شرکت‌کننده کانال انجام می‌شود (یا آنها می‌خواهند تخصیص دارایی را تغییر دهند)، یک جفت جدید از تراکنش‌های Commitment ایجاد می‌کنند. مقدار در Sats در هر خروجی ممکن است بسته به پیاده‌سازی تغییر نکند یا تغییر کند، زیرا نقش اصلی آن امکان‌پذیر ساختن ساخت UTXOهای معتبر است. از سوی دیگر، خروجی OP_RETURN (یا Taproot) باید تغییر کند تا شامل RGB Anchor جدید باشد که نمایانگر توزیع جدید دارایی‌ها در کانال است.


به عنوان مثال، اگر Alice مبلغ 30 USDT را به Bob در کانال منتقل کند، State Transition جدید موجودی 400 USDT برای Alice و 100 USDT برای Bob را نشان خواهد داد. تراکنش تعهد به OP_RETURN/Taproot Anchor اضافه می‌شود (یا توسط آن تغییر می‌یابد) تا این انتقال را شامل شود. توجه داشته باشید که از دیدگاه RGB، ورودی به انتقال همان Multisig اولیه باقی می‌ماند (جایی که دارایی‌های On-Chain واقعاً تخصیص داده می‌شوند تا زمانی که کانال بسته شود). تنها خروجی‌های RGB (تخصیص‌ها) تغییر می‌کنند، بسته به توزیع مجددی که تصمیم‌گیری شده است.


Commitment Transaction امضا شده توسط Alice، آماده برای توزیع توسط Bob:


![RGB-Bitcoin](assets/fr/094.webp)


Commitment Transaction امضا شده توسط Bob، آماده برای توزیع توسط Alice:


![RGB-Bitcoin](assets/fr/095.webp)


### مدیریت HTLC


در واقع، Lightning Network امکان مسیریابی پرداخت‌ها از طریق کانال‌های متعدد را با استفاده از HTLCs (*قراردادهای قفل زمانی هش شده*) فراهم می‌کند. همین امر در مورد RGB نیز صدق می‌کند: برای هر پرداختی که از طریق کانال در حال انتقال است، یک خروجی HTLC به تراکنش تعهدی اضافه می‌شود و یک تخصیص RGB به این HTLC مرتبط می‌شود. بنابراین، هر کسی که خروجی HTLC را خرج کند (به لطف راز یا پس از انقضای قفل زمانی) هر دو دارایی Sats و RGB مرتبط را بازیابی می‌کند. از سوی دیگر، بدیهی است که باید به اندازه کافی پول نقد در جاده به لحاظ دارایی‌های Sats و RGB داشته باشید.


![RGB-Bitcoin](assets/fr/096.webp)


عملیات RGB بر روی Lightning باید به موازات عملیات خود Lightning Network در نظر گرفته شود. اگر مایلید به این موضوع بیشتر بپردازید، به شدت توصیه می‌کنم به این دوره آموزشی جامع دیگر نگاهی بیندازید:


https://planb.network/courses/34bd43ef-6683-4a5c-b239-7cb1e40a4aeb

### نقشه کد RGB


در نهایت، قبل از اینکه به بخش بعدی برویم، می‌خواهم یک نمای کلی از کدی که در RGB استفاده شده است به شما بدهم. این پروتکل بر اساس مجموعه‌ای از کتابخانه‌های Rust و مشخصات منبع باز است. در اینجا یک نمای کلی از مخازن و جعبه‌های اصلی آمده است:


![RGB-Bitcoin](assets/fr/097.webp)


#### Client-side Validation




- مخزن**: [client_side_validation](https://github.com/LNP-BP/client_side_validation)
- Crates**: [client_side_validation](https://crates.io/crates/client_side_validation)، [single_use_seals](https://crates.io/crates/single_use_seals)


مدیریت اعتبارسنجی off-chain و منطق مهرهای یکبار مصرف.


#### تعهدات قطعی Bitcoin (DBC)




- مخزن**: [bp-core](https://github.com/BP-WG/bp-core)
- جعبه**: [bp-dbc](https://crates.io/crates/bp-dbc)


مدیریت لنگر اندازی قطعی در تراکنش‌های Bitcoin (Tapret، OP_RETURN، و غیره).


#### Multi Protocol Commitment (MPC)




- مخزن**: [client_side_validation](https://github.com/LNP-BP/client_side_validation)
- جعبه**: [commit_verify](https://crates.io/crates/commit_verify)


ترکیب‌های مختلف تعامل و یکپارچگی با پروتکل‌های مختلف.


#### انواع سختگیرانه و رمزگذاری سختگیرانه




- مشخصات**: [website strict-types.org](https://www.strict-types.org/)
- مخازن**: [strict-types](https://github.com/strict-types/strict-types)، [strict-encoding](https://github.com/strict-types/strict-encoding)
- Crates**: [strict_types](https://crates.io/crates/strict_types)، [strict_encoding](https://crates.io/crates/strict_encoding)


سیستم تایپینگ سختگیرانه و سریال‌سازی تعیین‌کننده استفاده شده برای Client-side Validation.


#### RGB هسته




- مخزن**: [RGB-core](https://github.com/RGB-WG/RGB-core)
- Crate**: [RGB-core](https://crates.io/crates/RGB-core)


هسته پروتکل که منطق اصلی اعتبارسنجی RGB را در بر می‌گیرد.


#### کتابخانه استاندارد RGB & Wallet




- مخزن**: [RGB-std](https://github.com/RGB-WG/RGB-std)
- جعبه**: [RGB-std](https://crates.io/crates/RGB-std)


استاندارد پیاده‌سازی‌ها، مدیریت Stash و Wallet.


#### RGB CLI




- مخزن**: [RGB](https://github.com/RGB-WG/RGB)
- Crates**: [RGB-CLI](https://crates.io/crates/RGB-CLI), [RGB-Wallet](https://crates.io/crates/RGB-Wallet)


`RGB` CLI و جعبه Wallet، برای دستکاری قراردادها از طریق خط فرمان.


#### RGB Schema




- مخزن**: [RGB-schemata](https://github.com/RGB-WG/RGB-schemata/)


شامل مثال‌هایی از طرح‌واره‌ها (NIA، UDA، و غیره) و پیاده‌سازی‌های آن‌ها.


#### GW-۲۱۹۵




- اطلاعات**: [AluVM.org](https://www.AluVM.org/)
- مخازن**: [AluVM-spec](https://github.com/AluVM/AluVM-spec)، [alure](https://github.com/AluVM/alure)
- Crates**: [AluVM](https://crates.io/crates/AluVM), [aluasm](https://crates.io/crates/aluasm)


ماشین مجازی مبتنی بر رجیستری که برای اجرای اسکریپت‌های اعتبارسنجی استفاده می‌شود.


#### پروتکل Bitcoin - BP




- مخازن**: [bp-core](https://github.com/BP-WG/bp-core)، [bp-std](https://github.com/BP-WG/bp-std)، [bp-Wallet](https://github.com/BP-WG/bp-Wallet)


افزونه‌ها برای پشتیبانی از پروتکل Bitcoin (تراکنش‌ها، دور زدن‌ها، و غیره).


#### رایانش قطعی فراگیر - UBIDECO




- مخزن**: [UBIDECO](https://github.com/UBIDECO)


اکوسیستم مرتبط با توسعه‌های قطعی متن‌باز.


# ساخت بر روی RGB


<partId>3b4b0d66-0c1b-505a-b5ca-4b2e57dd73c2</partId>


## DIBA و پروژه Bitmask


<chapterId>dc92a5e8-ed93-5a3f-bcd0-d433932842f4</chapterId>


:::video id=2ec9a181-a8b0-4da2-b7b5-9dfaaaeb10ba:::


بخش نهایی این دوره بر اساس ارائه‌هایی است که توسط سخنرانان مختلف در بوت‌کمپ RGB انجام شده است. این بخش شامل نظرات و بازتاب‌هایی درباره RGB و اکوسیستم آن، و همچنین ارائه ابزارها و پروژه‌های مبتنی بر پروتکل می‌باشد. فصل اول توسط هانتر بیست مدیریت می‌شود و دو فصل بعدی توسط فردریکو تنگا.


### از جاوااسکریپت به Rust، و به اکوسیستم Bitcoin


در ابتدا، هانتر بیست عمدتاً با جاوااسکریپت کار می‌کرد. سپس او **Rust** را کشف کرد که در ابتدا نحو آن ناخوشایند و ناامیدکننده به نظر می‌رسید. با این حال، او به قدرت این زبان، کنترل بر حافظه (*heap* و *stack*)، و امنیت و عملکردی که با آن همراه است، پی برد. او تأکید می‌کند که Rust یک زمینه آموزشی عالی برای درک عمیق از نحوه کارکرد یک کامپیوتر است.


هانتر بیست پیشینه خود را در پروژه‌های مختلف در اکوسیستم *Altcoin* مانند اتریوم (با Solidity، TypeScript و غیره) و بعداً فایل‌کوین بازگو می‌کند. او توضیح می‌دهد که در ابتدا تحت تأثیر برخی از پروتکل‌ها قرار گرفته بود، اما در نهایت به دلیل توکنومیک آنها، از بیشتر آنها ناامید شد. او انگیزه‌های مالی مشکوک، ایجاد تورمی توکن‌ها که سرمایه‌گذاران را رقیق می‌کند و جنبه بالقوه استثماری این پروژه‌ها را محکوم می‌کند. بنابراین او در نهایت موضع **Bitcoin Maximalist** را اتخاذ کرد، نه تنها به این دلیل که برخی افراد چشمان او را به مکانیسم‌های اقتصادی محکم‌تر Bitcoin و به استحکام این سیستم باز کردند.


### جذابیت RGB و ساخت بر روی لایه‌ها


آنچه که او را به طور قطعی به اهمیت Bitcoin متقاعد کرد، به گفته خودش، کشف RGB و مفهوم لایه‌ها بود. او معتقد است که قابلیت‌های موجود در بلاکچین‌های دیگر می‌توانند در لایه‌های بالاتر، بالای Bitcoin، بدون تغییر در پروتکل پایه، بازتولید شوند.


در فوریه 2022، او به **DIBA** پیوست تا به طور خاص بر روی RGB کار کند و به ویژه بر روی **Bitmask** Wallet. در آن زمان، Bitmask هنوز در نسخه 0.01 بود و RGB را در نسخه 0.4 اجرا می‌کرد، فقط برای مدیریت توکن‌های منفرد. او اشاره می‌کند که این کمتر به خود حضانتی متمرکز بود نسبت به امروز، زیرا منطق تا حدی مبتنی بر سرور بود. از آن زمان، معماری به سمت این مدل تکامل یافته است که بسیار مورد توجه بیت‌کوینرها قرار گرفته است.


### پایه‌های پروتکل RGB


پروتکل **RGB** جدیدترین و پیشرفته‌ترین تجسم مفهوم _colored coins_ است که قبلاً در حدود سال‌های ۲۰۱۲-۲۰۱۳ مورد بررسی قرار گرفته بود. در آن زمان، چندین تیم به دنبال ارتباط دادن ارزش‌های مختلف Bitcoin به UTXOs بودند که منجر به پیاده‌سازی‌های پراکنده متعددی شد. این عدم استانداردسازی و تقاضای کم در آن زمان مانع از آن شد که این راه‌حل‌ها جایگاه پایداری پیدا کنند.


امروز، RGB به دلیل استحکام مفهومی و مشخصات یکپارچه‌اش از طریق انجمن LNP/BP برجسته است. این اصل بر اساس Client-side Validation است. Bitcoin Blockchain تنها تعهدات رمزنگاری (_commitments_، از طریق Taproot یا OP_RETURN) را ذخیره می‌کند، در حالی که اکثر داده‌ها (تعاریف Contract، تاریخچه انتقال‌ها و غیره) توسط کاربران مربوطه ذخیره می‌شود. به این ترتیب، بار ذخیره‌سازی توزیع شده و محرمانگی تبادلات تقویت می‌شود، بدون اینکه Blockchain را سنگین کند. این رویکرد امکان ایجاد دارایی‌های قابل تعویض (**استاندارد RGB20**) یا دارایی‌های منحصر به فرد (**استاندارد RGB21**) را در یک چارچوب مدولار و مقیاس‌پذیر فراهم می‌کند.


### عملکرد token (RGB20) و دارایی‌های منحصر به فرد (RGB21)


با **RGB20**، ما یک token قابل تعویض بر روی Bitcoin تعریف می‌کنیم. صادرکننده یک _عرضه_، یک _دقت_ انتخاب می‌کند و یک _قرارداد_ ایجاد می‌کند که در آن می‌تواند انتقال‌ها را انجام دهد. هر انتقال به یک Bitcoin UTXO ارجاع داده می‌شود که به عنوان یک *Single-Use Seal* عمل می‌کند. این منطق تضمین می‌کند که کاربر قادر نخواهد بود همان دارایی را دوبار خرج کند، زیرا تنها شخصی که قادر به خرج کردن UTXO است در واقع کلید به‌روزرسانی وضعیت Contract سمت مشتری را در اختیار دارد.


**RGB21** دارایی‌های منحصربه‌فرد (یا "NFT") را هدف قرار می‌دهد. دارایی دارای Supply برابر با 1 است و می‌تواند با فراداده (فایل تصویر، صدا و غیره) که از طریق یک فیلد خاص توصیف شده است، مرتبط شود. برخلاف NFTها در بلاکچین‌های عمومی، داده‌ها و شناسه‌های MIME آن‌ها می‌توانند خصوصی باقی بمانند و به صورت همتا به همتا به صلاحدید مالک توزیع شوند.


### راه‌حل Bitmask: یک Wallet برای RGB


برای بهره‌برداری از قابلیت‌های RGB در عمل، پروژه **DIBA** یک Wallet به نام [Bitmask](https://bitmask.app/) طراحی کرده است. ایده این است که یک ابزار غیرامانی مبتنی بر Taproot ارائه شود که به عنوان یک برنامه وب یا افزونه مرورگر قابل دسترسی باشد. Bitmask هم دارایی‌های RGB20 و هم RGB21 را مدیریت می‌کند و مکانیزم‌های امنیتی مختلفی را ادغام می‌کند:




- کد اصلی در Rust نوشته شده است، سپس در WebAssembly کامپایل می‌شود تا در محیط جاوااسکریپت (React) اجرا شود؛
- کلیدها به صورت محلی تولید می‌شوند و سپس به صورت رمزگذاری‌شده در محل ذخیره می‌شوند؛
- داده‌های ایالتی (Stash) در حافظه نگهداری می‌شوند، به صورت سریال و رمزگذاری شده از طریق کتابخانه **Carbonado**، که فشرده‌سازی، تصحیح خطا، رمزگذاری و تأیید جریان را با استفاده از Blake3 انجام می‌دهد.


با تشکر از این معماری، تمام تراکنش‌های دارایی در سمت مشتری انجام می‌شود. از بیرون، تراکنش Bitcoin چیزی بیش از یک تراکنش خرج کردن کلاسیک Taproot نیست، که هیچ‌کس مشکوک نمی‌شود که همچنین انتقال توکن‌های قابل تعویض یا NFTها را حمل می‌کند. عدم وجود بارگذاری بیش از حد On-Chain (عدم ذخیره‌سازی عمومی متادیتا) درجه‌ای از احتیاط را تضمین می‌کند و مقاومت در برابر تلاش‌های احتمالی سانسور را آسان‌تر می‌سازد.


### امنیت و معماری توزیع‌شده


تا جایی که پروتکل RGB نیاز دارد هر شرکت‌کننده تاریخچه تراکنش خود را نگه دارد (برای اثبات اعتبار انتقالاتی که دریافت می‌کند)، مسئله ذخیره‌سازی مطرح می‌شود. بیت‌ماسک پیشنهاد می‌کند که این Stash به صورت محلی سریال‌سازی شود، سپس به چندین سرور یا ابر (اختیاری) ارسال شود. داده‌ها توسط کاربر از طریق **Carbonado** رمزگذاری می‌شوند، بنابراین یک سرور نمی‌تواند آن را بخواند. در صورت فساد جزئی، تصحیح خطای Layer می‌تواند محتوا را بازسازی کند.


استفاده از CRDT (_نوع داده تکراری بدون تعارض_) امکان ادغام نسخه‌های مختلف Stash را فراهم می‌کند، در صورتی که از هم جدا شوند. هر کسی آزاد است این داده‌ها را هر جا که بخواهد میزبانی کند، زیرا هیچ Full node واحدی تمام اطلاعات مرتبط با دارایی را حمل نمی‌کند. این دقیقاً فلسفه *Client-side Validation* را منعکس می‌کند، جایی که هر مالک مسئول ذخیره شواهد اعتبار دارایی RGB خود است.


### به سوی یک اکوسیستم گسترده‌تر: بازار، قابلیت همکاری و عملکردهای جدید


شرکتی که پشت Bitmask است، خود را به توسعه ساده یک Wallet محدود نمی‌کند. DIBA قصد دارد توسعه دهد:




- بازاری برای تبادل توکن‌ها، به‌ویژه به‌صورت **RGB21**؛
- سازگاری با کیف پول‌های دیگر (مانند *Iris Wallet*);
- تکنیک‌های **انتقال دسته‌ای**، یعنی امکان گنجاندن چندین انتقال متوالی RGB در یک تراکنش واحد.


به طور همزمان، ما در حال کار بر روی **WebBTC** یا **WebLN** (استانداردهایی که به وب‌سایت‌ها اجازه می‌دهند از Wallet بخواهند تا تراکنش‌های Bitcoin یا Lightning را امضا کند) هستیم، و همچنین بر روی قابلیت "teleburn" ورودی‌های Ordinals (اگر بخواهیم Ordinals را به یک فرمت RGB که بیشتر محرمانه و انعطاف‌پذیر است، بازگردانیم) کار می‌کنیم.


### نتیجه‌گیری


کل فرآیند نشان می‌دهد که چگونه اکوسیستم RGB می‌تواند از طریق راه‌حل‌های فنی قوی پیاده‌سازی شده و در دسترس کاربران نهایی قرار گیرد. گذار از دیدگاه Altcoin به یک چشم‌انداز متمرکزتر بر Bitcoin، همراه با کشف *Client-side Validation*، مسیر نسبتاً منطقی را نشان می‌دهد: ما درک می‌کنیم که امکان پیاده‌سازی عملکردهای مختلف (توکن‌های قابل تعویض، NFT، قراردادهای هوشمند...) بدون فورک کردن Blockchain وجود دارد، تنها با بهره‌گیری از تعهدات رمزنگاری در تراکنش‌های Taproot یا OP_RETURNs.


**بیت‌ماسک** Wallet بخشی از این رویکرد است: در سمت Blockchain، تنها چیزی که می‌بینید یک تراکنش معمولی Bitcoin است؛ در سمت کاربر، شما یک وب Interface را مدیریت می‌کنید که در آن انواع دارایی‌های off-chain را ایجاد، Exchange و ذخیره می‌کنید. این مدل به وضوح زیرساخت پولی (Bitcoin) را از منطق صدور و انتقال (RGB) جدا می‌کند، در حالی که سطح بالایی از محرمانگی و مقیاس‌پذیری بهتر را تضمین می‌کند.


## کار Bitfinex بر روی RGB


<chapterId>d4d80e07-5eac-5b29-a93a-123180e97047</chapterId>


:::video id=04555813-516f-4eea-9767-7082c2ea6f01:::


در این فصل، بر اساس ارائه‌ای از فردریکو تنگا، به مجموعه‌ای از ابزارها و پروژه‌هایی که توسط تیم بیتفینکس برای RGB ایجاد شده‌اند، می‌پردازیم. هدف این تیم، پرورش ظهور یک اکوسیستم غنی و متنوع در اطراف این پروتکل است. هدف اولیه تیم، انتشار یک محصول تجاری خاص نیست، بلکه ارائه بلوک‌های ساخت نرم‌افزار، مشارکت در خود پروتکل RGB و پیشنهاد مراجع پیاده‌سازی ملموس مانند یک Wallet موبایل (*Iris Wallet*) یا یک نود لایتنینگ سازگار با RGB است.


### زمینه و اهداف


از حدود سال ۲۰۲۲، تیم Bitfinex RGB بر توسعه پشته فناوری که امکان بهره‌برداری و آزمایش کارآمد RGB را فراهم می‌کند، تمرکز کرده است. چندین مشارکت انجام شده است:




- مشارکت در کد منبع و مشخصات پروتکل، شامل نوشتن پیشنهادات بهبود، رفع اشکالات، و غیره؛
- ابزارهایی برای توسعه‌دهندگان به منظور ساده‌سازی یکپارچه‌سازی RGB در برنامه‌هایشان؛
- طراحی یک Wallet موبایل به نام [Iris](https://iriswallet.com/) برای آزمایش و نشان دادن بهترین روش‌ها برای استفاده از RGB؛
- ایجاد یک نود لایتنینگ سفارشی، قادر به مدیریت کانال‌ها با دارایی‌های RGB؛
- حمایت از تیم‌های دیگر در ساخت راه‌حل‌ها بر روی RGB، برای تشویق به تنوع و یک اکوسیستم قوی.


این رویکرد هدف دارد تا کل زنجیره نیازها را پوشش دهد: از کتابخانه سطح پایین (*[RGBlib](https://github.com/RGB-Tools/RGB-lib)*)، که امکان پیاده‌سازی یک Wallet را فراهم می‌کند، تا جنبه تولید (یک نود Lightning، یک Wallet برای اندروید، و غیره).


### کتابخانه RGBlib: ساده‌سازی توسعه برنامه‌های RGB


نکته مهم در دموکراتیزه کردن ایجاد کیف‌پول‌ها و برنامه‌های RGB این است که یک انتزاع به اندازه کافی ساده در دسترس قرار گیرد تا توسعه‌دهندگان نیازی به یادگیری همه چیز درباره منطق داخلی پروتکل نداشته باشند. این دقیقاً هدف **RGBlib** است که به زبان Rust نوشته شده است.


RGBlib به عنوان پلی بین نیازهای بسیار انعطاف‌پذیر (اما گاهی پیچیده) RGB که توانسته‌ایم در فصل‌های قبلی مطالعه کنیم، و نیازهای مشخص یک توسعه‌دهنده برنامه عمل می‌کند. به عبارت دیگر، یک Wallet (یا سرویس) که مایل به مدیریت انتقال‌های token، صدور دارایی، تأیید و غیره است، می‌تواند بدون دانستن هر جزئیات رمزنگاری یا هر پارامتر قابل تنظیم RGB، به RGBlib تکیه کند.


کتابفروشی ارائه می‌دهد:




- عملکردهای کلید در دست برای صدور (_issuance_) دارایی‌ها (قابل تعویض یا غیر قابل تعویض)؛
- توانایی انتقال (ارسال/دریافت) دارایی‌ها با دستکاری اشیاء ساده (آدرس‌ها، مقادیر، UTXOها، و غیره)؛
- مکانیسمی برای ذخیره‌سازی و بارگذاری اطلاعات وضعیت (*محموله‌ها*) مورد نیاز برای Client-side Validation.


بنابراین RGBlib به مفاهیم پیچیده‌ای که مختص RGB (Client-side Validation، لنگرهای Tapret/Opret) هستند، متکی است، اما آن‌ها را به گونه‌ای کپسوله می‌کند که برنامه نهایی نیازی به برنامه‌ریزی مجدد همه چیز یا اتخاذ تصمیمات پرخطر نداشته باشد. علاوه بر این، RGBlib در چندین زبان (Kotlin و Python) از قبل متصل شده است و درهای استفاده‌هایی فراتر از یک جهان ساده Rust را باز می‌کند.


### Iris Wallet: مثالی از یک RGB Wallet بر روی اندروید


برای اثبات اثربخشی RGBlib، تیم Bitfinex **Iris Wallet** را توسعه داده است که در این مرحله به‌طور انحصاری بر روی اندروید موجود است. این یک Wallet موبایل است که تجربه کاربری مشابه با یک Bitcoin Wallet معمولی را نشان می‌دهد: شما می‌توانید یک دارایی صادر کنید، آن را ارسال کنید، دریافت کنید و تاریخچه آن را مشاهده کنید، در حالی که در یک مدل خود نگهداری باقی می‌مانید.


ایریس دارای تعدادی ویژگی جالب است:


**استفاده از سرور الکتروم:**


مانند هر Wallet، آیریس نیاز دارد تا درباره تأییدیه‌های تراکنش در Blockchain بداند. به جای جاسازی یک نود کامل، آیریس به طور پیش‌فرض از یک سرور الکتروم که توسط تیم Bitfinex نگهداری می‌شود، استفاده می‌کند. با این حال، کاربران می‌توانند سرور خود یا یک سرویس شخص ثالث دیگر را پیکربندی کنند. به این ترتیب، تراکنش‌های Bitcoin می‌توانند به صورت ماژولار تأیید و اطلاعات (ایندکس‌گذاری) بازیابی شوند.


**سرور پروکسی RGB:**


برخلاف Bitcoin، RGB نیاز به Exchange از متادیتای off-chain (*محموله‌ها*) بین فرستنده و گیرنده دارد. برای ساده‌سازی این فرآیند، Iris راه‌حلی ارائه می‌دهد که در آن ارتباط از طریق یک سرور پروکسی انجام می‌شود. Wallet گیرنده یک *Invoice* تولید می‌کند که نشان می‌دهد فرستنده باید داده‌های *سمت مشتری* را به کجا ارسال کند. به‌طور پیش‌فرض، URL به یک پروکسی که توسط تیم Bitfinex میزبانی می‌شود اشاره می‌کند، اما شما می‌توانید این پروکسی را تغییر دهید (یا خودتان میزبانی کنید). ایده این است که به یک تجربه کاربری آشنا بازگردیم که در آن گیرنده یک کد QR نمایش می‌دهد و فرستنده این کد را برای تراکنش اسکن می‌کند، بدون هیچ‌گونه دستکاری پیچیده اضافی.


**پشتیبان‌گیری مداوم:**


در یک زمینه کاملاً Bitcoin، پشتیبان‌گیری از seed شما به طور کلی کافی است (اگرچه این روزها توصیه می‌کنیم به جای آن از seed و توصیف‌گرها پشتیبان‌گیری کنید). با RGB، این کافی نیست: شما همچنین باید تاریخچه محلی ( *کنساینمنت‌ها* ) را که ثابت می‌کند واقعاً مالک یک دارایی RGB هستید، نگه دارید. هر بار که یک رسید دریافت می‌کنید، دستگاه داده‌های جدیدی ذخیره می‌کند که برای هزینه‌های بعدی ضروری است. Iris به طور خودکار یک پشتیبان رمزگذاری‌شده را در Google Drive کاربر مدیریت می‌کند. این نیاز به اعتماد خاصی به گوگل ندارد، زیرا پشتیبان رمزگذاری شده است و گزینه‌های قوی‌تری (مانند یک سرور شخصی) برای آینده برنامه‌ریزی شده‌اند تا از هرگونه خطر سانسور یا حذف توسط یک اپراتور ثالث جلوگیری شود.


**ویژگی‌های دیگر:**




- ایجاد یک Faucet برای آزمایش سریع یا توزیع توکن‌ها برای آزمایش یا تبلیغ؛
- یک سیستم صدور گواهینامه (در حال حاضر متمرکز) برای تشخیص token معتبر از یک نمونه تقلبی که یک تیکر معروف را کپی می‌کند. در آینده، این گواهینامه ممکن است به صورت غیرمتمرکزتر (از طریق DNS یا مکانیزم‌های دیگر) شود.


در مجموع، آیریس تجربه کاربری نزدیک به Bitcoin Wallet کلاسیک ارائه می‌دهد و پیچیدگی‌های اضافی (مدیریت Stash، تاریخچه *Consignment* و غیره) را به لطف RGBlib و استفاده از یک سرور پروکسی پنهان می‌کند.


### سرور پروکسی و تجربه کاربری


سرور پروکسی که در بالا معرفی شد، شایسته توضیحات بیشتری است، زیرا کلید یک تجربه کاربری روان است. به جای اینکه فرستنده مجبور باشد *محموله‌ها* را به صورت دستی به گیرنده ارسال کند، تراکنش RGB در پس‌زمینه از طریق یک:




- گیرنده یک *Invoice* تولید می‌کند (که در میان سایر موارد، پروکسی Address را شامل می‌شود)؛
- فرستنده یک پروژه انتقال (*Consignment*) را از طریق یک درخواست HTTP به پروکسی ارسال می‌کند؛
- گیرنده این پروژه را بازیابی می‌کند و اعتبارسنجی *سمت کلاینت* را به صورت محلی اجرا می‌کند؛
- گیرنده سپس از طریق پروکسی، پذیرش (یا احتمالاً رد) State Transition را منتشر می‌کند؛
- فرستنده می‌تواند وضعیت اعتبارسنجی را مشاهده کند و در صورت پذیرش، تراکنش Bitcoin را برای نهایی‌سازی انتقال پخش کند.


به این ترتیب، Wallet تقریباً مانند یک Wallet معمولی عمل می‌کند. کاربر از تمام مراحل میانی بی‌اطلاع است. البته، پروکسی فعلی نه رمزگذاری شده و نه احراز هویت شده است (که نگرانی‌هایی درباره محرمانگی و یکپارچگی ایجاد می‌کند)، اما این بهبودها در نسخه‌های بعدی ممکن است. مفهوم پروکسی همچنان برای بازآفرینی تجربه "من یک کد QR ارسال می‌کنم، شما برای پرداخت اسکن می‌کنید" بسیار مفید است.


### ادغام RGB بر روی Lightning Network


تمرکز کلیدی دیگر کار تیم Bitfinex این است که Lightning Network را با دارایی‌های RGB سازگار کند. هدف این است که کانال‌های Lightning را در USDT (یا هر token دیگر) فعال کنیم و از همان مزایای Bitcoin در Lightning بهره‌مند شویم (تراکنش‌های تقریباً فوری، مسیریابی و غیره). به طور مشخص، این شامل ایجاد یک نود Lightning اصلاح‌شده برای:




- یک کانال باز کنید با قرار دادن نه تنها ساتوشی‌ها، بلکه یک یا چند دارایی RGB در تامین مالی UTXO Multisig؛
- generate Lightning Commitment تراکنش‌ها (Bitcoin سمت) همراه با انتقال‌های حالت مربوطه RGB. هر بار که کانال به‌روزرسانی می‌شود، یک انتقال RGB توزیع دارایی در خروجی‌های Lightning را بازتعریف می‌کند؛
- فعال‌سازی بستن یک‌جانبه، جایی که دارایی در یک UTXO انحصاری بازیابی می‌شود، مطابق با قوانین Lightning Network (HTLC، قفل زمانی، مجازات، و غیره).


این راه‌حل که "**گره رعد و برق RGB**" نامیده می‌شود، از LDK (*کیت توسعه رعد و برق*) به عنوان پایه استفاده می‌کند و مکانیزم‌های لازم برای تزریق توکن‌های RGB به کانال‌ها را اضافه می‌کند. تعهدات رعد و برق ساختار کلاسیک خود را حفظ می‌کنند (خروجی‌های قابل سوراخ، قفل زمانی...) و علاوه بر این Anchor و RGB State Transition (از طریق `Opret` یا `Tapret`). برای کاربر، این راه را به کانال‌های رعد و برق در استیبل‌کوین‌ها یا هر دارایی دیگری که از طریق RGB منتشر شده است، باز می‌کند.


### پتانسیل DEX و تأثیر آن بر Bitcoin


هنگامی که چندین دارایی از طریق Lightning مدیریت می‌شوند، می‌توان تصور کرد که یک **Exchange اتمی** در یک مسیر مسیریابی Lightning واحد، با استفاده از همان منطق اسرار و قفل‌های زمانی، انجام شود. به عنوان مثال، کاربر A دارای Bitcoin در یک کانال Lightning است و کاربر B دارای USDT RGB در کانال دیگری از Lightning است. آن‌ها می‌توانند مسیری ایجاد کنند که کانال‌های آن‌ها را به هم متصل کند و به طور همزمان Exchange BTC را برای USDT مبادله کنند، بدون نیاز به اعتماد. این چیزی جز یک **مبادله اتمی** که در چندین مرحله انجام می‌شود نیست، و شرکت‌کنندگان خارجی تقریباً از این واقعیت که آن‌ها در حال انجام یک معامله هستند، نه فقط مسیریابی، بی‌خبر هستند. این رویکرد ارائه می‌دهد:




- تاخیر بسیار کم، زیرا همه چیز به صورت off-chain بر روی Lightning باقی می‌ماند.
- یک **حریم خصوصی** برتر: هیچ‌کس نمی‌داند که این یک معامله است و نه یک مسیریابی عادی؛
- اجتناب از frontrunning، یک مشکل مکرر برای On-Chain DEX؛
- کاهش هزینه‌ها (شما هزینه بلاک‌اسپیس را پرداخت نمی‌کنید، فقط کارمزدهای مسیریابی لایتنینگ را می‌پردازید).


سپس می‌توانیم یک اکوسیستم را تصور کنیم که در آن نودهای لایتنینگ قیمت‌های مبادله‌ای ارائه می‌دهند (با فراهم کردن نقدینگی). هر نود، در صورت تمایل، می‌تواند نقش _بازارساز_ را ایفا کند و به خرید و فروش دارایی‌های مختلف بر روی لایتنینگ بپردازد. این چشم‌انداز یک DEX _لایه-۲_ این ایده را تقویت می‌کند که نیازی به Fork یا استفاده از بلاکچین‌های شخص ثالث برای دستیابی به مبادلات دارایی غیرمتمرکز نیست.


تأثیر بر Bitcoin می‌تواند مثبت باشد: زیرساخت‌های لایتنینگ (نودها، کانال‌ها و خدمات) به لطف حجم‌های تولید شده توسط این *استیبل‌کوین‌ها*، مشتقات و توکن‌های دیگر به طور کامل‌تری مورد استفاده قرار می‌گیرند. بازرگانانی که به پرداخت‌های USDT در لایتنینگ علاقه‌مند هستند، به صورت مکانیکی پرداخت‌های BTC در لایتنینگ را کشف خواهند کرد (که توسط همان پشته مدیریت می‌شود). نگهداری و تأمین مالی زیرساخت Lightning Network نیز می‌تواند از تکثیر این جریان‌های غیر BTC بهره‌مند شود، که به طور غیرمستقیم به نفع کاربران Bitcoin خواهد بود.


### نتیجه‌گیری و منابع


تیم Bitfinex که به RGB اختصاص داده شده است، از طریق کار خود، تنوع آنچه را که می‌توان بر روی پروتکل انجام داد نشان می‌دهد. از یک سو، RGBlib وجود دارد، یک کتابخانه که طراحی کیف‌پول‌ها و برنامه‌ها را تسهیل می‌کند. از سوی دیگر، ما Iris Wallet را داریم، یک نمایش عملی بر روی اندروید از یک Interface کاربر نهایی مرتب. در نهایت، ادغام RGB با Lightning نشان می‌دهد که کانال‌های استیبل‌کوین ممکن هستند و راه را برای یک DEX غیرمتمرکز بالقوه بر روی Lightning باز می‌کند.


این رویکرد همچنان به طور عمده تجربی است و به تکامل خود ادامه می‌دهد: کتابخانه RGBlib در حال بهبود است، Iris Wallet به طور منظم بهبود می‌یابد، و نود اختصاصی Lightning هنوز یک کلاینت اصلی Lightning نیست.


برای کسانی که مایل به یادگیری بیشتر یا مشارکت هستند، منابع متعددی در دسترس است، از جمله:




- [GitHub RGB Tools repositories](https://github.com/RGB-Tools);
- [یک سایت اطلاعاتی اختصاص داده شده به Iris Wallet](https://iriswallet.com/) برای تست Wallet بر روی اندروید.


در فصل بعدی، نگاهی دقیق‌تر به نحوه راه‌اندازی یک نود RGB Lightning خواهیم داشت.


## RLN - RGB نود لایتنینگ


<chapterId>ecaabe32-20ba-5f8c-8ca1-a3f095792958</chapterId>


:::video id=d1e9753e-6093-4a47-bcdc-da1aebaefffc:::


در این فصل نهایی، فردریکو تنگا شما را به صورت گام به گام از طریق راه‌اندازی یک نود Lightning RGB در محیط Regtest راهنمایی می‌کند و به شما نشان می‌دهد که چگونه توکن‌های RGB را بر روی آن ایجاد کنید. با راه‌اندازی دو نود جداگانه، شما همچنین خواهید آموخت که چگونه یک کانال Lightning بین آنها و دارایی‌های Exchange RGB باز کنید.


این ویدیو به عنوان یک آموزش عمل می‌کند، مشابه آنچه در فصل قبلی پوشش دادیم، اما این بار به طور خاص بر روی Lightning متمرکز است!


منبع اصلی برای این ویدیو مخزن گیت‌هاب [RGB Lightning Node](https://github.com/RGB-Tools/RGB-lightning-node) است که راه‌اندازی این پیکربندی در Regtest را برای شما آسان می‌کند.


### استقرار یک نود Lightning سازگار با RGB


این فرآیند تمام مفاهیم مطرح شده در فصل‌های قبلی را در بر می‌گیرد و به کار می‌گیرد:




- ایده‌ای که **UTXO** مسدود شده بر روی یک کانال لایتنینگ 2/2 Multisig می‌تواند نه تنها بیت‌کوین دریافت کند، بلکه می‌تواند یک Single-Use Seal از دارایی‌های RGB (قابل تعویض یا غیرقابل تعویض) نیز باشد؛
- افزودن، در هر تراکنش تعامل Lightning، یک خروجی (`Tapret` یا `Opret`) اختصاص داده شده به لنگر انداختن RGB State Transition؛
- زیرساخت‌های مرتبط (bitcoind/indexer/proxy) برای اعتبارسنجی تراکنش‌های Bitcoin و داده‌های *سمت‌کاربر* Exchange.


### معرفی RGB-lightning-node


پروژه **`RGB-lightning-node`** یک Rust daemon است که بر اساس `Rust-lightning` (LDK) Fork اصلاح شده تا وجود دارایی‌های RGB در یک کانال را در نظر بگیرد. هنگامی که یک کانال باز می‌شود، می‌توان حضور دارایی‌ها را مشخص کرد و هر بار که وضعیت کانال به‌روزرسانی می‌شود، یک انتقال RGB ایجاد می‌شود که توزیع دارایی در خروجی‌های Lightning را منعکس می‌کند. این امکان را فراهم می‌سازد:




- کانال‌های لایتنینگ را در USDT باز کنید، برای مثال؛
- این توکن‌ها را از طریق شبکه مسیریابی کنید، به شرطی که مسیرهای مسیریابی دارای نقدینگی کافی باشند؛
- از منطق مجازات و قفل زمانی Lightning بدون تغییر بهره‌برداری کنید: به سادگی Anchor انتقال RGB را در یک خروجی اضافی از Commitment Transaction انجام دهید.


کد هنوز در مرحله آلفا است: ما توصیه می‌کنیم از آن فقط در **regtest** یا روی **Testnet** استفاده کنید.


### نصب Node


برای کامپایل و نصب باینری `RGB-lightning-node`، ابتدا با کلون کردن مخزن و زیر-ماژول‌های آن شروع می‌کنیم، سپس اجرا می‌کنیم:


```bash
git clone https://github.com/RGB-Tools/rgb-lightning-node --recurse-submodules --shallow-submodules
```


![RGB-Bitcoin](assets/fr/098.webp)




- گزینه `--recurse-submodules` همچنین زیر دستگاه‌های ضروری (از جمله نسخه اصلاح‌شده `Rust-lightning`) را کلون می‌کند؛
- گزینه `--shallow-submodules` عمق کلون را محدود می‌کند تا سرعت دانلود افزایش یابد، در حالی که همچنان دسترسی به کامیت‌های ضروری را فراهم می‌کند.


از ریشه پروژه، فرمان زیر را برای کامپایل و نصب باینری اجرا کنید:


```bash
cargo install --locked --debug --path .
```


![RGB-Bitcoin](assets/fr/099.webp)




- `--locked` تضمین می‌کند که نسخه وابستگی‌ها به‌طور دقیق رعایت شود؛
- `--debug` اجباری نیست، اما می‌تواند به شما کمک کند تمرکز کنید (می‌توانید از `--release` استفاده کنید اگر ترجیح می‌دهید)؛
- `--path .` به `cargo install` می‌گوید که از دایرکتوری فعلی نصب کند.


در پایان این دستور، یک فایل اجرایی `RGB-lightning-node` در مسیر `$CARGO_HOME/bin/` شما موجود خواهد بود. اطمینان حاصل کنید که این مسیر در `$PATH` شما قرار دارد تا بتوانید دستور را از هر دایرکتوری اجرا کنید.


### الزامات عملکرد


برای عملکرد، `RGB-lightning-node` daemon نیاز به حضور و پیکربندی دارد:




- یک **`bitcoind`** گره


هر نمونه RLN نیاز خواهد داشت تا با `bitcoind` ارتباط برقرار کند تا تراکنش‌های On-Chain خود را پخش و نظارت کند. احراز هویت (ورود/رمز عبور) و URL (میزبان/پورت) باید به daemon ارائه شود.




- یک ایندکسر** (Electrum یا Esplora)


daemon باید بتواند تراکنش‌های On-Chain را فهرست کرده و بررسی کند، به‌ویژه برای یافتن UTXO که یک دارایی بر روی آن لنگر انداخته است. شما باید URL سرور Electrum یا Esplora خود را مشخص کنید.




- یک پروکسی **RGB**


همان‌طور که در فصل‌های قبلی مشاهده شد، **سرور پروکسی** یک مؤلفه (اختیاری، اما به شدت توصیه می‌شود) برای ساده‌سازی Exchange *محموله‌ها* بین همتایان Lightning است. بار دیگر، باید یک URL مشخص شود.


شناسه‌ها و URLها زمانی وارد می‌شوند که daemon از طریق API _باز شود_. اطلاعات بیشتر در ادامه.


### راه‌اندازی Regtest


برای استفاده‌ی ساده، یک اسکریپت `regtest.sh` وجود دارد که به‌صورت خودکار، از طریق Docker، مجموعه‌ای از سرویس‌ها را راه‌اندازی می‌کند: `bitcoind`، `electrs` (ایندکسر)، `RGB-proxy-server`.


![RGB-Bitcoin](assets/fr/100.webp)


این به شما امکان می‌دهد یک محیط محلی، ایزوله و از پیش پیکربندی شده را راه‌اندازی کنید. این کار کانتینرها و دایرکتوری‌های داده را در هر بار راه‌اندازی مجدد ایجاد و حذف می‌کند. ما با شروع از:


```bash
./regtest.sh start
```


این اسکریپت انجام خواهد داد:




- یک دایرکتوری `docker/` ایجاد کنید تا ذخیره شود؛
- اجرای `bitcoind` در regtest، به همراه ایندکسر `electrs` و `RGB-proxy-server`;
- صبر کنید تا همه چیز آماده استفاده شود.


![RGB-Bitcoin](assets/fr/101.webp)


سپس، چندین گره RLN را راه‌اندازی خواهیم کرد. در شل‌های جداگانه، به عنوان مثال اجرا کنید (برای راه‌اندازی 3 گره RLN):


```bash
# 1st shell
rgb-lightning-node dataldk0/ --daemon-listening-port 3001 \
--ldk-peer-listening-port 9735 --network regtest
# 2nd shell
rgb-lightning-node dataldk1/ --daemon-listening-port 3002 \
--ldk-peer-listening-port 9736 --network regtest
# 3rd shell
rgb-lightning-node dataldk2/ --daemon-listening-port 3003 \
--ldk-peer-listening-port 9737 --network regtest
```


![RGB-Bitcoin](assets/fr/102.webp)




- پارامتر `--network regtest` نشان‌دهنده استفاده از پیکربندی regtest است؛
- `--daemon-listening-port` نشان می‌دهد که نود لایتنینگ بر روی کدام پورت REST برای فراخوانی‌های API (JSON) گوش خواهد داد؛
- `--ldk-peer-listening-port` مشخص می‌کند که به کدام پورت Lightning P2P گوش دهد؛
- `dataldk0/`، `dataldk1/` مسیرهای دایرکتوری‌های ذخیره‌سازی هستند (هر گره اطلاعات خود را به‌طور جداگانه ذخیره می‌کند).


شما همچنین می‌توانید دستورات را از مرورگر خود بر روی گره‌های RLN خود اجرا کنید:


```url
https://rgb-tools.github.io/rgb-lightning-node/
```


برای اینکه یک نود یک کانال باز کند، ابتدا باید بیت‌کوین‌ها را بر روی یک Address که با فرمان زیر تولید شده است، داشته باشد (برای مثال، برای نود شماره 1):


```bash
curl -X POST http://localhost:3001/address
```


پاسخ به شما یک Address ارائه خواهد داد.


![RGB-Bitcoin](assets/fr/103.webp)


در `bitcoind` Regtest، قصد داریم چند بیت‌کوین استخراج کنیم. اجرا کنید:


```bash
./regtest.sh mine 101
```


![RGB-Bitcoin](assets/fr/104.webp)


ارسال وجه به نود Address که در بالا ایجاد شده است:


```bash
./regtest.sh sendtoaddress <address> <amount>
```


![RGB-Bitcoin](assets/fr/105.webp)


سپس یک بلاک را استخراج کنید تا تراکنش تأیید شود:


```bash
./regtest.sh mine 1
```


![RGB-Bitcoin](assets/fr/106.webp)


### راه‌اندازی Testnet (بدون Docker)


اگر می‌خواهید یک سناریوی واقعی‌تر را آزمایش کنید، می‌توانید به جای Regtest، 3 گره RLN را بر روی Testnet راه‌اندازی کنید و به خدمات عمومی اشاره کنید:


```bash
rgb-lightning-node dataldk0/ --daemon-listening-port 3001 \
--ldk-peer-listening-port 9735 --network testnet
rgb-lightning-node dataldk1/ --daemon-listening-port 3002 \
--ldk-peer-listening-port 9736 --network testnet
rgb-lightning-node dataldk2/ --daemon-listening-port 3003 \
--ldk-peer-listening-port 9737 --network testnet
```


به طور پیش‌فرض، اگر هیچ پیکربندی یافت نشود، daemon سعی خواهد کرد از:




- `bitcoind_rpc_host`: `electrum.iriswallet.com`
- `bitcoind_rpc_port`: `18332`
- indexer_url`: `ssl://electrum.iriswallet.com:50013`
- `proxy_endpoint`: `rpcs://proxy.iriswallet.com/0.2/json-RPC`


با ورود:




- `bitcoind_rpc_username`: `user`
- `bitcoind_rpc_username`: `password`


شما همچنین می‌توانید این Elements را از طریق API `init`/`unlock` سفارشی‌سازی کنید.


### صدور یک RGB token


برای صدور token، ابتدا با ایجاد UTXOهای "قابل رنگ‌آمیزی" شروع خواهیم کرد:


```bash
curl -X POST -H "Content-Type: application/json" \
-d '{
"up_to": false,
"num": 4,
"size": 2000000,
"fee_rate": 4.2,
"skip_sync": false
}' \
http://localhost:3001/createutxos
```


![RGB-Bitcoin](assets/fr/107.webp)


البته می‌توانید ترتیب را تغییر دهید. برای تأیید تراکنش، ما استخراج می‌کنیم:


```bash
./regtest.sh mine 1
```


اکنون می‌توانیم یک دارایی RGB ایجاد کنیم. فرمان بستگی به نوع دارایی که می‌خواهید ایجاد کنید و پارامترهای آن دارد. در اینجا من یک دارایی NIA (*دارایی غیرقابل تورم*) token به نام "PBN" با یک Supply از 1000 واحد ایجاد می‌کنم. `precision` به شما اجازه می‌دهد تا قابلیت تقسیم‌پذیری واحدها را تعریف کنید.


```bash
curl -X POST -H "Content-Type: application/json" \
-d '{
"amounts": [
1000
],
"ticker": "PBN",
"name": "Plan B Network",
"precision": 0
}' \
http://localhost:3001/issueassetnia
```


![RGB-Bitcoin](assets/fr/108.webp)


پاسخ شامل شناسه دارایی جدید ایجاد شده است. به یاد داشته باشید که این شناسه را یادداشت کنید. در مورد من، این است:


```txt
rgb:fc7fMj5S-8yz!vIl-260BEhU-Hj1skvM-ZHcjfyz-RTcWc10
```


![RGB-Bitcoin](assets/fr/109.webp)


سپس می‌توانید آن را به On-Chain منتقل کنید یا در یک کانال Lightning تخصیص دهید. این دقیقاً همان کاری است که در بخش بعدی انجام خواهیم داد.


### باز کردن یک کانال و انتقال دارایی RGB


ابتدا باید نود خود را با استفاده از دستور `/connectpeer` به یک همتا در Lightning Network متصل کنید. در مثال من، هر دو نود را کنترل می‌کنم. بنابراین کلید عمومی نود دوم لایتنینگ خود را با این دستور بازیابی خواهم کرد:


```bash
curl -X 'GET' \
'http://localhost:3002/nodeinfo' \
-H 'accept: application/json'
```


دستور کلید عمومی نود شماره ۲ من را برمی‌گرداند:


```txt
031e81e4c5c6b6a50cbf5d85b15dad720fec92c62e84bafb34088f0488e00a8e94
```


![RGB-Bitcoin](assets/fr/110.webp)


سپس، کانال را با مشخص کردن دارایی مربوطه (`PBN`) باز خواهیم کرد. فرمان `/openchannel` به شما اجازه می‌دهد اندازه کانال را به ساتوشی تعریف کنید و انتخاب کنید که دارایی RGB را شامل شود. این بستگی به چیزی دارد که می‌خواهید ایجاد کنید، اما در مورد من، فرمان به این صورت است:


```bash
curl -X POST -H "Content-Type: application/json" \
-d '{
"peer_pubkey_and_opt_addr": "031e81e4c5c6b6a50cbf5d85b15dad720fec92c62e84bafb34088f0488e00a8e94@localhost:9736",
"capacity_sat": 1000000,
"push_msat": 10000000,
"asset_amount": 500,
"asset_id": "rgb:fc7fMj5S-8yz!vIl-260BEhU-Hj1skvM-ZHcjfyz-RTcWc10",
"public": true,
"with_anchors": true,
"fee_base_msat": 1000,
"fee_proportional_millionths": 0,
"temporary_channel_id": "a8b60c8ce3067b5fc881d4831323e24751daec3b64353c8df3205ec5d838f1c5"
}' \
http://localhost:3001/openchannel
```


اینجا بیشتر بدانید:




- `peer_pubkey_and_opt_addr`: شناسه همتایی که می‌خواهیم به آن متصل شویم (کلید عمومی که قبلاً پیدا کردیم)؛
- `capacity_sat`: کل ظرفیت کانال به ساتوشی؛
- `push_msat`: مقدار به میلی‌ساتوشی که در ابتدا هنگام باز شدن کانال به همتا منتقل می‌شود (اینجا من بلافاصله 10,000 Sats منتقل می‌کنم تا او بتواند بعداً یک انتقال RGB انجام دهد)؛
- `asset_amount`: مقدار دارایی‌های RGB که باید به کانال اختصاص داده شود؛
- `asset_id`: شناسه منحصربه‌فرد دارایی RGB که در کانال درگیر است؛
- `عمومی`: نشان می‌دهد که آیا کانال باید برای مسیریابی در شبکه عمومی شود یا خیر.


![RGB-Bitcoin](assets/fr/111.webp)


برای تأیید تراکنش، ۶ بلاک استخراج می‌شوند:


```bash
./regtest.sh mine 6
```


![RGB-Bitcoin](assets/fr/112.webp)


کانال Lightning اکنون باز است و همچنین شامل 500 توکن `PBN` در سمت نود شماره 1 می‌باشد. اگر نود شماره 2 بخواهد توکن‌های `PBN` دریافت کند، باید generate و Invoice را انجام دهد. در اینجا نحوه انجام آن آمده است:


```bash
curl -X POST -H "Content-Type: application/json" \
-d '{
"amt_msat": 3000000,
"expiry_sec": 420,
"asset_id": "rgb:fc7fMj5S-8yz!vIl-260BEhU-Hj1skvM-ZHcjfyz-RTcWc10",
"asset_amount": 100
}' \
http://localhost:3002/lninvoice
```


با:




- `amt_msat`: مقدار Invoice به میلی‌ساتوشی (حداقل 3000 Sats)؛
- `expiry_sec`: زمان انقضا Invoice به ثانیه؛
- `asset_id`: شناسه دارایی RGB مرتبط با Invoice؛
- `asset_amount`: مقدار دارایی RGB که باید با این Invoice منتقل شود.


در پاسخ، شما یک RGB Invoice دریافت خواهید کرد (همان‌طور که در فصل‌های قبلی توضیح داده شد):


```txt

```


![RGB-Bitcoin](assets/fr/113.webp)


ما اکنون این Invoice را از اولین گره که دارای وجه نقد لازم با `PBN` token است، پرداخت خواهیم کرد:


```bash
curl -X POST -H "Content-Type: application/json" \
-d '{
"invoice": ""
}' \
http://localhost:3001/sendpayment
```


![RGB-Bitcoin](assets/fr/114.webp)


پرداخت انجام شده است. این موضوع را می‌توان با اجرای دستور زیر تأیید کرد:


```bash
curl -X 'GET' \
'http://localhost:3001/listpayments' \
-H 'accept: application/json'
```


![RGB-Bitcoin](assets/fr/115.webp)


در اینجا نحوه استقرار یک نود لایتنینگ که برای حمل دارایی‌های RGB اصلاح شده است، آورده شده است. این نمایش بر اساس:




- یک محیط regtest (از طریق `./regtest.sh`) یا Testnet؛
- یک نود لایتنینگ (`RGB-lightning-node`) بر پایه یک `bitcoind`، یک ایندکسر و یک `RGB-proxy-server`;
- مجموعه‌ای از APIهای JSON REST برای باز کردن/بستن کانال‌ها، صدور توکن‌ها، انتقال دارایی‌ها از طریق Lightning و غیره.


با تشکر از این فرآیند:




- تراکنش‌های تعامل رعد و برق شامل یک خروجی اضافی (OP_RETURN یا Taproot) با لنگر انداختن یک انتقال RGB هستند؛
- انتقالات دقیقاً به همان شیوه پرداخت‌های سنتی لایتنینگ انجام می‌شوند، اما با افزودن یک RGB token؛
- می‌توان چندین گره RLN را به هم متصل کرد تا پرداخت‌ها را در مسیرهای مختلف گره‌ها هدایت و آزمایش کرد، به شرطی که نقدینگی کافی هم در بیت‌کوین و هم در دارایی RGB در مسیر وجود داشته باشد.


پروژه همچنان در مرحله آلفا قرار دارد. بنابراین اکیداً توصیه می‌شود که خود را به محیط‌های آزمایشی محدود کنید (regtest، Testnet).


فرصی که با این سازگاری LN-RGB ایجاد شده‌اند قابل توجه است: استیبل‌کوین‌ها بر روی لایتنینگ، DEX Layer-2، انتقال توکن‌های قابل تعویض یا NFT با هزینه بسیار کم... فصل‌های قبلی معماری مفهومی و منطق اعتبارسنجی را توضیح داده‌اند. اکنون شما دید عملی از چگونگی راه‌اندازی چنین نودی برای توسعه‌ها یا آزمایش‌های آینده خود دارید.


# بخش نهایی


<partId>b0baebfc-d146-5938-849a-f835fafb386f</partId>


## بررسی‌ها و رتبه‌بندی‌ها


<chapterId>0217e8b0-942a-5fee-bd91-9a866551eff3</chapterId>


<isCourseReview>true</isCourseReview>

## نتیجه‌گیری


<chapterId>0309536d-c336-56a0-869e-a8395ed8d9ae</chapterId>


<isCourseConclusion>true</isCourseConclusion>
---
term: OP_BOOLOR (0X9B)
definition: Opcode-ی که یک عمل 'یا' منطقی (OR) روی دو مقدار بالای استک انجام می‌دهد.
---

رفتار یک گیت منطقی `OR` را تقلید می‌کند. دو مقدار را از بالای پشته می‌گیرد و اگر یکی یا هر دو از Elements غیر صفر باشند، `1` را برمی‌گرداند. در غیر این صورت، `0` را برمی‌گرداند.
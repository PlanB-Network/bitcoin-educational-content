---
term: Schema
---

在 RGB 协议中，Schema 是一段声明性代码，它定义了所有变量、规则和管理 Contract 运行的 Business Logic（*Business Logic*）。它规定了状态结构，包括在 Genesis 期间建立的初始配置，并详细说明了允许的转换类型以及验证转换的严格条件。Schema 作为规范框架，确保 Contract State 的任何演变都符合签发者制定的规则。
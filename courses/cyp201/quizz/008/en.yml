question: Why does SHA256 apply a preprocessing to the input message?
answer: To ensure a length multiple of 512 bits
wrong_answers:
  - To verify the integrity of the message
  - To encrypt the data before hashing
  - To increase the size of the generated hashes
explanation: |
  The preprocessing adjusts the message length by adding padding bits and a binary representation of its length.
reviewed: false
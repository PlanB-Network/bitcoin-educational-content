---
term: BRANCH (GIT)
---

In the context of Git, represents a separation from the main workflow, allowing parallel development without affecting the main branch (usually called `master` or `main`). Branches enable modifications, tests and experiments to be carried out in an isolated environment, before being integrated into the main project via a merge.
---
term: 支部（Bitcoin）
---

在 Bitcoin 中，分支是一个有效区块序列，其中每个新区块都与前一个区块相关联。Blockchain 代表累积工作最多的分支，但分支可能会产生相互竞争的分支。每个分支至少与主分支共享一个共同块，即 Genesis 块。区块中与分支相关的部分有时被称为 "段"。当出现两个相互竞争的分支时，诚实的节点会同步处理累积工作量最多的分支。如果一个分支比另一个分支有更多的累积工作量，则该分支有时被称为 "强 "分支，否则称为 "弱 "分支。
---
term: CONDENSAT
---

在密码学中，指对一组数据应用密码 Hash 函数后产生的结果（或输出）。浓缩物是一串大小固定的字符，一般用十六进制符号（16 进制）表示为一系列数字和字母。这种结果的特殊性在于，它对输入数据几乎是唯一和特定的，因此输入数据的微小变化都会产生完全不同的浓缩结果。加密 Hash 函数被设计成单向和抗碰撞的，因此很难从浓缩结果中恢复原始数据，也很难发现两个不同的输入会产生相同的浓缩结果。
# Lessons — si

## 2026-07-03
- For Sinhala Simplicity/type-theory quizzes with no Sinhala glossary entries, keep specialized terms such as `combinator`, `product type`, `sum type`, `unit type`, `value`, `input`/`output`, `jets`, `DAG`, `static analysis`, and `gas model` in English, translating only the surrounding prose.
- If a YAML scalar starts with a quoted English term such as `'take'`, wrap the entire Sinhala value in double quotes so the embedded single quotes remain text and the YAML stays valid.
- The `scr403/quizz` si set follows an OLDER, shifted numbering: some English quizzes already have an exact si translation under a different number (e.g. en `017` = si `018`, en `025` = si `027`). Before translating a quiz, grep existing `*/si.yml` for the topic and reuse the wording verbatim for consistency; the offset is NOT constant.
- Register: keep technical nouns/identifiers in English inside Sinhala sentence frames — `Simplicity`, `combinator`, `branch`, `input`/`output`, effect names (`Failure`/`Reader`/`Writer` effect), `jet`, `Taproot`, `CMR`, `type`, `signature`, `exception`, `commutative`, `idempotent`. Translate ordinary verbs/adjectives (e.g. `predictable` → `පුරෝකථනය කළ හැකි`, `useful` → `ප්‍රයෝජනවත්`, `important` → `වැදගත්`). Do NOT add invented `(English gloss)` parentheticals not present in the source.
- YAML gotcha: a plain scalar cannot start with `'`. When a question naturally begins with a quoted term (e.g. `'case' combinator ...`), wrap the whole value in double quotes; this keeps valid YAML without changing the parsed structure.
- Render `e.g.,` as `උදා.,` (matches existing si files).
- In Sinhala Simplicity material, keep protocol/product/function names and effect names in English: Simplicity, SimplicityHL, Taproot/TapLeaf/Tapscript, Schnorr, CMR, jets, Reader effect, Failure effect, `bip0340-verify`, `sig-all-hash`, `PrecomputedTransactionData`.
- Translate “combinator” as “සංයෝජකය” in prose, but keep code-like combinators/access notation such as `take`, `drop`, `iden`, O/I/H verbatim.
- For Simplicity/formal-methods quiz terms such as `commutative`, `idempotent`, `unitary`, `jet`, `CMR`, `NUMS`, and combinator names, keep the English term in Sinhala prose and attach Sinhala particles/suffixes around it; translating them risks obscuring the term-of-art.

## 2026-07-06
- For Sinhala SOC104 Bitcoin-origin quizzes, keep the exact genesis-block newspaper headline (`The Times 03/Jan/2009 Chancellor on the brink of a second bank bailout`) verbatim because it is a historical embedded message, not ordinary prose to localize.
- For SOC104 Sinhala political-theory quizzes, follow the existing code-switching register: keep ideological families and terms of art such as `totalitarianism`, `despotism`, `secular religions`, `moral hazard`, `libertarian`, `conservative`, `socialist`, `centrist`, `state`, and `private property` in English, translating the surrounding Sinhala prose.
- For Sinhala SOC104 political quizzes, keep term-of-art labels and proper political categories such as `Nolan diagram`, `Nolan diamond`, `Libertarianism`, `Conservatism`, `Socialism`, `Centrism`, `statism`, `classical liberalism`, `anarcho-capitalists`, `minarchists`, and `fiat` in English inside Sinhala sentence frames; translate ordinary explanatory prose around them.
- For SOC/political-philosophy quizzes, keep specialized ideology and theory labels in English inside Sinhala sentence frames when no Sinhala glossary precedent exists: `libertarian`, `constructivism`, `spontaneous order`, `individualism`, `collectivism`, `open society`, `closed society`, `pro-business`, and `pro-market`; translate surrounding prose and ordinary verbs.
- For SOC104 Sinhala quizzes, match existing register by keeping political-science terms in English inside Sinhala frames: `conservatism`, `libertarianism`, `centrism`, `state`, `policy`, `free market`, `interventionism`, `technocracy`, `non-aggression principle`, `minarchist`, `anarcho-capitalist`.
- For SOC104 Sinhala political quizzes, keep core Anglo-American political terms in English inside Sinhala sentence frames when no Sinhala glossary entry exists: `libertarian`, `liberal`, `statism`, `state intervention`, `paleo-libertarianism`, `conservatism`, `laissez-faire`, and named institutions/parties.
- Translate directional labels normally when they function as political camps: left/right → වම/දකුණ; but preserve compound labels like `left-wing`, `right-wing`, `far-right` when used as term-of-art adjectives if clarity would suffer.
- For SOC104 Sinhala political-philosophy quizzes, keep ideological terms such as `libertaire`, `libertarian`, `socialism`, `structural definition`, `intentional definition`, `non-aggression principle`, `holism`, and `Green New Deal` in English inside Sinhala sentence frames.
- Preserve the distinction between French `libertaires` and `libertarians`; do not translate both as a generic Sinhala “libertarian” equivalent.
- Keep technical political-economy nouns such as `private property`, `self-ownership`, `spoliation`, `state intervention`, `civil disobedience`, and `class struggle` in English when matching existing SOC104 Sinhala register.

question: 字节替换操作（"S-Box"）在AES加密中扮演什么角色？
answer: 它通过用另一个字节替换每个字节来引入混淆
wrong_answers:
  - 它通过检查消息字节来确保完整性
  - 它通过打乱列来增加扩散
  - 它将明文分割成128位块
explanation: |
  AES中的字节替换操作（S-Box）通过根据一个表将每个字节替换为一个不同的值来引入混淆，使加密文本更难以分析。
---
term: OP_NOTIF (0X64)
definition: Opcode که اگر شرط بالا نادرست باشد، بخشی از script را اجرا می‌کند (برعکس OP_IF).
---

به صورت مخالف با `OP_IF` عمل می‌کند و بخش بعدی اسکریپت را اجرا می‌کند اگر مقدار در بالای پشته صفر (نادرست) باشد.
---
term: OUTPOINT（外点）

---
对未用的交易输出（UTXO）的独特引用。它由两个元素组成：

- `txid`：创建输出的交易标识符；
- `vout`：交易中的输出索引。

这两个元素的组合可精确识别 UTXO。例如，如果一个交易的 `txid` 为 `abc...123`，而输出索引为 `0`，则输出点将被记为：

```text
abc...123:0
```

外点用于新交点的输入（`vin`），以指示正在使用的 UTXO。

> ► *“外点”这一词通常与 “UTXO” 同义*。

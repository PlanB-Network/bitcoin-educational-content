---
term: LITTLE-ENDIAN
---

Data storage format in computer systems where the least significant bytes ("bits") are placed first in address order. In a multi-byte sequence, the byte with the lowest weight (for example, the rightmost digits in hexadecimal) is stored first.
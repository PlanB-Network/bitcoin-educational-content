---
term: BRANCH (GIT)
---

In the context of Git, a branch represents a separation from the main workflow, enabling parallel development without affecting the primary branch (usually called `master` or `main`). Branches allow developers to make changes, test features, and experiment in an isolated environment before eventually integrating them into the main project through a merge.
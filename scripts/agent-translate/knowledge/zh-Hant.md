# Lessons — zh-Hant

## 2026-07-03
- Terminology (Simplicity/type-theory): combinator=組合子, sum type=和型別, product type=積型別, unit type=單位型別, boolean=布林, carry=進位, half-adder=半加器, static analysis=靜態分析, completeness theorem=完備性定理, type former=型別建構子, DAG=有向無環圖(DAG).
- Keep verbatim: combinator names (iden, unit, comp, pair, case, take, drop, injl, injr, scribe), code formulas (comp f g, f ▵ g, and ▵ xor), math symbols (𝟙, 𝟚, σᴸ⟨⟩, ⟨a, b⟩, A⁴ = A² × A²), protocol/tech names (Simplicity, Liquid Network, Bitcoin Script, Ethereum, SHA-256, Merkle, Rocq), and "jets"/"jet" (untranslated).
- YAML gotcha: if a translated value begins with a quote char (e.g. a term like 'take' fronting the sentence), the whole value must be wrapped in double quotes, otherwise YAML mis-parses it as a quoted scalar.
- For `explanation: >-` folded scalars, put the whole Chinese text on ONE indented line — folded newlines insert a stray ASCII space between CJK characters otherwise.
- scr403 (Simplicity) terminology, anchored to `courses/scr403/zh-Hant.md`: `組合子`=combinator, `副作用`=effect (keep effect names English: `Failure 副作用`, `Reader 副作用`), `序列演算`=sequent calculus, `承諾 Merkle Root`=Commitment Merkle Root, `見證`/`見證資料`=witness, `型別`=type, `和型別`/`積型別`/`單位型別`, `修剪`=prune, `委派`=delegation, `遞迴契約`=recursive covenant, `不動點`=fixed-point.
- Keep verbatim: combinator/identifier names (`case`, `iden`, `take`, `drop`, `jet`), API names (`bip0340-verify`, `sig-all-hash`, `PrecomputedTransactionData`, `TxEnv`), `Bit Machine`, `De Bruijn`, `Curry-Howard`, `lambda 演算`, versions (`0xbe`), math/type signatures (`A × C ⊢ D`, `2^256`, `Xᑉ⁸`), tag literals (`Simplicity␟Commitment␟iden`).
- For folded YAML block scalars (`explanation: >-`), put the whole Chinese translation on a single 2-space-indented line: line folding would otherwise insert spurious spaces between Chinese characters.
- Preserve each source key's quoting style exactly (some quizzes double-quote `question`/`answer`/`wrong_answers`, others use plain scalars); Chinese values need no extra quoting since they contain no `": "` or leading YAML indicators.
- Distinguish `簽名` (type signature) from `簽章` (cryptographic signature).
- scr403 (Simplicity) quiz `answer`/`wrong_answers` values are frequently pure Simplicity code (e.g. `case (injl iden) (injr iden)`) — keep the ENTIRE value verbatim, don't translate. Only mixed code+prose values (type sig `—` gloss) get the prose part translated.
- Keep all Simplicity combinators and notation verbatim: `iden unit comp pair case take drop injl injr scribe copair jet fold map zip push-<n pop-<n verify bip0340-verify sig-all-hash`, and glyphs `⊢ × + σᴸ σᴿ ▵ ⨾ ⟨⟩ Aⁿ Aᑉⁿ #ᶜ tag∥tag`. Also kept in English: Simplicity, Bit Machine, Rocq, SimplicityHL, CMR, NUMS, Failure/Reader/Writer (effects), midstate, jet.
- Canonical zh-Hant renderings (from `courses/scr403/zh-Hant.md`): combinator=組合子, expression=運算式, sum/product/unit type=和/積/單位型別, half-adder/full-adder=半加器/全加器, carry bit=進位位元, sum bit=和位元, buffer=緩衝區, vector=向量, overflow=溢位, total/partial function=全函數/偏函數, effect=效果(副作用), commutative/idempotent/unitary=可交換/冪等/單位性, completeness=完備性, witness=見證資料, pruned=修剪, Merkle root=Merkle Root, Merkle tree=Merkle 樹, tweak=微調 (per glossary), macro=巨集, r/s-component=r/s 分量.
- For YAML folded scalars (`explanation: >-`), write the whole translation on ONE indented line. Line-wrapping a folded block in Chinese would fold newlines into spurious ASCII spaces between CJK characters.
- Mirror the source's quoting: keep a value quoted only if the source quoted it (values with `：`/`:`). Use fullwidth punctuation （，；：——） in prose so plain scalars never hit a `: ` or leading-indicator YAML pitfall.
- Simplicity technical primitives are best kept in English when they are identifiers or named constructs (`iden`, `unit`, `case`, `witness`, jets, CMR), while explanatory type-theory prose can use 繁中 terms such as 組合子、和型別、積型別、單位型別、序列演算。
- For Simplicity effects, keep the effect names in English and translate the category word: Failure 效應、Reader 效應、Writer 效應; translate the properties as 可交換、冪等、單位.
- Keep named Bitcoin/Taproot path terms in English inside technical prose when they are part of protocol vocabulary (`key-spend path`, `script path`, `internal key`, `tweak`, `tagged hash`) unless the sentence is purely explanatory.

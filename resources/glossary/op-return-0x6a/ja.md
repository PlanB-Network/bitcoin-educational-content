---
term: OP_RETURN (0X6A)

definition: アウトプットを支出不可能にするOpcodeで、任意のデータを記録するために頻繁に使用されます。
---
無効なスクリプトであることを示し、そのスクリプトを含む出力は、証明可能 に使用不可能になる。そのため、ネットワークノードはこの出力をUTXOセットから削除することができる。OP_RETURN`はブロックチェーンに任意のデータを記録するためによく使われる。
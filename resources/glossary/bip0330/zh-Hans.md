---
term: BIP0330
---

一项名为 "*Erlay*"的提案，旨在优化 Bitcoin 网络中未确认事务的传播。目前，事务会广播到一个节点的所有对等节点，导致冗余和带宽过度使用。BIP330 建议将这种广播默认限制在 8 个对等节点，然后使用一种调和机制来有效分享丢失的事务。Erlay 可减少约 40% 的带宽消耗。它还避免了带宽消耗随着连接到节点的对等节点数量的增加而呈线性增长。
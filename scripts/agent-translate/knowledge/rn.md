# Lessons — rn

## 2026-07-03
- For Rundi Simplicity material, keep protocol/library terms such as Simplicity, Taproot, Liquid, Schnorr, witness, jet, combinator, CMR, Reader, Failure, and Writer in English; translate the surrounding explanatory prose.
- Preserve mathematical/type-theory vocabulary in English when it is part of a formal term (`sum type`, `product type`, `unit type`, `sequent calculus`, `type inference`) and explain it in Kirundi around it rather than inventing unstable calques.
- For Simplicity material in Rundi, keep combinator/operator names and type expressions verbatim (`case`, `pair`, `take/drop`, `A × B ⊢ ...`, `⟨...⟩`, `#ᶜ(...)`); translate only the surrounding prose.
- Follow the existing SCR403 Rundi convention of keeping `Reader`, `Writer`, `Failure`, `environment`, `witness`, `CMR`, `NUMS`, `Taproot`, `jet`, and `SimplicityHL` as technical terms rather than localising them.
- Render signature prose as `umukono` / `imikono`, but keep fixed protocol terms such as `Schnorr`, `BIP-0341`, `key-spend`, `key-path`, `internal key`, and `private key` unchanged when they identify Bitcoin/Taproot mechanisms.
- Keep Simplicity core combinator names (`iden`, `unit`, `comp`, `pair`, `case`, `take`, `drop`, `injl`, `injr`) verbatim; translate the surrounding role as `umuteranyi` / `abateranyi`.
- Render `static analysis` as `isesengura ridahinduka` and `dynamic gas model` as `uburyo bwa gas buhindagurika`.
- For Simplicity type theory, use `ubwoko bw'igiteranyo` for `sum type`, `ubwoko bw'igikubo` for `product type`, and keep technical primitives like `boolean`, `unit`, `DAG`, `jet`, `hash`, and protocol names unchanged when no canonical Rundi glossary term exists.
- For Rundi Simplicity quizzes, keep formal names and identifiers such as `case`, `take`, `drop`, `iden`, `sig-all-hash`, `bip0340-verify`, CMR, TapLeaf, Reader, Failure, and SimplicityHL in English; translate only the surrounding explanation.
- Preserve formal type-theory terms such as `sum`, `product`, `unit`, `option type`, `sequent calculus`, and `type inference` in English when they are naming concepts, matching the existing SCR403 Rundi course style.

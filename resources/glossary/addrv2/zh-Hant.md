---
term: ADDRV2
---

建議與 BIP155 一起演進 Bitcoin 網路上的 `addr` 訊息。`addr` 訊息是用來廣播接受傳入連線的節點地址，但它只限於 128 位元的地址。這個大小對於 IPv6、IPv4 和 Tor V2 位址是足夠的，但對於其他通訊協定則不足夠。更新後的版本 `addrv2` 旨在支援更長的位址，包括 256 位元的 Tor v3 隱藏服務，以及其他網路通訊協定，例如 I2P 或未來的通訊協定。
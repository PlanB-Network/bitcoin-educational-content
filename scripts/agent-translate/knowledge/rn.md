# Lessons — rn

## 2026-07-03
- For Rundi Simplicity material, keep protocol/library terms such as Simplicity, Taproot, Liquid, Schnorr, witness, jet, combinator, CMR, Reader, Failure, and Writer in English; translate the surrounding explanatory prose.
- Preserve mathematical/type-theory vocabulary in English when it is part of a formal term (`sum type`, `product type`, `unit type`, `sequent calculus`, `type inference`) and explain it in Kirundi around it rather than inventing unstable calques.
- For Simplicity material in Rundi, keep combinator/operator names and type expressions verbatim (`case`, `pair`, `take/drop`, `A × B ⊢ ...`, `⟨...⟩`, `#ᶜ(...)`); translate only the surrounding prose.
- Follow the existing SCR403 Rundi convention of keeping `Reader`, `Writer`, `Failure`, `environment`, `witness`, `CMR`, `NUMS`, `Taproot`, `jet`, and `SimplicityHL` as technical terms rather than localising them.
- Render signature prose as `umukono` / `imikono`, but keep fixed protocol terms such as `Schnorr`, `BIP-0341`, `key-spend`, `key-path`, `internal key`, and `private key` unchanged when they identify Bitcoin/Taproot mechanisms.
- Keep Simplicity core combinator names (`iden`, `unit`, `comp`, `pair`, `case`, `take`, `drop`, `injl`, `injr`) verbatim; translate the surrounding role as `umuteranyi` / `abateranyi`.
- Render `static analysis` as `isesengura ridahinduka` and `dynamic gas model` as `uburyo bwa gas buhindagurika`.
- For Simplicity type theory, use `ubwoko bw'igiteranyo` for `sum type`, `ubwoko bw'igikubo` for `product type`, and keep technical primitives like `boolean`, `unit`, `DAG`, `jet`, `hash`, and protocol names unchanged when no canonical Rundi glossary term exists.
- For Rundi Simplicity quizzes, keep formal names and identifiers such as `case`, `take`, `drop`, `iden`, `sig-all-hash`, `bip0340-verify`, CMR, TapLeaf, Reader, Failure, and SimplicityHL in English; translate only the surrounding explanation.
- Preserve formal type-theory terms such as `sum`, `product`, `unit`, `option type`, `sequent calculus`, and `type inference` in English when they are naming concepts, matching the existing SCR403 Rundi course style.

## 2026-07-06
- For Rundi SOC104 political-philosophy quizzes, keep ideology labels such as `libertarian`, `libertarianism`, `conservatism`, `centrism`, `socialism`, `minarchist`, `anarcho-capitalist`, `neoconservative`, and `paleoconservative` in English-derived form when they function as course terms; translate the surrounding prose.
- Keep comparative labels such as `pro-business`, `pro-market`, and formal economic/political terms like `fiat`, `gold standard`, `Federal Reserve`, `technocracy`, `laissez-faire`, `corporatism`, and `crony capitalism` verbatim or English-derived for consistency when no stable Rundi term is evident.
- For SOC104 Rundi social/political material, render `privacy` as `ubuzima bwite` in prose; keep `Cypherpunk Manifesto` and quoted slogans such as `Cypherpunks write code` in English.
- Translate `cash` as `amafaranga akoreshwa mu ntoke` when contrasting physical anonymous money with digital payments.
- Keep historically exact embedded Bitcoin text, such as the genesis block Times headline, byte-identical inside quotes while translating the surrounding explanation.
- Use `amafaranga ya Fiat` for fiat currencies, following the Rundi glossary term `Fiat`.
- For SOC104 political-philosophy quizzes in Rundi, render `libertarian(s)` as `uwuharanira/abaharanira umwidegemvyo`, while keeping fixed labels such as `liberal`, `libertarian`, `paleo-libertarianism`, `neo-libertarianism`, and institution names in English when they identify movements or organizations.
- Render `statism/statist(s)` descriptively as support for or use of `ububasha bwa Leta` rather than inventing a single unstable Kirundi noun; keep `Statism` when the question names the doctrine.
- For U.S. federalism in Rundi SOC material, render `states` as `intara zigize igihugu` where needed to avoid confusing them with generic `Leta`.
- For SOC104 Rundi political quizzes, keep distinction terms `libertaires`, `libertarian`, and `libertarianism` in English/French form to avoid collapsing the course's central contrast.
- Render `private property` consistently as `umutungo bwite`, `state` as `Leta`, `freedom/liberty` as `umwidegemvyo`, and `socialism` as `sosiyalizimu`.
- For political theory labels without stable Kirundi equivalents, keep terms such as `holism`, `Green New Deal`, `materialisme historique`, and `proletariat` while translating the explanatory prose around them.
- For SOC104 Rundi political quizzes, render political families consistently as `aba-libertarian`, `abasosiyalisite`, `abakonservateur`, and `abari hagati`; keep `moral hazard` in English and explain it in Kirundi around the term.
- Render `totalitarianism` as `ubutegetsi bw'igitugu bugenzura vyose`, `secular religions` as `amadini adashingiye ku Mana`, and `minimum wage` as `umushahara fatizo`.
- For SOC104/Rundi political quizzes, render left/right as `ibubamfu`/`iburyo`, `Nolan diagram` as `igishushanyo ca Nolan`, and `personal/economic freedoms` as `umwidegemvyo w'umuntu ku giti ciwe` / `umwidegemvyo w'ubutunzi`.
- Keep political-theory labels such as `liberalism`, `libertarianism`, `statism`, `conservatism`, `socialism`, `centrism`, `anarcho-capitalists`, and `minarchists` in English when they name formal positions; translate the surrounding explanation.
- Render `fiat money/currency` as `amafaranga/ifaranga fiat`, keeping `fiat` unchanged.
- For SOC104 Rundi political quizzes, keep analytical labels such as `individualism`, `collectivism`, `constructivism`, `pro-business`, and `pro-market` in English when used as fixed doctrine/position names; translate the surrounding explanation.
- Render Hayek's `central planning` consistently as `igenamigambi riva hagati` / `igenamigambi ry'ubutunzi riva hagati`, matching the SOC104 course wording.

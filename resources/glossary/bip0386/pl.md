---
term: BIP0386
---

Wprowadza funkcje deskryptora dla Taproot. Definiuje funkcje `tr(KEY)` i `tr(KEY, TREE)` do znajdowania wyjść Taproot, gdzie `KEY` jest kluczem wewnętrznym, a `TREE` jest opcjonalnym drzewem ścieżek skryptu. BIP386 został zaimplementowany w Bitcoin Core w wersji 22.0.
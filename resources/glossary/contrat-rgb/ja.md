---
term: Contract RGB
---

RGBプロトコルを介して複数のアクター間でデジタル的に実行される権利のセットを指す。アクティブな状態と、Schemaによって定義されるBusiness Logicを持ち、どの操作（転送、拡張など）が許可されるかを指定する。Contractの状態と有効性のルールはSchemaで表現される。任意の時点で、Contractは、このSchemaと（例えばAluVMで実行される）検証スクリプトによって許可されることに従ってのみ進化する。
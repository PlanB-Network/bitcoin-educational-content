---
term: OP_ELSE (0X67)
definition: Opcode-ی که در صورت برقرار نبودن شرط قبلی، دستورات بعدی را اجرا می‌کند.
---

جریان اجرای یک اسکریپت شرطی را تغییر می‌دهد: نشان می‌دهد که عملیات‌های پس از آن باید اجرا شوند اگر شرط قبلی مشخص شده توسط `OP_IF`، `OP_NOTIF`، یا یک `OP_ELSE` دیگر برآورده نشده باشد.
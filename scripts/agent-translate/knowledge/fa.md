# Lessons — fa

## 2026-07-03
- در محتوای Simplicity، نام‌های پروتکل/سازه مانند Simplicity، SimplicityHL، CMR، Commitment Merkle Root، TapLeaf، jets، effects، Reader/Failure، witness و نام combinatorها مثل `take`/`drop`/`iden` را انگلیسی نگه دارید و فقط نقش یا توضیح پیرامونی را فارسی کنید.
- برای covenant مطابق واژه‌نامه فارسی از «کاوننت» استفاده کنید؛ در صورت نیاز به توضیح عمومی می‌توان «پیمان» را فقط در متن توضیحی به‌کار برد، اما در پاسخ‌های فنی «کاوننت» سازگارتر است.
- برای “nine” در کنار «ترکیب‌گر/قاعده» از «نُه» استفاده کنید تا با «نه» نفی اشتباه خوانده نشود.
- برای Simplicity، اصطلاح `combinator` را «ترکیب‌گر»، `sum type` را «نوع جمع»، `product type` را «نوع حاصل‌ضرب»، و `unit type` را «نوع واحد» ترجمه کن؛ نام‌های `Simplicity`، `Bitcoin Script`، `Taproot`، `Liquid Network` و شناسه‌های کدی را انگلیسی نگه دار.
- برای اصطلاحات اثرها، `side effect` را «اثر جانبی»، `Reader/Failure/Writer effect` را با نام انگلیسی اثر نگه دار، `commutative` را «جابه‌جاپذیر»، `unitary` را «واحدی»، و `idempotent` را به‌صورت انگلیسی نگه دار مگر متن آموزشی صراحتاً معادل فارسی بخواهد.
- محتوای داخل fenced code blockها را حتی وقتی شامل توضیح انگلیسی داخل `{...}` است ترجمه نکن؛ فقط متن بیرون code blockها ترجمه شود.
- برای محتوای Simplicity، نام ترکیب‌گرها و شناسه‌ها (`iden`, `comp`, `case`, `take`, `drop`, `injl`, `injr`, `pair`, `unit`, `scribe`, `fold`, `map`, `jet`) را انگلیسی و verbatim نگه دارید؛ فقط نثر اطرافشان را فارسی کنید.
- در مدارها، `full-adder` و `half-adder` در نثر به «جمع‌کنندهٔ کامل» و «نیم‌جمع‌کننده» ترجمه می‌شوند، اما داخل عبارات/نام‌های کدی مثل `half-adder⟨1,1⟩` یا مسیرهای combinator verbatim می‌مانند.
- برای اصطلاحات اثرها، `commutative` را «جابجایی‌پذیر»، `idempotent` را «ایدمپوتنت»، و `unitary` را «واحدی» ترجمه کنید؛ نام اثرهای `Reader`، `Writer` و `Failure` را انگلیسی نگه دارید.
- برای Taproot/Simplicity، اختصارات و نام‌های پروتکل مانند `CMR`, `NUMS`, `UTXO`, `Taproot`, `Schnorr`, `BIP-0341` و `key-spend` را verbatim نگه دارید مگر وقتی عبارت روشن فارسی لازم است، مثل «خرج از مسیر کلید» برای توضیح key-spend.
- برای Simplicity نام زبان/پروتکل را لاتین نگه دار؛ مفاهیم نوعی را به‌صورت «نوع جمعی»، «نوع ضربی» و «نوع واحد» ترجمه کن.
- نام ترکیب‌گرها و عبارت‌های Simplicity مانند `iden`, `unit`, `case`, `take`, `drop`, `injl`, `injr`, `comp f g` و `and ▵ xor` باید verbatim بمانند؛ فقط واژه عمومی `combinator` را «ترکیب‌گر» ترجمه کن.
- برای jets در Simplicity از «جت‌ها» استفاده کن و در توضیح فنی آن را «پیاده‌سازی بومی» بنام؛ `Merkle root` را «ریشه Merkle» نگه دار.
- برای Ethereum gas در فارسی از «گس» استفاده کن، نه «بنزین» یا ترجمه تحت‌اللفظی.

## 2026-07-06
- در محتوای فلسفه سیاسی soc104، `constructivism` را «سازه‌گرایی» ترجمه کن؛ آن را با «ساختارگرایی» که معادل `structuralism` است خلط نکن. `anti-constructivism` را «ضد سازه‌گرایی» بنویس.
- برای این دوره، `libertarian(s)` را «لیبرتارین/لیبرتارین‌ها»، `welfare state` را «دولت رفاه»، `fiscal system` را «نظام مالیاتی»، و `monetary monopoly` را «انحصار پولی» ترجمه کن.
- در محتوای SOC104، برای تمایز English `libertarian` از French `libertaire`، `libertarian/libertarianism` را «لیبرترین/لیبرترینیسم» و `libertaire` را «لیبرتِر» نگه دارید تا دو سنت سیاسی با هم ادغام نشوند.
- برای `intentional definitions` در تحلیل نظام‌های سیاسی از «تعاریف نیت‌محور» استفاده کنید، در برابر `structural definitions` به‌صورت «تعاریف ساختاری».
- برای `holism` در زمینه فلسفه سیاسی، خود اصطلاح را «هولیسم» نگه دارید و در توضیح آن از «کل‌گرایی/کل تقسیم‌ناپذیر» استفاده کنید.
- در محتوای علوم سیاسی SOC104، `totalitarianism` را «تمامیت‌خواهی»، `libertarian` را «لیبرتارین»، `centrist` را «میانه‌رو»، `conservative` را «محافظه‌کار» و `socialist` را «سوسیالیستی/سوسیالیست» ترجمه کن.
- برای `moral hazard` در بحث یارانه‌ها از «مخاطره اخلاقی» استفاده کن.
- در محتوای فلسفهٔ سیاسی SOC، `libertarian` را «لیبرتارین» نگه دارید تا با «لیبرال» یا معنای عام «آزادی‌خواه» خلط نشود.
- برای Hayekian `constructivism` از «سازه‌گرایی» و برای `spontaneous order` از «نظم خودجوش» استفاده کنید.
- برای `pro-business` و `pro-market` در تمایز سیاست اقتصادی، «حامی کسب‌وکار» و «حامی بازار» را به‌کار ببرید.
- در بافت کانت، `minority` را «صغارت» و `Sapere aude` را لاتین نگه دارید؛ معنای آن را «جرئت کن برای خود بیندیشی» ترجمه کنید.
- برای محتوای فلسفه سیاسی در SOC104، `libertarian` را «لیبرتارین»، `libertarianism` را «لیبرتارینیسم»، `minarchist` را «مینارشیست»، و `anarcho-capitalist` را «آنارکو-کاپیتالیست» ترجمه کنید.
- برای گرایش‌های محافظه‌کاری، `neoconservative` را «نومحافظه‌کار» و `paleoconservative` را «پالئومحافظه‌کار» به‌کار ببرید؛ اگر متن درباره برچسب تاریخی انگلیسی صحبت می‌کند، خود برچسب انگلیسی را verbatim نگه دارید.
- برای سیاست و اقتصاد، `centrism` را «میانه‌گرایی»، `technocracy` را «تکنوکراسی»، `laissez-faire` را «لسه‌فر»، `sound money` را «پول سالم»، و `crony capitalism` را «سرمایه‌داری رفاقتی» ترجمه کنید.
- در محتوای سیاسی SOC104، `statism` را «دولت‌گرایی» و `statist(s)` را «دولت‌گرا/دولت‌گرایان» ترجمه کن.
- برای `libertarian` از «لیبرتارین» استفاده کن؛ ترکیب‌های `paleo-libertarianism` و `neo-libertarianism` را «پالئو-لیبرتارینیسم» و «نئو-لیبرتارینیسم» بنویس.
- نام مؤسسه‌ها و احزاب مانند `Cato Institute`، `Mises Institute` و `Libertarian Party` را لاتین نگه دار، اما نقش‌ها و توضیح پیرامونی را فارسی کن.
- برای `spontaneous order` از «نظم خودجوش» و برای `laissez-faire` خود واژهٔ لاتین `laissez-faire` را در متن فارسی نگه دار.
- برای SOC/نمودار Nolan، `libertarianism` را «لیبرترینیسم» و `libertarian` را «لیبرترین» نگه دار؛ `statism` را «دولت‌گرایی»، `minarchist` را «مینارشیست»، و `anarcho-capitalist` را «آنارکو-کاپیتالیست» ترجمه کن.
- برای `fiat money system` در متن اقتصادی از «سیستم پول فیات» استفاده کن و Fiat/فیات را مطابق کاربرد فارسی واژه‌نامه و فایل‌های قبلی حفظ کن.

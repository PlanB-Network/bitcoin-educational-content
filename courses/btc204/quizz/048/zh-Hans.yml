question: 什么是比特币交易中的OP_RETURN？

answer: 一个标记输出为无效并允许数据存储的操作码。

wrong_answers:
  - 一个在比特币交易中验证签名有效性的操作码。
  - 一个包含指向存储在IPFS上文件的超链接的操作码。
  - 一个从比特币区块链中删除交易的命令。

explanation: |
  OP_RETURN是一个表示脚本无效的操作码，使其包含的输出被证明为不可花费。因此，网络节点可以从它们的UTXO集合中移除这个输出。OP_RETURN常被用来在区块链中记录任意数据。

reviewed: false
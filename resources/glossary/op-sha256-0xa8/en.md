---
term: OP_SHA256 (0XA8)
definition: Opcode hashing the element at the top of the stack with SHA256.
---

Takes the top element of the stack and replaces it with its hash using the `SHA256` function.


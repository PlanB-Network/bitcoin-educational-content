---
term: MERKLE ROOT (哈希树根)

---
哈希树的哈希值或 “顶部哈希”，表示树中所有信息的哈希值。哈希树是一种加密累加器结构，有时也被称为 “哈希树”。在比特币中，哈希树用于组织区块内的交易，并方便快速验证特定交易是否包含在内。因此，在比特币区块中，哈希树根是通过连续对交易进行哈希函数得到的，直到只剩下一个哈希值（Merkle 根）。然后将其包含在相应区块的头部。这种哈希树也可以在 UTTREEXO（一种可以压缩UTXO节点集的结构）和 MAST Taproot 中找到。

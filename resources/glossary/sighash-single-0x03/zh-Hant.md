---
term: sighash_single (0x03)
---

在 Bitcoin 交易簽章中使用的 SigHash Flag 種類，表示簽章適用於交易的所有輸入，並且只適用於一個輸出，與簽章的相同輸入的索引相對應。因此，使用 `SIGHASH_SINGLE` 签名的每个输入都与特定的输出特别相关。其他的輸出不會被簽章所承諾，因此可以在之後修改。在複雜的交易中，參與者希望將某些輸入連結到特定的輸出，同時為交易的其他輸出留有彈性，這種類型的簽章非常有用。